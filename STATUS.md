# État du projet — Pokemon Tactics

> **Dernière release : [v2026.8.2](https://github.com/kekel87/pokemon-tactics/releases/tag/v2026.8.2) (2026-08-29)** — **Phase 6.5 « Client jouable » close, les 3 lots publiés.** Contrôles tactiles (un tap agit, pinch → crans de zoom, boussole tapable, « Annuler » sur les 6 phases annulables) ; contrôles clavier & manette complets (curseur de plateau, navigation des menus DOM, focus qui survit au re-rendu, liseré visible, glyphes de prompt selon la source active) ; écran de remapping (2 slots par action, capture de touche, échange automatique de conflit, pads non standard débloqués) ; menu de combat en cours de partie **et** au placement (Reprendre / Paramètres / Recommencer / Abandonner / Quitter, ce dernier gardant la partie reprenable) ; refonte de l'écran de sélection d'équipe (formats en segments toujours visibles, Humain/IA à deux états, une modale d'équipe par camp) + Team Builder et ses 3 sélecteurs jouables à la manette ; panoramique caméra au clavier (pavé numérique + repli Maj+flèches) ; légende de contrôles près de la boussole + glyphe de touche sous chaque bouton du chrome et sur les listes qui défilent ; modificateurs contextuels dans l'infobulle d'attaque (puissance/précision effectives, mention de brûlure) ; match nul sur K.O. simultanés d'une même résolution ; migration i18n complète du journal de combat (234 clés) ; typecheck des tests verrouillé sur les 8 paquets ; séquence d'intro reproductible + bande-annonce et captures de publication. CI gate full vert (3955 unit + 388 intégration + 23 scénario) et **suite e2e complète 519/519** jouée avant publication. itch-deploy OK (build 2249).
>
> **Release précédente : [v2026.8.1](https://github.com/kekel87/pokemon-tactics/releases/tag/v2026.8.1) (2026-08-19)** — **Lot 3 du plan-cadre 173 clos.** Preview de combat à la confirmation d'attaque (dégâts min–max + %, verdict K.O., précision/critique effectifs, puces de modificateurs, cycle multi-cibles), panneau d'info de case (terrain, hauteur, hazards, champ, zones), InfoPanel allié enrichi (types, stats effectives, talent, nature), information ennemie cachée (PV en %, objet et talent révélés à l'usage), tooltip d'attaque enrichi + noms de type unifiés (`getTypeName`), auras en anneaux permanents au sol, responsive mobile + manifeste PWA + plein écran + wake lock, reprise d'un combat en cours par rejeu du journal, IA positionnement ring-out (offensif + défensif), Zone Magique neutralise tous les objets registry, +6 OP sets (209). CI gate full vert (3729 unit + 388 intégration + 20 scénario + 419 e2e). itch-deploy OK.

> MAJ : 2026-09-03 — **Deux correctifs télémétrie sur la ligne de visite, plus un correctif de comptage côté Worker.** Incident du matin : le tableau de bord itch.io affichait 2 « Browser Plays » pour le 2026-09-03 alors que la base D1 ne portait **aucune** ligne ce jour-là. Diagnostic vérifié de bout en bout : le Worker est en ligne et fonctionne (sonde live sur le build déployé GitHub Pages → `POST /e` répond `204`, ligne écrite en base puis supprimée pour ne pas polluer le relevé) ; la base ne contenait que 11 lignes, toutes du 2026-09-02 (tests humains de la veille). Le 2026-09-02, 4 visites itch captées contre ~3 « Browser Plays » comptés par itch — la chaîne marche, le trou porte sur les 2 plays du 2026-09-03. **Correctif 1 (`#888`)** : `initTelemetry()` appelle désormais `flushSession()` immédiatement au boot du bundle, plus seulement en fin de page. ⚠️ **Le mécanisme causal donné en première rédaction était faux, corrigé par revue de code le jour même** : ce ping ne peut PAS couvrir un joueur qui referme pendant le téléchargement des 4,3 Mo de `main.js` plus Babylon — `initTelemetry()` s'exécute depuis le corps de `babylon-boot.ts`, dont le graphe d'imports statiques inclut Babylon ; en sémantique ESM, ce graphe est intégralement chargé et évalué **avant** la première instruction du corps du module. Ce que ce correctif couvre réellement : les visites où le bundle a fini de charger mais où le beacon de fin de page n'est jamais parti (bug WebKit sur `visibilitychange`, onglet tué par iOS, éviction du bfcache, iframe itch démontée par la page parente) — plausiblement la vraie cause du trou, puisque 3 des 4 visites itch de la veille étaient sur Safari iOS. **Correctif 2 (`#889`, décidé après le correctif 1)** : une **balise de visite inline** injectée dans `index.html` par un greffon Vite (`visitBeaconPlugin`, `packages/app/vite.config.ts`), qui s'exécute avant le téléchargement du bundle et couvre donc réellement le cas de fermeture pendant le chargement. Anti-double-comptage : elle ne pose `window.__pokemonTacticsVisitSent` que si `sendBeacon` a mis la requête en file ; `initTelemetry()` lit ce drapeau et renonce à sa propre ligne `first` s'il est posé. Source de vérité unique dans `packages/app/src/analytics/telemetry-contract.ts` (endpoint, hôtes de publication, paliers d'écran, nom du drapeau), clé de langue dans `packages/app/src/i18n/storage-key.ts`. **Correctif 3** : `packages/telemetry-worker/src/report.ts` comptait les six axes d'audience (pays, navigateur, système, langue, palier d'écran, référent) **par ligne** — avec deux lignes `session` par visite, ces axes doublaient (même bug que « 8 versions pour 4 visites » du 2026-09-02, réintroduit ailleurs). Corrigé : comptés sur la ligne `first` uniquement, sauf la source d'entrée (structurellement absente de la ligne de boot, comptée sur la ligne suivante). **Contrepartie assumée** : une visite réelle produit désormais 2 lignes au lieu d'une, et `inputSource` est **toujours** `null` sur cette première ligne — structurel, pas une approximation. 30 tests unitaires verts dans `packages/app/src/analytics/telemetry.test.ts`, dont 3 nouveaux. Décisions `#888`, `#889`. **Reste à faire, pas encore fait** : `deploy.yml` (GitHub Pages) et `itch-deploy.yml` ne se déclenchent que sur release ou `workflow_dispatch` manuel — les correctifs **ne sont pas en ligne**, les deux déploiements restent à relancer à la main.
>
> MAJ : 2026-09-02 (fin de journée) — **Lot A (télémétrie) quasi clos.** `docs/plans/196-telemetrie-cloudflare-workers.md` reste `in-progress` mais 7 de ses 9 étapes sont faites (0, 1, 2, 3, 4, 5, 7, 8) et la 8ᵉ colonne vertébrale de la Phase 8 (équilibrage) est déjà exploitable. Seule l'**étape 6 (vérification en production) est partielle**. **Livré depuis la MAJ du matin** (commits `934d110`, `dbc9421`, `b920dc3` + travail non commité) : le Worker est **en ligne** (`https://pokemon-tactics-telemetry.kekel87.workers.dev`), la collecte `POST /e` est **vérifiée en production sur itch.io et GitHub Pages** ; un **relevé live protégé par mot de passe** (`GET /tableau`, auth HTTP basique, secret `dashboardPassword` posé par `wrangler secret put`) affiche fréquentation, audience, entonnoir, cartes et formats, avec sélecteur de fenêtre 7/30/90/365 jours (pas de série adapté) ; **`pnpm stats`** sort en terminal les statistiques d'usage (Pokemon, talents, objets tenus, attaques, causes de K.O.) — seule matière d'équilibrage disponible pour la Phase 8 ; `src/report.ts` est un **module partagé** (agrégation + rendu) importé à la fois par le Worker (dashboard) et par le script — un seul générateur, deux consommateurs ; endpoint de collecte renommé `src/index.ts` → `src/worker.ts`. **Goatcounter retiré du bundle** (plugin Vite supprimé, vérifié au build) — le **compte Goatcounter lui-même n'est pas fermé** (action humaine différée, il faut exporter les données d'abord). Décisions **#880-886** ajoutées à `docs/decisions.md`. **Ce qui n'est pas fait, à ne pas annoncer comme fait** : **aucune partie n'a été menée jusqu'au bout** en production — zéro ligne `battle_ended` en base, donc tout ce que cet événement porte (attaques réellement lancées, tour et cause des K.O., durée, tours) reste **non éprouvé** ; le **workflow GitHub de déploiement du Worker** (étape 5 bis) n'existe pas, le déploiement reste **manuel** (`wrangler deploy`, jeton d'API en secret de dépôt à poser — action humaine) ; **anomalie non résolue** : les builds CI estampillent la version `-dirty` alors qu'un checkout est propre — le build est désormais instrumenté pour dire quels fichiers sont sales, mais aucun run de CI ne l'a encore rapporté, cause inconnue ; la **suite e2e complète** n'a pas été rejouée depuis le correctif `pagehide` (seul le smoke, 2 tests, est passé). Détail complet, écarts au plan : `docs/plans/196-telemetrie-cloudflare-workers.md`.
>
> MAJ : 2026-09-02 (matin) — **Phase 7 démarrée par le Lot A (télémétrie).** `docs/plans/195-phase7-multijoueur-telemetrie.md` et `docs/plans/196-telemetrie-cloudflare-workers.md` passent de `draft` à `in-progress`. **Compte Cloudflare créé** (`account_id fb522b06e2c2d12bfa3657f32a4fd44a`) et **base D1 `pokemon-tactics-events` créée** (`database_id f0a2fca1-e016-4537-82c3-2f6cb8d53eca`, région WEUR) — étape 0 du plan 196 soldée. **Étape 1 (spike) FAITE** : le POST `sendBeacon` passe sur itch.io **et** GitHub Pages (origines mesurées : `https://html-classic.itch.zone`, `https://kekel87.github.io`). **Le diagnostic du plan 114 est réfuté** : l'iframe itch n'est pas sandboxée (`sandbox` vaut `null`) et le document du jeu ne porte aucune CSP — le blocage historique de Goatcounter venait des bloqueurs de publicité côté navigateur des joueurs, pas d'un sandboxing itch. **Étape 2 (paquet) LIVRÉE** : nouveau paquet `packages/telemetry-worker/` — `wrangler.toml`, `migrations/0001_init.sql`, `src/index.ts` (endpoint `POST /e`), `src/validate.ts` et `src/visitor.ts` (fonctions pures), 64 tests unitaires ; **pas de script `build`** (un Worker se déploie, il ne se build pas) ; dépendances `wrangler` et `@cloudflare/workers-types` ajoutées à la racine. Décisions **#880** (`battle_id` éphémère par partie retenu — tranche la question restée ouverte depuis le 2026-08-31) et **#881** (POST `sendBeacon` + garde-fou `Origin`, et réfutation du plan 114). **Reste à faire, pas encore fait** : le Worker n'est **pas déployé**, la migration n'est **pas appliquée** sur la base distante, le client de jeu n'est **pas câblé** (étapes 3 à 8 restantes du plan 196), et **Goatcounter est toujours en place** (son retrait est l'étape 7, volontairement après vérification en production). **Prochaine action concrète** : étapes 3-4 (client `telemetry.ts` + câblage des trois événements `session`/`battle_started`/`battle_ended`), puis déploiement.
>
> MAJ : 2026-08-31 — **Phase 7 planifiée (pas démarrée) : plans 195 (cadre) et 196 (Lot A télémétrie détaillé), statut `draft`, aucun code écrit.** Le cadrage du 2026-08-29 (décisions #862-870, `docs/multiplayer.md` v2) est découpé en lots exécutables : `docs/plans/195-phase7-multijoueur-telemetrie.md` (Lot A télémétrie, Lot B multijoueur P2P 1v1 en 4 tranches — transport+lobby / combat / robustesse / désync —, Lot C réduit à l'écran de victoire enrichi ; après-V1 non engagé : signaling maison, relais NAT, FFA à 12) et `docs/plans/196-telemetrie-cloudflare-workers.md` (Lot A, 9 étapes). **Télémétrie sur trois événements** : `session` (fréquentation, funnel d'écran et usage de l'interface — crédits, Showdown import/export, équipes, contrôles, menu de combat, reprise — compteurs accumulés en mémoire, envoyés en deltas par `sendBeacon` à `visibilitychange → hidden`, jamais un envoi par clic), `battle_started` (composition d'équipe par mode, modèle *usage stats* à la Showdown/Smogon — la composition voyage au démarrage, pas à la fin, pour ne pas perdre les abandons des statistiques), `battle_ended` (issue, durée, tours, K.O. par cause, attaques réellement lancées). Décisions **#871-878** : pas de nom de domaine (l'API reste sur `*.workers.dev`), Worker dans `packages/telemetry-worker/` (workspace existant, pas de script `build`), Lot C réduit à l'écran de victoire (Speed controls et Tutoriel interactif partent en Phase 9 — Polish, sans lien avec le réseau ni la télémétrie), interrogation de la base en chat via `wrangler d1 execute --remote` + script `pnpm stats` (noms FR officiels obligatoires à l'affichage), plafond réel corrigé à **~25 000 parties/jour** (pas ~50 000, une écriture indexée compte deux lignes), limitation de débit par le binding `ratelimit` (le WAF gratuit exige un domaine, indisponible sur `*.workers.dev`). **Complété en fin de journée — parité Goatcounter exigée (#879)** : visiteurs uniques par `HMAC(secret ⊕ date du jour, IP + agent)` calculé dans le Worker, **l'IP jamais écrite** et aucun suivi inter-jours possible (uniques par jour, comme Goatcounter) ; navigateur/système en catégories, **pays par `request.cf.country`** (ni base GeoIP ni IP à lire), langue par `Accept-Language`, écran en paliers ; référents captés sur GitHub Pages mais **pas sur itch.io**, où l'iframe masque le vrai référent (cross-origin) — le dashboard itch reste la source pour cette plateforme. Contrepartie : on quitte « article 82 hors périmètre » pour la **mesure d'audience exemptée de consentement** (#878) — et **décision humaine : aucun livrable de conformité en V1**, ni bandeau, ni mention, ni interrupteur d'opposition, comme avec Goatcounter et pour la même raison d'échelle. Cinq des six conditions de l'exemption sont tenues par la structure du schéma sans rien faire ; à revisiter si l'audience devient significative. **Constat technique consigné, pas une décision** : `analytics.ts` déclare 8 événements dont 3 jamais émis (`game-loaded`, `battle-start`, `battle-end`) depuis le refactor `e0c1a221` du 2026-06-15 — aucun combat n'est mesuré depuis deux mois et demi, traité par l'étape 3 du plan 196. Seule décision encore ouverte : un `battle_id` éphémère par partie (Reco : oui, non tranchée). **Prochaine action concrète, qui bloque tout le reste : créer le compte Cloudflare** (étape 0 du plan 196, action humaine non déléguable). Aucun commit à ce stade — les deux plans sont en rédaction.

> MAJ : 2026-08-29 — **Trois correctifs de dette technique du backlog, commit `e9f23d1`.** `__ptE2e__` ne survit plus à la destruction de sa scène — `installE2eSceneHook` renvoie un `uninstall`, appelé dans le `dispose()` de `combat-scene.ts`, désinstallation gardée par identité (item ouvert depuis le plan 194, 2026-08-28). Étiquettes de carte et libellé de format d'équipe ne sont plus codés en dur en français : `tags` passe de `string[]` à `{ fr, en }[]` dans `maps-registry.ts` (neuf cartes, treize étiquettes), et une nouvelle clé i18n `teamSelect.format.option` recalculée à chaque rendu remplace le gabarit `` `${…}J × ${…}` `` de `FormatPicker.ts` (dette laissée ouverte à dessein à la sortie de v2026.8.2). **Trouvaille annexe de `test-writer`, au-delà du correctif** : plusieurs specs e2e (`normal-game`, `combat-menu`, `battle-resume`, `placement-menu`, `platform-chrome`, `responsive-chrome`) passaient parce que `waitReady()` était un no-op — le hook périmé de l'aperçu de carte le satisfaisait instantanément, absorbé par le timeout de l'`expect` suivant. Elles gatent désormais réellement sur le vrai boot du combat. Décision #861. Les 3 items quittent `docs/backlog.md` → `docs/backlog-archive.md`.
>
> MAJ : 2026-08-28 — **Plan 194 — Séquence d'intro : bande-annonce et captures reproductibles, livré et VALIDÉ À LA MAIN (volet menus le 2026-08-27, volet combat le 2026-08-28).** `pnpm capture:intro` joue la séquence complète à la manette, en anglais (menu → constructeur d'équipe → carte → équipes → un tour de combat 6v6 déjà engagé) et sort une vidéo 1920×1080 + 52 captures PNG + un manifeste `beats.json` ; `pnpm capture:trailer` monte la bande-annonce habillée (titre animé « comme le logo Nintendo », cartons de texte, écran de plateformes en trois colonnes, transitions `pixelize`, ~1 min 40, **sans son** — décision #860) ; `pnpm capture:release` sort le GIF du combat entier (accéléré ×2 pour tenir sous les 3 Mo d'itch.io) + 3 captures de publication ; `pnpm capture:video`/`pnpm capture:gifs` restent des outils de vérification, jamais publiés tels quels. **Deux verrous de reproductibilité, vérifiés et non supposés** : `?seed=` reste strictement verrouillé sur `DEV`/`VITE_E2E` (0 occurrence de `forcedBattleSeed`/`VITE_E2E`/`get("seed")` sur le bundle publié, décision #856), et le placement automatique — qui tirait sur `Math.random()`, une seconde source d'entropie indépendante du seed de combat — est désormais seedé lui aussi (`placement-flow.ts`/`combat-screen.ts`) : une seule source d'entropie par partie, placement compris (décision #857, reproductibilité vérifiée empiriquement sur deux runs consécutifs identiques). La séquence se joue **entièrement à la manette**, jamais au clic (le premier `.click()` efface le liseré de focus à l'image, décision #858) ; l'ordre du montage (`TIMELINE`) est indépendant de la chronologie du tournage, par nom de beat (décision #859). **Deux correctifs de jeu au passage** : le sélecteur de format perdait le focus manette à chaque changement de format (testid manquant, `FormatPicker.ts` — résolu, archivé) ; nouveau `pnpm typecheck:scripts` ajouté au gate (`scripts/tsconfig.json`) — les scripts de montage n'étaient couverts par aucune cible de typecheck. Nouveaux testids de contrat de test : `format-segment`, `team-row`, `pokemon-edit-preset`, `battle-over`. **Ce que ça débloque** : les 5 visuels `docs/images/*-screenshot.png` encore en rendu Phaser (reportés depuis 2026-06-16, captures automatiques Babylon déjà rejetées une fois par l'humain) ont enfin leur matière — refonte non encore faite, notée en `docs/next.md` § À faire maintenant. **Nouveau bug noté au backlog** : les étiquettes de carte (`maps-registry.ts`, champ `tags`) sont codées en français en dur, visibles en anglais dans une interface anglaise. Doc complète : `docs/capture-sequence.md`, skill `/capture-intro`. Décisions #856–#860. Détail complet : `docs/plans/194-sequence-intro-captures.md`.
>
> MAJ : 2026-08-27 — **Plan 190 — Trois reliquats soldés avant release : migration i18n complète du journal de combat, points 4K de l'interface de combat, purge de l'échelle morte du Team Builder.** Gate local **full** vert (build, lint, typecheck, 3870 unit, 388 intégration, **516 e2e** — 2ᵉ passage, le 1er est tombé sur un correctif d'accent qui touchait deux sélecteurs e2e, voir plus bas). **Migration i18n du journal** (dette identifiée au plan 178 le 2026-08-03, consignée dans `docs/next.md`) : `packages/ui-dom/src/BattleLogFormatter.ts` portait **157** ternaires `lang === "fr"` (essentiellement des gabarits de phrase inline) **plus 10 tables** de libellés, soit **234 chaînes** dans un fichier de 1617 lignes — un périmètre bien plus gros que les « six familles » annoncées par la note d'origine, mesuré et non estimé. Migration **scriptée** (extracteur maison — l'API compilateur JS de TypeScript n'existe plus en TS 7, portage Go — plutôt que 157 sites retapés à la main), invariant directeur « sortie octet pour octet identique en FR et EN » vérifié par les ~419 tests e2e existants. Livré : **0** occurrence de `=== "fr"` dans le formateur, **+234 clés `battleLog.*`** ajoutées aux deux locales (699 → 933), `translate` désormais injecté dans `BattleLogContext` (`language` y reste, pour les recherches de noms de **données** via `getTypeName` — même partage de rôles que `I18nContext`), `translateIn(language, key, params)` extrait de `t()`. Test **dédoublé par responsabilité**, pas déplacé : clé + paramètres côté `ui-dom`, phrases FR/EN rendues par les vraies locales côté `app` — aucune couverture perdue (les 519 lignes d'assertions sur les phrases existent toujours). Ce que ça débloque : une 3ᵉ langue devient un fichier de locale de plus, plutôt que 157 sites à retoucher. **4K de l'interface de combat** : les 2 points laissés en attente de décision humaine par le plan 179 (pastille d'instruction, dialog de victoire — texte scalé sans sa garniture) sont réglés par une famille de tokens locale `--bc-pad-{xs,sm,md,lg}`/`--bc-radius-{sm,md}` sur `:where(.bc-root, .bc-left-col)`, avec passe complète de `battle-chrome.css` (plus aucun `--spacing-*`/`--radius-*` fixe dedans) et de `move-tooltip.css` (même défaut). **Team Builder** : décision humaine de **ne pas** ressusciter l'échelle `--tb-px` (code mort depuis la suppression de `team-edit-harness.ts`, 2026-07-20 — jamais montée sous `#game-stage`) mais de la **purger** — `team-builder-overlay.css` passe de 216 à 78 lignes, plus 7 indirections `var(--tb-*, Npx)` sans déclarant retirées de `stat-bar.css`/`set-op.css`/`edit-panels.css`. Le retour « l'app est trop petite en 4K » reste non traité pour cet écran, faute de chantier de rescale dédié. **Deux dettes consignées au backlog plutôt que traitées ici** (décision humaine) : `components/button.css` (`.tb-btn`) et `turn-timeline.css` gardent des valeurs fixes sous la police mise à l'échelle ; **le match nul de combat n'a aucun chemin d'exécution** (`checkVictory` appelé à chaque K.O. individuel, `BattleEngine.ts:3816` — le second K.O. d'une même résolution arrive toujours trop tard, donc `battle.draw` et ses clés associées sont du code mort de fait, question ouverte non tranchée : grouper les K.O. ou retirer les clés) ; et **`t()` retombe d'abord sur l'anglais, pas sur la clé brute** — un balayage « aucune clé brute » ne détecte donc pas une clé absente d'une seule locale sur les **familles composées** (12, pas les 6 qu'annonçait la note d'origine), comblé pour le journal par `packages/app/src/i18n/battle-log-keys.test.ts` (+72 cas). **Contrecoup d'une correction d'accents faite au passage** : deux libellés non accentués (`"action.move"`/`"action.undoMove"`) sont des sélecteurs e2e — les corriger a fait tomber ~30 tests au premier passage du gate, répercuté sur 25 occurrences dans 15 fichiers e2e + 12 dans `docs/test-plan.md`. Décisions #849–#855. Détail complet : `docs/plans/190-i18n-journal-de-combat.md`. **19 commits non publiés depuis `v2026.8.1` (2026-08-19), plus celui-ci — aucune release depuis.**

> MAJ : 2026-08-26 — **Plan 189 — Panoramique clavier, menu de combat au placement, découvrabilité du défilement, livré et VALIDÉ À LA MAIN scénario par scénario (six défauts trouvés et corrigés pendant la recette) — solde les trois derniers trous de `docs/next.md` § Reporté avant release.** Trois volets réunis parce qu'ils partagent leurs fichiers (`bindings-store.ts`, `control-legend.ts`, `combat-screen.ts`). **Volet A — panoramique caméra au clavier** : nouveau `packages/app/src/input/keyboard-hold-source.ts`, premier modèle d'entrée **continu** côté clavier (`keydown` ajoute au jeu de touches tenues, une boucle `requestAnimationFrame` réémet l'action, `keyup` retire, `blur`/`visibilitychange` purgent — sinon `Alt+Tab` laisse une touche « collée » qui fait dériver la caméra au retour). Le panoramique redevient **remappable** (défauts `Numpad8/2/4/6`), gagne un jeu de secours **fixe** `Maj`+flèches pour les claviers sans pavé numérique (non remappable, non capturable, jamais prioritaire sur un binding du joueur), et revient dans l'écran de contrôles ; en échange `Numpad1/2/3` sont libérés des crans de zoom (qui gardent `Digit1/2/3`). À la manette, reste le stick droit — annoncé, jamais assignable (`GAMEPAD_STICK_ACTIONS`). Révise les décisions **#807 et #811** (`docs/decisions.md` § Révisé au plan 189) : leur prémisse (« le panoramique n'existe qu'en continu, la couche d'entrée est en `keydown` ») est tombée. **Volet B — menu de combat pendant le placement** : une seconde instance de `createCombatMenu` vit pendant la phase de placement (Reprendre / Paramètres / Recommencer / Quitter), détruite quand `runBattle` prend la main — jamais deux vivantes à la fois. Pas d'« Abandonner » : il purgerait une sauvegarde qui n'existe pas encore à ce stade. « Quitter » demande confirmation (placements perdus). `Échap` ouvre le menu seulement quand il n'a rien à annuler — `undoLastPlacement` reste prioritaire, même règle qu'en combat (décision #820). **Volet C — un glyphe de touche sous chaque contrôle** : règle générale actée, pas trois cas particuliers — Journal et `☰` Menu affichent leur touche sous le bouton, en permanence. Nouveau bloc de glyphes de défilement pour la timeline CT, sous la légende de contrôles caméra, affiché **en permanence** (elle déborde toujours, 4K comprise) et lu aux extrémités de la liste ; dans le journal, les glyphes de défilement n'apparaissent qu'**au débordement** (`scrollHeight > clientHeight`, il est vide en début de combat). `R3` + direction annonce aussi le geste manette de ce nouveau bloc, en réutilisant le mécanisme du plan 186 (décision #817). **Capuchons de touche larges enfin supportés** : `--cl-cap-span` (1 ou 2 tuiles) élargit la **fenêtre du masque**, pas seulement la largeur du capuchon — lève la limite « 1 tuile » de la décision **#791** (révisée, même fichier) sans la contredire : un cap large ne rentrait toujours pas dans un masque d'une tuile, ce n'est plus le masque qui est limité à une tuile. Relevé complet des tuiles consigné le 2026-08-26 dans `docs/references/kenney-input-prompts-tileset.md` (`ESC`, `PAGE↑`/`PAGE↓`, `SHIFT`, Select/Start cerclés, `R3`, croix directionnelle). **Point ouvert à la clôture, tranché et corrigé le 2026-08-27** : les lignes de légende caméra vivant désormais dans la colonne latérale de l'ordre de jeu, elles suivaient la timeline quand sa case active se vide en prévisualisation de coût CT — ce que la décision **#798** interdit. Arbitrage humain : **stabiliser la colonne**. `.tt-active` réserve la hauteur d'une vignette active même vide (`min-block-size`, format téléphone compris, plus les deux bordures du portrait en `content-box`). Légende, liste et capuchons de défilement immobiles au pixel près — **et la boussole aussi** : `chrome-insets` écarte toute mesure de largeur nulle, or un slot vide reste large de 0, ce qui distingue ce cas du `min-inline-size` que le plan 185 avait dû rejeter. Test e2e §4.18 repassé au vert. Note de conception hors périmètre (non résolue) : « Placement auto » coché par défaut à la sélection d'équipe rend la phase de placement invisible au premier lancement, donc le nouveau menu du Volet B avec elle — à rediscuter quand la question du premier contact reviendra. Décisions #843–#848 + révisions de #807/#811/#791.
> MAJ : 2026-08-26 — **Plan 188 — Refonte de l'écran de sélection d'équipe + passe manette (Team Builder & modales), livré et VALIDÉ À LA MAIN, 5 scénarios sur 5 (clavier et manette Switch Pro filaire) — dernier chantier issu de la validation du Lot 2 du plan-cadre 173.** **Volet 1 (sélection d'équipe)** : le `<select>` replié du format devient une rangée de segments toujours visible (`1v1 2v2 3v3 4v4`), libellés `2J × 6` (« Joueurs × Pokemon ») au lieu de `2v6` qui se lisait « deux contre six » ; Humain/IA devient un segment à deux états visibles en permanence ; **la notion de « joueur actif » disparaît** — chaque cellule ouvre une modale de choix d'équipe, le focus avance ensuite au camp suivant non pourvu ; portraits d'équipe sur les cartes, deux colonnes au-delà de 6 camps, « Remplir IA » supprimé (devenu sans objet, chaque camp IA reçoit déjà une équipe aléatoire à l'assignation). **Volet 2 (Team Builder à la manette, jamais éprouvé)** : c'était une impasse, pas une gêne — chips de filtre et lignes de résultat des 3 sélecteurs (Pokemon/capacité/objet) étaient des `<div>` invisibles au focus, aucune sortie de modale au pad (pas d'`Échap`), aucun arbitrage du contrôle focalisé côté manette. Tout converti en `<button>`, `B` referme la modale, curseurs de PS réglables au pad, Nature devenue une **liste maison** (`NaturePickerModal`, partagée avec le sandbox), champs texte sautés au pad (`data-nav-skip="gamepad"` — pas de saisie continue à écrire). **Deux bugs de fond trouvés en cours de test humain, hors cadrage initial** : (a) le focus était détruit à chaque re-rendu **partout** dans le Team Builder → helper partagé `renderPreservingFocus` (`packages/app/src/ui/dom/preserve-focus.ts`), branché sur 5 vues ; (b) une exception d'un consommateur (`applyToControl` appelant `stepUp` détaché de son receveur, `Illegal invocation`) **tuait la boucle du poller manette jusqu'au rechargement** — corrigé à la source et par un filet `try/catch` dans `gamepad-source.ts` (décision #842). **Infra e2e** : nouveau `scripts/with-cpu-cap.sh` — l'e2e tourne désormais sous plafond noyau (4 cœurs/16, 8 Go, priorité basse) et 3 workers par défaut, `PT_FULL_SPEED=1` pour débrider (règle dure ajoutée `.claude/rules/e2e.md`) — motif : l'humain travaille/joue sur la machine pendant les runs. Nouveau e2e `dom/gamepad-pickers.spec.ts` (manette synthétique partagée `e2e/pages/gamepad.ts`), cahier §6.4/§6.13. Gate CI full vert (499 e2e, ~28 min). **Points restés ouverts** (`docs/next.md` § Reporté) : revalidation sur **pad réel** des correctifs de la revue de code (rejoués seulement à la manette synthétique via e2e) ; le filet `try/catch` du poller manette n'est couvert par aucun test direct (seul le bug précis qui l'a motivé l'est). Décisions #830–#842.
> MAJ : 2026-08-25 — **Plan 187 — Menu de combat, livré et VALIDÉ À LA MAIN scénario par scénario (flux d'attaque non régressé, modale, niveaux Paramètres → Contrôles, manette, les trois sorties, bouton tactile) — dernier chantier issu de la validation du Lot 2 du plan-cadre 173.** Nouvelle surcouche d'interface, explicitement **pas une pause** (décision #819) : le combat continue de tourner derrière (l'IA joue, les animations se déroulent, le futur chronomètre multijoueur tournera) — en ligne une pause est impossible, autant n'avoir qu'un seul comportement, validé dès le solo, plutôt qu'un mécanisme de suspension à retirer en Phase 7. Ouverte par `Start` à la manette (index laissé libre exprès par le plan 186), aucun défaut clavier (`Échap` fait déjà le travail, voir plus bas) ; entrées **Reprendre / Paramètres / Recommencer / Abandonner / Quitter**. Nouveau `packages/app/src/ui/dom/combat-menu.ts` : un `<dialog>` à **quatre niveaux** (menu → Paramètres → Contrôles → confirmation) qui **empile sa propre registration** sur la pile de l'`InputSystem` du plan 184 (décision #821) — le menu devient l'unique consommateur pendant qu'il est ouvert, sans toucher une ligne des consommateurs du combat (curseur, caméra, zoom se taisent, et une visée/un choix d'orientation en cours sont retrouvés intacts à la fermeture) — et neutralise le `cancel` natif du `<dialog>` (`preventDefault()`, décision #822) pour éviter le double traitement d'`Échap` (fermeture native + action logique qui rouvrirait). **`BattleOrchestrator.onEscape()` passe de `void` à `boolean`** (décision #820) — « ai-je annulé quelque chose ? » — et les deux `cancel` de `combat-screen.ts` propagent ce booléen au lieu de renvoyer `true` en dur : `Échap` ouvre désormais le menu quand il n'y avait rien à annuler (menu d'actions racine, plateau au repos), plutôt qu'une liste de phases écrite en dur côté app. Changement de comportement assumé et le point le plus à risque de régression du plan (`Échap` est la sortie de tout le flux d'attaque) — couvert par des tests d'orchestrateur phase par phase avant branchement de l'ouverture, et validé à la main : viser, choisir une orientation, `Échap` remonte cran par cran sans jamais ouvrir le menu. **Deux sorties distinctes, pas une** (décision #823, révisée pendant le test humain) : `Abandonner` détruit la partie (purge de la sauvegarde de reprise du plan 181), derrière une confirmation ; `Quitter` rend la main au menu principal en **gardant** la partie reprenable, **sans** confirmation — une confirmation sur une action réversible use le réflexe jusqu'à ce qu'on valide sans lire, y compris devant l'abandon. `Quitter` n'apparaît que là où une sauvegarde existe (`onQuitKeepingSave` optionnel, fourni seulement par le vrai combat) — jamais dans le studio sandbox. Corrige un trou signalé par la revue design et confirmé au premier passage du test humain : jusqu'ici fermer l'onglet préservait la partie alors que le menu du jeu ne le proposait nulle part. **Extraction des panneaux Réglages/Contrôles** dans `packages/app/src/ui/dom/panels/` (`createSettingsPanel`/`createControlsPanel`, contrat `Panel = { element, dispose(), cancelCapture?() }`, décision #824) — `ScreenManager` fait *dispose puis mount*, donc naviguer `combat → settings` par l'écran normal aurait tué la partie en cours ; les deux `*-screen.ts` ne gardent que l'enveloppe (titre, retour, `bindScreenInput`), sans qu'un seul `data-testid` ne bouge ni qu'un e2e existant ne soit touché. Le panneau des Contrôles expose `cancelCapture?(): boolean`, appelé en premier par le `cancel` de la modale (une capture de touche en cours doit s'annuler avant de dépiler un niveau). **Icônes** : le burger `☰` passe au menu de combat, le journal prend `▤` (décision #825) — `☰` est le glyphe conventionnel de « menu », le journal reste étiqueté par son titre alors que le nouveau bouton est icône seule. **Créée par `runBattle`** (le point unique par lequel passent les trois chemins de combat : placement, reprise, sandbox), sur les `onExit`/`onReplay` déjà là, et détruite par le `signal` d'abandon comme les autres écouteurs de cette fonction — jamais passée en paramètre. La victoire referme le menu en **décorant** `showVictory` au seul endroit où le chrome est remis à l'orchestrateur, sans que le menu écoute les événements du combat ni que `view-core` apprenne son existence. Trois cas de bord testés : la victoire qui survient menu ouvert (le menu se referme, la victoire prend la main), `open()` refusé si un autre `dialog` est déjà ouvert (dialogue de victoire), une visée en cours retrouvée intacte à la fermeture. Bouton tactile `☰` inséré entre le plein écran et le journal, désactivé visuellement pendant `locked` (un bouton tactile sans retour se fait taper plusieurs fois de suite). **Ce que ce plan ne fait pas** : pas de pause, pas de sauvegardes multiples/créneaux nommés, pas de refonte des Paramètres/Contrôles (extraction sans changement de comportement), pas de menu pendant la phase de placement (trou préexistant, noté en § Reporté), pas de drapeau multijoueur pour `Recommencer` (code mort non testable tant que le multi n'existe pas). Décisions #819–#826.
> MAJ : 2026-08-25 — **Plan 186 — Écran de remapping clavier & manette, livré et VALIDÉ À LA MAIN, MANETTE RÉELLE COMPRISE** (capture de bouton, `Select` pour le journal, `R3 + direction` pour le défilement, saut des colonnes clavier au pad). Les bindings quittent les sources d'entrée pour un **magasin unique** (`packages/app/src/input/bindings-store.ts`) transposé **par action**, dont les tables de recherche sont dérivées et mises en cache — le chemin chaud (chaque frappe, chaque frame de poll) n'itère jamais. La source clavier, la source manette et la légende du plan 185 le relisent : une touche réassignée est donc **dessinée** par la légende sans une ligne de câblage. Nouvel écran `controls`, atteint depuis Réglages : **une seule table à 3 colonnes** — Principal / Secondaire / **Manette** (les onglets par appareil du premier jet ont été abandonnés sur retour humain : l'alignement devient structurel et la question de l'onglet par défaut disparaît), 5 sections (« Curseur & menus », « Prévisualisation AoE », « Caméra », « Barre d'ordre de jeu », « Journal de combat »), **capture de touche** par `InputSystem.beginCapture` (et non un écouteur `keydown` de plus — le plan 184 en avait supprimé cinq), **échange automatique** en cas de conflit avec message nommant l'action délogée, sortie de capture par `Échap`/B **et** par un bouton « Annuler » visible (seule issue d'un joueur tactile). Deux états de case bien distincts : *vide de naissance* (la majorité des actions, neutre) vs *vidé par un échange* (rouge + astérisque) — sans cette distinction l'écran s'ouvrait couvert d'alertes au premier lancement. **Apports de jeu au passage** : `J` ouvre/ferme le **journal de combat** (son repli existait mais n'était atteignable qu'à la souris), `Page ↑/↓` passe à la **barre d'ordre de jeu** et le journal sous `Maj+Page ↑/↓` (l'ordre de jeu se lit à chaque tour, le journal se consulte après coup), bascule **« Inverser le stick droit »** (préférence dans `pt-settings`, pas un binding). Côté manette, le journal devient **entièrement pilotable** (`Select` l'ouvre, `R3 + ↑/↓` le fait défiler) et le geste de défilement — un maintien + direction qui existait depuis le plan 184 **sans être écrit nulle part** — est désormais annoncé dans la table ; son modificateur passe de `Y` (qui prend « Cible précédente ») à `R3`, pressable du pouce droit pendant que le gauche pousse la croix. `Start` est laissé libre pour le menu de pause à venir. **Trois bugs de manette corrigés par la validation humaine** : (1) manette **muette sous Firefox** — `pollGamepad` refusait tout pad au `mapping` non standard, or Firefox renvoie une chaîne vide pour une Switch Pro pourtant standard (Bugzilla #952773) : on route désormais toujours avec les indices standard, une supposition fausse se corrigeant dans l'écran ; (2) **anneau de focus invisible** à la manette — `:focus-visible` suit la modalité de la dernière interaction et ignore un appui de pad, d'où `[data-input-source="gamepad"] :focus` (⚠️ Chromium masque ce défaut, une assertion sur l'`outline` calculé ne prouve rien) ; (3) **poller qui s'éteignait** à la première frame vide alors que `gamepadconnected` ne repasse jamais — délai de grâce + démarrage de rattrapage. Persistance : `pt-bindings` ne stocke que les **écarts** au défaut, pour qu'un défaut révisé atteigne un joueur qui n'avait rien personnalisé. `MenuNext`/`MenuPrevious` (actions mortes depuis le plan 184) supprimées, `NumpadEnter` perdu (2 slots par action). **Tests** : 93 unit sur `packages/app/src/input/`, 16 e2e — `dom/controls-remapping`, `dom/gamepad-menus` (**manette synthétique** injectée via `navigator.getGamepads`, jouée sur `mapping` standard **et** vide), `combat/controls-remapping` — cahier §6.12. Décisions #803–#816.

> MAJ : 2026-08-24 — **Plan 185 — Légende de contrôles près de la boussole, livrée et VALIDÉE À LA MAIN sur desktop et téléphone réel.** Nouvel élément **DOM** permanent (`packages/ui-dom/src/control-legend.ts` + `styles/control-legend.css`, monté par `battle-chrome.ts`) plutôt qu'un mesh de plus dans la scène Babylon : glyphe « ça se clique » (souris ou main) à droite de la boussole, centré verticalement, puis en dessous une ligne **rotation** et une ligne **zoom**, chaque entrée lisant `[dessin][touche]` (`⟲ A` / `⟳ E`, loupe `+` `R` / loupe `−` `F`) ; **manette** : pas de glyphe de clic, `LB`/`RB` pour la rotation, `RT`/`LT` pour le zoom (RT = zoom avant, conforme à `gamepad-source.ts`) ; **tactile** : ligne rotation masquée (la boussole tourne déjà la vue au tap), zoom = les mêmes loupes accompagnées d'une main écartée/pincée. Permanente, alpha 0,72, `pointer-events: none` — jamais un contrôle ; source d'entrée résolue en **CSS pur** via `data-input-source`. **Positionnement, le vrai morceau du plan** : la légende lit la **même mesure** que le renderer utilise pour épingler la boussole (`chrome-insets.ts`, gagne un `subscribe`). Deux approches ont échoué avant : ancrage DOM dans la case active de la timeline (elle se vide pendant la prévisualisation de coût en CT → boîte 0×0 → légende écrasée sur la boussole), puis réservation de cette boîte en CSS (déplaçait la boussole, qui mesure la même boîte) — **leçon retenue : une mesure a UN propriétaire**, les deux consommateurs la lisent. **Étiquette de touche selon la disposition clavier** (`packages/app/src/input/key-legend.ts`) : `navigator.keyboard.getLayoutMap()` (Chromium seulement, contexte sécurisé requis, peut lever `SecurityError`) avec repli sur la langue du jeu (FR → `A`, EN → `Q` pour `KeyQ` — la seule position qui diverge) ; piège documenté (`docs/references/kenney-input-prompts-tileset.md` § Capuchons de touches) : les capuchons Kenney sont rangés par **position physique QWERTY** mais **dessinent** une légende QWERTY, donc pour un joueur AZERTY le binding `KeyQ` (plan 184) doit être dessiné avec la tuile `A`, pas la tuile `Q`. **Mesh anneau supprimé** de `babylon-compass.ts` : le glyphe de rotation qui vivait à droite de la boussole (billboard Babylon, décisions #770–#775) n'existe plus — c'était le second des deux chemins de rendu de glyphe du jeu — et le proxy de picking redevient un simple **carré** (plancher 44 px) qui ne s'étend plus vers la droite pour l'englober. **Deuxième feuille de tuiles intégrée** : pack Kenney `cursor-pixel-pack` (CC0), commité en **variante masque** (`packages/app/public/assets/ui/cursors/tilemap-1bit.png`, grille 20×11 tuiles de 16 px sans gouttière) — l'original dessine des lignes blanches dans un contour noir opaque, incompatible avec le masque CSS (`mask-image` + `background-color: currentcolor`) déjà utilisé pour la feuille input-prompts ; la variante ne garde que les pixels non-noirs, peints en blanc. Tuiles en service : loupe `+` (7,0), loupe `−` (8,0), main écartée (14,6), main de tap (17,6), main pincée (15,8), paire de rotation (9,10)/(10,10). **Le tap est désormais dessiné par cette feuille partout** : la ligne d'instruction (`.bc-input-glyph`) utilise la même main que la légende, et sait changer de feuille et de grille ensemble. Crédits ajoutés (`credits.cursors`). **Bug non lié corrigé au passage** : Babylon stampe lui-même `tabindex="1"` sur le canvas (constructeur de `Scene`, pas d'`Engine`), en faisant le premier arrêt de tabulation de la page — un `tabindex` positif est interdit par `.claude/rules/html.md` — et dessinant le liseré `:focus-visible` global (posé au plan 184) autour de toute la scène de combat ; corrigé par `canvas.tabIndex = -1` après `new Scene(...)` et `#game-canvas:focus{outline:none}`. **Tests** : `e2e/tests/combat/compass-rotate-hint.spec.ts` renommé `compass-and-legend.spec.ts` (6 tests verts : zone tapable carrée ancrée sur le portrait, clic qui tourne, clic sous la boussole qui ne tourne pas, légende posée à droite et sous la boussole, légende qui suit la source d'entrée, légende immobile quand la timeline perd son entrée active) ; `input-prompt-glyph.spec.ts` +2 tests (feuille input-prompts en pointeur fin, feuille de curseurs au doigt) → 12 verts ; nouveau `packages/app/src/input/key-legend.test.ts` (6 verts unit). **Le chantier « écran de remapping », initialement lié à celui-ci** (décision humaine 2026-08-21 : même question « le joueur sait-il ce qu'il peut faire ? ») **en est désormais détaché** (décision humaine 2026-08-24) — il reste **seul** ouvert en `docs/next.md` § Reporté. Décisions #797–#802.
> MAJ : 2026-08-21 — **Plan 184 — Contrôles clavier & manette (Lot 2 du plan-cadre 173) : LIVRÉ, étapes A→E, gate local vert — la Phase 6.5 « Client jouable » a désormais ses 3 lots clos.** **Couche d'actions logiques** dans `packages/app/src/input/` (7 modules) : un **seul** écouteur clavier pour toute l'app, un routeur qui remet chaque action à **un** consommateur selon le contexte (`menu`/`board`/`screen`/`locked`, dérivé des 9 phases de `InputState` via le nouvel accesseur `inputContext()`, qui laisse `InputState` privé), et un tracker de source *last-input-wins* publié en `data-input-source` sur la racine. Les **5 `window.addEventListener("keydown")` dispersés dans 4 fichiers sont supprimés** — dont celui qui devait appeler `stopImmediatePropagation()` pour qu'un Échap n'annule pas **et** ne défasse pas un placement ; l'arbitrage est désormais explicite et couvert par un **test d'invariant** (« une action n'atteint jamais deux consommateurs »). **Le curseur de plateau était LE trou** : il n'existait que comme conséquence d'une position de pointeur, donc au clavier seul rien ne pouvait désigner une case, et tout l'étage d'information du Lot 3 restait invisible — même cause qu'au doigt avant le plan 183. Résolu par une conversion écran→grille qui **projette les 4 voisins et garde le meilleur match** (helper partagé `bestNeighborForScreenVector`, réutilisé par le pointeur qui vise une orientation) plutôt qu'une table par azimut : ça s'auto-corrige à toute rotation, élévation ou handedness. Vecteurs de référence = les **diagonales** écran — en vue dimétrique les 4 axes de grille se projettent en diagonales, donc un « haut » de (0,−1) tomberait à égalité entre deux axes (convention FFT/FFTA). **Bindings par POSITION de touche** (`KeyboardEvent.code`, jamais `.key`) : `KeyW/A/S/D` = ZQSD en AZERTY et WASD en QWERTY, `KeyQ`/`KeyE` = A/E et Q/E pour la rotation (4 azimuts iso), zoom `Digit1/2/3` en absolu (3 crans seulement) + `KeyR`/`KeyF` en relatif ; `+`/`−` écartés (la position `Minus` porte `)` en AZERTY), **aucun chemin `.key` implémenté**. **Manette** : polling `requestAnimationFrame` démarré au premier `gamepadconnected`, fronts détectés sur des **valeurs primitives** (🔴 Chrome mute les objets `Gamepad`/`GamepadButton` en place à chaque frame : garder une référence compare l'objet avec lui-même et casse toute détection de front **en silence**), deadzone **circulaire**, répétition après délai, `Y` maintenu + direction = défilement journal/timeline. **Focus** : une règle `:focus-visible` **globale** (il n'en existait qu'une dans tout le CSS, sur les lignes d'attaque), le menu de combat qui reprend le focus après son `replaceChildren` **seulement** si la source active est clavier/manette, et `settings-screen` qui mute son libellé au lieu de se reconstruire (dette du Lot 3, décision #752). **Étape E — dette du plan 183 soldée en déplacement réel** : les ~180 lignes de gestes (table de pointeurs, seuils par `pointerType`, pinch, arbitrage tap/glissé, visée en deux temps) vivent dans `packages/app/src/input/pointer-source.ts`, et `render-babylon` ne contient plus **aucun** `addEventListener("pointer*")` — le critère de réussite écrit dans le plan. Prix payé : 9 primitives d'entrée sur le port `CombatScene` (`pickTileAt`, `isCompassHitAt`, `setCursor`, `dispatchTileClick`, les 4 du sélecteur d'orientation, `panCameraByPixels`) — frontière nette, **l'app décide, le renderer mesure**. **Trou trouvé à l'exécution** (décision #783) : `onTileClick` n'a **aucun** cas pour la phase du choix d'orientation, donc au clavier « Attendre » ouvrait une phase sans issue — et le **placement**, qui utilise le même sélecteur, n'aurait laissé placer aucun Pokemon sans souris ; les flèches visent désormais le sélecteur ouvert et Confirm lui est offert avant la validation de case. Tests : **13 e2e clavier** (`combat/keyboard-controls.spec.ts`, nouveau `cursorTile()` dans le hook de scène), **45 unit** sur la couche (routeur, tracker, table de bindings, manette), **5 unit** `inputContext()`, et les **7 e2e tactiles du plan 183 verts après le déplacement**. Gate local : 4187 unit + 388 intégration + 20 scénario + e2e. **VALIDÉ À LA MAIN le 2026-08-21**, scénario par scénario : clavier (AZERTY/Firefox), caméra, menus, choix d'orientation, placement, **manette Switch Pro** filaire, **téléphone réel** (pinch, pan à deux doigts, tap, boussole — la revalidation qu'exigeait l'étape E) et **téléphone + manette**, le cas « first-class » du plan-cadre 173, qui marche effectivement sans code spécifique. ⚠️ **La recette a produit 14 correctifs — plus que l'implémentation initiale.** Sept étaient structurels : le **menu principal ignorait le clavier** (seul écran sans registration, faute de « retour » à brancher — or navigation au focus et retour sont deux besoins distincts) ; **à la manette aucune activation native ne suit un appui**, donc A ne faisait *rien* sur les menus (une manette n'a pas de comportement natif de formulaire, décision #792) ; le **focus était piégé** dans les contrôles de formulaire (règle binaire « un champ = on se tait » → chacun garde désormais l'axe qu'il *utilise*, #785) ; la navigation suivait l'**ordre DOM** dans une mise en page 2D (← → morts, ↓ en diagonale → navigation **spatiale**, #786) ; le bouton **« Terminer » du placement était inatteignable** (contexte `board`, où les flèches ne déplacent aucun focus DOM → l'étape « choix du Pokemon » est devenue un contexte de **menu**, #788) ; le **dialogue de victoire** était classé `locked` donc hors d'atteinte à la manette (#794) ; et le **placement n'avait aucune origine de curseur** (elle vient de `panCameraTo`, appelé par l'orchestrateur… qui n'existe pas encore à ce moment-là). **Le point commun des trois derniers mérite d'être retenu : une phase classée dans le mauvais contexte d'entrée rend une UI entière inatteignable, sans erreur et sans test rouge** — c'est le risque propre à cette architecture, et le prix de son avantage. Sept autres relevaient de ce que seul un humain voit : un liseré de focus sur « Annuler » qui promettait qu'Espace allait annuler (corrigé structurellement — le focus n'est repris que par les deux menus que les flèches naviguent, #790, le garde-fou par contexte ne suffisant pas puisque le chrome est rendu **avant** la bascule de phase) ; **« Annuler le déplacement » en tête de menu**, donc déclenché sans être visé, et un ordre de menu qui changeait d'un tour à l'autre (#796) ; le curseur qui ne repartait pas du Pokemon actif et restait affiché pendant le tour adverse en montrant la fiche du mon précédent (#789) ; **annuler Plénitude ne faisait rien** (motif statique → retour vers une phase qui se re-confirme) et **annuler Destruction** laissait la zone clignoter avec ses dégâts prévus (#795) ; **A/B inversés sur manette Nintendo** — fait matériel, donc détecté par `Gamepad.id` plutôt que réglé (#793) ; **pan au stick droit inversé** (un stick parle le langage du regard, `panCamera` celui du glissé, #794) ; et le calage des glyphes, après deux tentatives rejetées documentées dans le CSS (#791). Décisions #776–#796. **Limites connues** : Firefox renvoie `mapping === ""` pour toute manette absente de sa table interne, même standard (Bugzilla #952773/#1542893/#1922925) — une manette réelle peut y être silencieusement invisible ; et aucun glyphe de prompt **clavier** n'est affichable, la feuille Kenney n'ayant Entrée et Espace qu'en 2 tuiles de large (reporté à l'écran de remapping). Décisions #776–#784.
> MAJ : 2026-08-20 — **Plan 184 — Contrôles clavier & manette (Lot 2 du plan-cadre 173) : plan rédigé, revu (`plan-reviewer` + `game-designer` + `best-practices`) et corrigé, statut `draft` prêt à exécuter — aucun code écrit.** Le trou central n'est pas la manette mais le **curseur** : il n'existe que comme conséquence d'une position de pointeur (`onTileHover`) ou d'un tap, donc sans pointeur aucune façon de désigner une case — tout le Lot 3 est invisible au clavier pour la même raison qu'il l'était au doigt avant le plan 183. Architecture retenue : couche d'actions logiques + tracker de source *last-input-wins* dans `packages/app/src/input/` (pas de nouveau package), accesseur `inputContext()` sur l'orchestrateur (`InputState` reste privé), ajouts au port `CombatScene` (`moveCursor`, `cursorTile`, `rotateCamera`, `zoomCamera`, `setZoomLevel`) — la conversion « haut de l'écran » → direction de grille vit dans la scène, seule à connaître l'azimut. **Bindings par position physique** (`KeyboardEvent.code`, pas `.key`) : `KeyW/A/S/D` donne ZQSD en AZERTY et WASD en QWERTY, `KeyQ`/`KeyE` donne A/E et Q/E — un seul jeu de bindings, zéro détection de disposition. Zoom = `Digit1/2/3` (3 crans de `ZOOM_LEVELS` en absolu) + `KeyR`/`KeyF` en relatif ; `+`/`−` écartés (la position `Minus` porte `)` en AZERTY, et la rangée du haut n'y produit des chiffres qu'avec `Maj` — argument de plus pour `code`). **Correction factuelle attrapée en revue** : la caméra tourne par **quarts de tour, 4 azimuts** (`AZIMUTH_STEP = Math.PI / 2`, décision #476), pas par crans de 45° comme l'écrivait le draft — « haut de l'écran » tombe donc toujours pile sur un axe de grille, aucune diagonale ni gestion de 8 directions (l'erreur aurait fait sur-concevoir la conversion). **2 pièges de stack documentés avant de coder** : Chrome **mute les objets `Gamepad` en place** à chaque frame (garder une référence casse toute détection de front, silencieusement — copier les valeurs primitives), et Chrome émet un `pointermove` de **delta 0** au `pointerdown` (constaté sur Babylon.js : sans filtre explicite sur les coordonnées, un appui rebascule la source active sur `pointer` juste après une entrée clavier/manette). **Limite connue Firefox** : `mapping === ""` pour toute manette absente de sa table interne, même physiquement standard (Bugzilla #952773 / #1542893 / #1922925, DualSense encore ouvert) → une manette réelle peut y être silencieusement invisible, sans écran de remapping pour s'en sortir ; à vérifier sous Chromium avant d'accuser notre code. **Décisions humaines** : bindings **fixes** (l'écran de remapping devient un plan dédié **après** celui-ci), navigation des menus par **focus DOM natif** (ni *roving tabindex* ni `role="menu"` — choix assumé, Tab reste séquentiel et les flèches sont un raccourci en plus), inspection du plateau **perdue** pendant `action_menu`/`attack_submenu` (assumé ; bascule `Tab`/`Y` écartée mais peu coûteuse à rouvrir, le curseur est gelé et non détruit), défilement du journal et de la timeline CT par **bindings dédiés** (`PageUp`/`PageDown`, `Maj+` pour la timeline ; manette `Y` maintenu + croix) plutôt que par `tabindex`. Le **rapatriement des gestes tactiles** de `combat-scene.ts` (dette explicite du plan 183) est l'**étape E, volontairement en dernier et coupable du plan** : validé sur téléphone réel la veille, c'est le comportement le plus coûteux à revalider. Critère de réussite écrit : plus aucun `addEventListener("pointer*")` dans `render-babylon` — un adaptateur qui subsiste = étape ratée, pas terminée.
> MAJ : 2026-08-20 — **Plan 183 — Contrôles tactiles (Lot 1 du plan-cadre `docs/plans/173-phase-client-jouable-ui-controles.md`), livré et validé sur téléphone réel — Lot 1 clos.** Nouvelle couche multi-pointeurs dans `combat-scene.ts` (`Map<pointerId>` + `pointercancel` + `setPointerCapture`, remplace l'état scalaire pensé souris) ; seuil tap/glissé dérivé de `pointerType` (5px souris/stylet, 10px doigt) ; pinch à 2 doigts → crans de zoom existants (pas de zoom continu) + pan par centroïde. **Un tap agit du premier coup** : le tap en deux temps généralisé a été écrit puis abandonné après mesure sur le vrai flux — redondant avec l'étape de confirmation déjà présente dans le jeu (4 taps contre 2 clics à la souris). **Seule exception** : viser un pattern directionnel (cône/ligne/fauche/charge s'ouvre avec sa direction déjà affichée, retaper la **même direction** lance — comparaison directionnelle et non par case, arbitrée dans l'orchestrateur qui seul la connaît), même règle pour l'orientation de fin de tour (bug corrigé au passage : un booléen validait la nouvelle direction au lieu de l'afficher). **Boussole devenue tapable** (rotation à sens unique), taille et ancrage **repris de la première case de la timeline** (nouveau `packages/ui-dom/src/chrome-insets.ts`, mesure DOM injectée dans `render-babylon` via `CombatSceneOptions`, sans que le renderer dépende de `ui-dom`) après 3 dérives constatées sur téléphone. **Annulation atteignable au doigt sur les 6 phases annulables** (5/6 étaient des culs-de-sac sans clavier) : bouton « Annuler » étendu, `BattleInstruction` (`render-ports`) gagne 2 variantes, 2 clés i18n FR/EN ajoutées. Nouveau `tapTile` dans le hook e2e debug, qui traverse la vraie couche d'entrée ; `clickTile`/`hoverTile` inchangés (court-circuit direct), donc les ~419 tests e2e existants restent intacts par construction. **Dette assumée** : le tactile reste codé en direct dans `combat-scene.ts` — le Lot 2 (clavier/manette) devra le rapatrier derrière la couche d'actions logiques device-agnostique, pas l'envelopper. Décisions #763–#767.
> MAJ : 2026-08-20 — **Plan 183 (suite) — 4 ajustements après le premier passage doc-keeper, avant le commit `837f0a9` poussé sur `main` (gate full vert : 3729 unit, 388 intégration, **427 e2e**).** (1) **Renommage** : la ligne d'instruction des 4 motifs directionnels dit désormais « Choisis la direction » (nouvelle variante `aimDirection` de `BattleInstruction`) au lieu de « Sélectionne la cible » — parler de cible induisait en erreur là où on choisit une direction (retour humain). (2) **Les motifs statiques** (soi-même, croix, zone) **sautent désormais la phase de ciblage** et arrivent direct à la confirmation : le core (`resolveTargeting`) lit `caster.position` et ignore la case visée, donc cette phase faisait choisir entre une seule option qui se validait sur n'importe quel clic ; l'empreinte reste affichée et la prévision de dégâts s'ajoute. (3) **2 bugs de confirmation directionnelle attrapés avant publication**, tous deux invisibles à la souris (qui survole toujours avant de cliquer) mais réels : un **Critical** en code-review — `pointerup`/`pointermove` écoutés sur la fenêtre (`pointerdown` sur le canvas seul, nécessaire pour suivre un glissé hors canvas) laissait un relâchement venu d'un bouton du chrome DOM être traité comme un clic plateau et engager une action non demandée, corrigé en n'agissant que sur les pointeurs dont le `pointerdown` a été vu sur le canvas ; et un clic **sans survol préalable** en phase de visée directionnelle aurait tiré dans l'axe du lanceur au lieu de la case cliquée, corrigé en distinguant la direction « affichée par défaut » de la direction « réellement visée » (`previewDirectionIsDefault`). (4) **Tests e2e adaptés** : POM `castMove`/`aimFirstMove` ne dépensent plus un clic à viser quand la phase de ciblage est sautée (2) ; un test de table de chute qui lisait la carte de prévision restée à l'écran au lieu des PV réels corrigé (attend désormais la résolution du tour). Décisions #768–#769.
> MAJ : 2026-08-20 — **Chantier « aide visuelle des gestes attendus » (suite du Lot 1 du plan-cadre 173), commit WIP `6891639` + corrections de revue.** La ligne d'instruction du combat dit désormais *quoi* faire (« Choisis la direction ») mais ne disait pas *comment* : rien n'indiquait qu'il faut retaper une direction pour la valider au doigt. Ajout d'un **glyphe de geste** à côté du texte (nouveau `packages/ui-dom/src/input-prompt-glyph.ts`, pastille restructurée en `.bc-instruction-row` parent, `.bc-instruction` reste texte pur) : masque CSS sur la feuille Kenney **`input-prompts-pixel-1-bit`** (16×16, 1-bit, CC0), souris en pointeur fin / main-curseur en pointeur grossier via `@media (pointer: coarse)`, suffixe « ×2 » tactile uniquement sur les 2 phases directionnelles (`aimDirection`, `selectDirection`). Taille `0.9em` (suit la police, après 2 rejets — 48px trop gros en 4K, 32px trop petit). **Glyphe de rotation à droite de la boussole** (`babylon-compass.ts`) : plan texturé billboard dans la scène (NEAREST, alpha-blend, groupe HUD) ; le proxy de picking de la boussole devient un rectangle aligné écran étendu **vers la droite seulement** — boussole + glyphe forment une seule zone tapable, pas un second contrôle. Assets : `packages/app/public/assets/ui/input-prompts/tilemap-1bit.png` (7,9 Ko) + `LICENSE.txt`, `getInputPromptSheetUrl` injecté par l'hôte (`UiDomConfig`), nouvelle clé `credits.inputPrompts` (FR/EN). **Pack retenu contre la reco du plan-cadre 173** (`input-prompts` 64×64 couleur — jeu pixel-art, chrome déjà monochrome) et **avancé hors du Lot 2**, où le plan 183 en repoussait le choix. **Le pack n'a aucun glyphe de geste tactile** (ni tap, ni double tap, ni pinch, ni swipe) — la note « couvre manette, tactile et souris » de `docs/next.md` était fausse, corrigée ; d'où le tenant-lieu main-curseur + suffixe texte. Tuiles retenues : **111** (clic souris, après rejet de la 478) ; **578** (doigt) ; **colonne 27/ligne 19** pour la rotation, choisie par l'humain **contre** la lecture géométrique du sens (la tuile qui *paraît* horaire se lit à l'envers sur la scène réelle). Exception assumée au facteur entier pour le glyphe boussole : demi-pas de **8px** plutôt que 16px (79px compass 4K, les paliers entiers n'offraient que trop petit/trop gros). **Piège CSS transversal** : `calc(32px / 16px)` (longueur ÷ longueur → nombre, CSS Values 4) est accepté par Chromium et **rejeté par Firefox**, qui jette toute la déclaration — le glyphe disparaissait sans erreur console ; le facteur doit être un nombre nu. Doc de référence complète : `docs/references/kenney-input-prompts-tileset.md`. e2e : `input-prompt-glyph.spec.ts` (10) + `compass-rotate-hint.spec.ts` (4), cahier §4.8/§4.18 (3 cases 👁→🤖). Dette notée (`docs/next.md`) : bug préexistant `normalizeMesh` (bounding info périmée du mesh boussole, sans effet visuel — l'e2e s'appuie sur le proxy de picking) ; canvas 0×0 → `NaN` en amont de `pinToCorner` (récupération automatique) ; « ×2 » pas strictement vrai si la direction par défaut affichée convient déjà ; géométrie de feuille dupliquée CSS/Babylon ; golden `credits-visual-linux.png` régénéré. **`cursor-pixel-pack` non intégré** — reste ouvert, Lot 2 réutilisera la même feuille pour ses glyphes clavier/manette. Décisions #770–#775.
> MAJ : 2026-08-19 — **Décision #752 — le support lecteur d'écran (a11y au sens WCAG) est abandonné, pas reporté.** Motif : le combat est un canvas Babylon, non représentable dans l'arbre d'accessibilité ; le rendre jouable en non-voyant demanderait une représentation textuelle parallèle de tout l'état tactique. Les morceaux utiles sont rapatriés sous leur vraie justification : la **gestion du focus** rejoint le Lot 2 du plan-cadre 173 (prérequis clavier/manette) ; le **HTML sémantique et les noms accessibles** restent, justifiés par le harnais e2e (`getByRole`) et non plus par le lecteur d'écran (`.claude/rules/html.md`) ; la **taille de cible tactile** était déjà livrée au plan 179 ; le **lint a11y Biome** reste non activé (`biome.json` : `a11y.preset: "none"`). Voir `docs/decisions.md` #752.
> MAJ : 2026-08-14 — **Plan 181 — Reprise d'un combat en cours (lot 180-c), livré et validé en human-testing (3 scénarios : reprise après rechargement, effacement à la fin du combat, menu à 6 entrées sur petit écran).** **Approche** : la sauvegarde est **le journal d'actions + le seed + les entrées de setup** (carte, équipes, placements), jamais d'état dérivé (PV, positions, jauges) ; la reprise reconstruit le moteur par `createBattleFromPlacements` puis rejoue les actions. L'infra existait déjà (`BattleReplay`, `BattleEngine.exportReplay`, `runReplay`, golden replay) → **aucun sérialiseur d'état à écrire**, exact par construction (PRNG, jauges de Charge Time, compteurs, révélations d'information du plan 176). Mesuré : 600 actions 6v6 = 45 Ko, rejeu 17 ms. **Nouveaux fichiers** : `packages/app/src/app/battle-persistence.ts` (clé `pt-battle-resume`, port `load`/`save`/`clear`, rejet sur version de schéma ou `buildVersion` différents), `packages/app/src/babylon/battle-resume.ts` (`buildBattle` partagé live/reprise, `resumeBattle`), `packages/app/src/testing/{local-storage-stub,mock-battle-resume}.ts`. **Core** : `runReplay` gagne un observateur d'events optionnel (`ReplayActionObserver`) qui alimente la **reconstruction intégrale du journal de combat** — rien d'autre ne bouge dans le core. `BattleOrchestratorConfig.onActionCommitted` sauvegarde après **chaque action validée** (humain et IA), et une première fois au démarrage du combat. **UI** : entrée « Reprendre le combat — <nom FR de la carte> » en tête du menu principal, visible seulement si une sauvegarde valide existe (décision humaine : pas de reprise silencieuse, pas de modale au boot) ; clé i18n `menu.resumeBattle` FR/EN. Effacement de la sauvegarde à la fin de combat (`BattleEnded`), au retour au menu, et sur « Rejouer ». **Correctif de déterminisme** : le chemin live passait `creationRng = Math.random` par défaut — les genres non fixés par le Team Builder étaient donc tirés hors seed et pouvaient changer à la reprise (Attraction lit le genre) ; désormais `createPrng(seed)`, ce qui supprime aussi le dernier `Math.random` du chemin livré. **2 bugs attrapés en code-review avant publication** : (a) une sauvegarde écrite juste avant que la victoire ne soit vue (l'écriture précède le drain des animations) décrivait un combat **terminé** et se rejouait sans erreur → menu d'action sans action légale, aucune modale de victoire, soft-lock ; `resumeBattle` refuse désormais un journal contenant `BattleEnded`. (b) le spawn des billboards de la reprise déduisait le numéro d'équipe de `playerId === Player1 ? 1 : 2`, alors que `formatKey` vaut `{teamCount}v{maxPokemonPerTeam}` et monte à **12 équipes** → les joueurs 3+ étaient repeints en couleur ennemie ; helper `spawnBillboardsFromState` désormais partagé avec le sandbox, numéro d'équipe lu du préfixe d'id comme le chemin live. **Dette du plan 180 soldée au passage** : les 4 stubs `localStorage` faits main des tests de `packages/app` convergent vers `src/testing/local-storage-stub.ts`. **Tests** : 8 unit persistance, 5 intégration (dont comparaison de **tout** l'état reconstruit vs live, et le refus d'un combat terminé), 1 unit core, 5 e2e (`e2e/tests/combat/battle-resume.spec.ts` + `e2e/tests/dom/battle-resume-menu.spec.ts`), cahier §6.11. Décisions #744–#751.
> MAJ : 2026-08-14 — **Plan 180 — Comportement plateforme mobile, lots 180-a et 180-b livrés et validés sur téléphone réel (commit WIP `118cd55`).** **180-a — manifeste PWA + plein écran + orientation + diagnostic WebGL** : nouveau `packages/app/public/manifest.json` + 3 icônes (`icon-192.png`, `icon-512.png`, `apple-touch-icon.png`) obtenues par **agrandissement nearest-neighbor** du `favicon.png` 28×28 existant (aucun logo/artwork dans le dépôt), padding opaque `#1a1a2e` (iOS ne gère pas les icônes *maskable*) — décision #738. Liens manifeste + `theme-color` dans `index.html`, **sans** `apple-mobile-web-app-capable` (plus recommandé). Nouveaux modules `packages/app/src/platform/{fullscreen,pwa,wake-lock}.ts`. Ligne « Plein écran » (bascule live sur `document.fullscreenElement`, jamais persistée — décision #740) et ligne « Installer l'app » (iPhone non installé seulement) dans l'écran de réglages ; bouton plein écran dans le chrome de combat, rangée partagée `.bl-log-row` (`createBattleLogRow`, `ui-dom`). Ordre d'appel figé par test (`requestFullscreen()` synchrone puis `orientation.lock("landscape")` après résolution — décision #741). Diagnostic `onContextLostObservable`/`onContextRestoredObservable` dans `combat-scene.ts`. **180-b — Wake Lock + persistance d'écran** : ré-acquisition sur `visibilitychange` ; nouveau `packages/app/src/app/screen-persistence.ts` (clé `pt-last-screen`, péremption 1h) — reprise silencieuse du dernier écran de menu, **écrans à paramètre exclus** (`team-select`/`team-edit`/`combat`) via un type dérivé `ParamlessScreenId`, combat non restaurable = 180-c (décision #742). **Bug attrapé en revue avant publication** : les URLs du manifeste étaient absolues, cassant l'installabilité en silence sur GitHub Pages et itch.io (3 bases de service différentes, `vite.config.ts` ne réécrit pas le contenu de `public/`) — corrigé en relatif, décision #739, **le point le plus important de ce lot**. Limites iOS reconfirmées, rien à changer (décision #743). **Dettes notées** (`docs/next.md`) : ~~`aria-pressed` absent des 3 bascules de réglages (transversal, pas que la nouvelle)~~ **écarté (2026-08-19, décision #752)** — ne sert que le lecteur d'écran, non visé ; ce qui reste vrai (perte de focus clavier au `render()`) part au Lot 2 du plan 173 ; 3 stubs `localStorage` de test à converger ; bouton plein écran d'itch.io à retirer seulement après validation embed ; golden visuel `settings-visual-linux.png` à régénérer (e2e `visual` local-only). **180-c (persistance/reprise d'un combat en cours) reste `draft`.** Décisions #738–#743.
> MAJ : 2026-08-06 — **Plan 179 — Responsive + dette mobile, résout l'item Lot 3 « responsive + dette mobile » (implémentation + review + e2e, commit WIP `8d13d58`).** Nouveau **second référentiel de design mobile** : `--ui-scale` se calcule contre **1280×720** au lieu de 1920×1080 quand `height < 500px` **ou** `width < 900px` (la 2ᵉ condition couvre la tablette tenue en portrait) — constantes `MOBILE_DESIGN_REFERENCE_*` (`packages/ui-dom/src/game-stage.ts`) ; zoom mobile ×1,5 arbitré par l'humain sur téléphone réel (essais ×1,5/×1,7/×1,9/×2,25) ; le système container-query (`--ip-px`/`--wh-px`/`--tt-size`) réplique le même seuil (~13 copies, syntaxe de plage partout). **Chrome de combat** raccordé à l'échelle : menu d'actions + indicateur de tour (jusqu'ici sur tokens fixes — seul élément du chrome qui ne profitait jamais de la place sur grand écran) en maquette 28px, plancher de cible tactile **30px** sous `pointer: coarse` (44 puis 36 rejetés, WCAG 2.2 SC 2.5.8 — hit-area seulement, pas le rendu), lignes d'attaques alignées sur le même plancher, journal élargi (400px maquette, corps 26px), vignettes de timeline agrandies mobile-only, safe-area sur les panneaux ancrés aux bords. **Écrans de menu** (le vrai gisement de dette) : choix de carte (grille `minmax(0,1fr)`, `.ms-list` devenu conteneur défilant, voile de chargement de l'aperçu), Team Builder (tokens compacts, boutons d'en-tête sur 1 ligne), les 3 sélecteurs Pokemon/capacité/objet (filtres en défilement horizontal + fondu de bord, grille de résultats qui mesurait **0px de haut** sur téléphone corrigée), sélecteur d'équipe (mêmes tokens compacts), barre de placement (compactée + safe-area, labels sous la barre de gestes corrigés). **Overlay d'orientation** (`OrientationPrompt.ts` + `orientation-prompt.css`, i18n FR/EN) : obstruction visuelle en portrait **téléphone** (`pointer: coarse` **et** largeur < 600px), pas un verrouillage (`screen.orientation.lock()` inatteignable sans plein écran, absent d'iOS Safari) — portrait reste jouable sur tablette. **Clavier virtuel** : `interactive-widget=resizes-content` + `viewport-fit=cover` au meta viewport, modales plafonnées en `dvh`, plus de focus auto du champ de recherche sous pointeur grossier (`packages/app/src/ui/team/picker-focus.ts`). **Unification des types** : le Team Builder migre sur `createTypeChip` (plan 178), désormais **exporté** par `ui-dom` (il ne l'était pas — cause de la non-adoption au plan 178) ; `.tb-type-badge` supprimé ; fix connexe `--type-chip-px` (repli `var(--ui-scale, 1)`, sinon `calc()` invalide hors `#game-stage` et icône à sa taille native). **Divers** : icônes officielles d'objet dans le sélecteur + champ « objet tenu », noms de type **FR** dans les puces de filtre des sélecteurs (affichaient l'id anglais — violation de la règle dure), `allowedHosts` derrière `PT_TUNNEL=1` (`vite.config.ts`) pour un tunnel de dev. **Validation humaine partielle** : combat, Team Builder, sélecteurs et orientation validés sur téléphone réel ; **dialog de victoire et rendu 4K jamais vus**. **3 points ouverts** (paddings non scalés de 3 éléments 4K, code mort `--tb-px` Team Builder, projet e2e `dom` proche de son délai) → `docs/next.md`. Décisions #733–#737.
> MAJ : 2026-08-05 — **Plan 176 — Information ennemie cachée, ex-« fog ennemi » (rétention d'information sur les Pokemon adverses), commit WIP `68075f9`.** Le panneau d'un Pokemon **ennemi** ne montre plus ses PV exacts (`142/180`), son objet tenu ni son talent tant qu'ils ne sont pas révélés : PV en **pourcentage seul** (la barre de vie reste inchangée, seuls les chiffres disparaissent) ; objet tenu et talent affichés `???` + icône générique dessinée en CSS (carré pointillé, aucun asset requis) tant qu'ils sont inconnus — le placeholder objet est posé **même sur un Pokemon sans objet**, « ne tient rien » étant une information comme une autre ; le talent quitte son ancien badge de révélation pour rejoindre son **slot normal** (comme un allié), rempli dès qu'il devient connu. **Révélation à l'usage** : nouveau module core pur `packages/core/src/battle/reveal-tracking.ts` (`applyRevealsFromEvents`) — tout event qui **nomme** un objet ou un talent (activé/consommé/brûlé/dégommé/volé/échangé, talent activé) le révèle **définitivement** (reset au K.O. seulement), branché sur seulement 2 points : `submitAction` devenu un wrapper mince autour d'un `applyAction` privé, et `consumeStartupEvents`. Dégâts affichés en **% des PV max** sous information cachée, à la fois dans la preview de combat (plan 175) et dans l'**overlay flottant au-dessus des sprites**. Ceinture Force et Fermeté ne sont nommées dans le garde-fou « sauf … » de la preview que si elles sont connues du joueur. **Correction de perspective au passage** : nouveau `humanPlayerIds` dans `BattleOrchestratorConfig` + `viewerPlayerId()` — le panneau gauche passait `isAlly: true` en dur, ce qui affichait l'ennemi **en clair** à chaque tour d'IA (bug pré-existant révélé par ce chantier). **Défauts** : masquage **ON en dur** en partie réelle (règle de jeu, pas de réglage joueur) ; sandbox `SandboxConfig.fogOfWar` (défaut **OFF**) + case à cocher au studio — masquage OFF = lecture complète de l'ennemi (assouplit « ennemi minimal » du plan 174). **Limites connues assumées** : les multiplicateurs silencieux (Bandeau, Éviolite, Technicien, Poing de Fer…) ne se révèlent jamais faute d'event ; l'IA n'est pas soumise au masquage (`getGameState` reste un passthrough, corrigé en Phase 7). Human-testing validé. Décisions #728–#732.
> MAJ : 2026-08-03 — **Plan 178 — Tooltip d'attaque enrichi + harmonisation de l'affichage des types.** **Volet 1 (tooltip)** : tags contrecoup (Bélier « 25 % des dégâts infligés », Métalaser « 50 % des PV max »), drain (Méga-Sangsue 50 %, Vampibaiser 75 %), auto-K.O. (`isExplosion`/`selfKo`/`selfKoOnConnect` — Explo-Brume, Souvenir, Vœu Soin, Tout ou Rien), chip de type nommé, coût CT chiffré, probabilité d'effet secondaire. Nouvelle API core `BattleEngine.previewMoveCtCost(moveId, targetIds?)` : sans cible (tooltip du sous-menu) elle rend le coût **de base** seul, cohérent avec les pastilles de tempo qui ignorent déjà Pression ; avec cibles (preview de confirmation, plan 175) elle rend le **total exact**, la surtaxe Pression affichée **séparément** du total (`CT: 720 (+100)`) plutôt que fondue dedans — impossible de la connaître avant qu'une cible ne soit choisie, et elle se cumule par porteur en AoE. `computeCurrentMoveCost` (rétrospectif) délègue désormais à cette API : un seul calcul, deux points d'entrée. **Volet 2 (types)** : trois mécanismes concurrents unifiés en une source unique `getTypeName` (`packages/data/src/i18n/type-names.ts`, aux côtés de `getMoveName`/`getPokemonName`) — suppression de `TYPE_LABEL` (map FR/EN en dur de `view-core`) et des 18 clés i18n `pokemonType.*` de l'app (doublon exact, plan 164), au profit d'un module accessible même du journal de combat (qui n'a pas de contexte i18n). Nouveaux composants partagés `ui-dom` : `createTypeChip` (remplace `.ip-type`, réutilisé InfoPanel + tooltip) et `createStatusChip`, qui exploite enfin les assets `label-<status>.png` — présents depuis le plan 018 mais **branchés nulle part**, tous les consommateurs affichaient le glyphe nu `icon-*`. `buildSecondaryEffectChip` extrait de la preview de combat (privée, plan 175) vers `view-core`, partagé par le tooltip — un seul calcul de la chance d'effet secondaire. Code mort supprimé : tag `mistyExplosionSelfKo` conditionné à un contrecoup `fraction >= 999` qu'aucun move ne porte (Explo-Brume ne disait donc jamais rien de son auto-K.O.). Décisions #724–#727. Commits `2e01f19`, `d26dea6` (fix sélecteur de talent sandbox + dérive de type dans un test).
>
> MAJ : 2026-07-27 — **Plan 175, suite : contexte de dégâts unifié + preview correcte des K.O. en un coup, et Fermeté passe sous information cachée.** **(1) `damage-context.ts`** (nouveau) : `resolveDamageContext` devient la source unique du contexte d'un coup (move après morphs, météo, écrans, champ, multiplicateurs post-roll), partagée par le vrai coup (`handle-damage.ts`, −126 lignes) et la prévision (`BattleEngine.estimateDamage`, −76 lignes). Les deux le recalculaient à la main et **avaient déjà divergé** : l'estimation ignorait le morph météo de Météore, le malus pluie de Lance-Soleil, le volatile Chargeur, Coup d'Main et Garde Amie — donc le chiffre affiché **et** les ~10 heuristiques IA qui le lisent étaient faux dès qu'un de ces cas était en jeu. **(2) `previewMove` corrigée sur 2 angles morts** : les moves **K.O. en un coup** (Abîme, Guillotine, Empal'Korne, Glaciation) contournent la formule de dégâts **et** `checkAccuracy` — le panneau annonçait « survit » sur un move qui one-shot ; nouveaux champs `MovePreview.isOhko` / `ohkoImmunity` (Fermeté = immunité **totale** ici, pas une survie à 1 PV), précision lue via `ohkoAccuracy`. Et les **moves appelés** (Métronome, Blabla Dodo, Photocopie) : la preview décrivait l'appelant (« Métronome, 0 dégât, Normal ») au lieu du move réellement engagé, désormais résolu via `resolveEffectiveMove`. **(3) Premier morceau de masquage appliqué (décision #723)** : le prédicat de visibilité de `combat-preview-view.ts` était un stub `return true` en attendant le plan 176 — il teste maintenant `revealedAbility` (ou cible alliée) et gouverne **les deux** affichages du talent, l'immunité Fermeté au K.O. en un coup et le garde-fou « sauf Fermeté ». Talent non révélé → verdict nu « K.O. », la surprise arrive à l'exécution. **Ténacité** (action vue) et **Ceinture Force** (objet déjà affiché dans l'InfoPanel) restent nommées ; immunités **de type** et **Glace** restent visibles, les types ennemis étant publics. Human-testing validé en 3 scénarios. Décision #723.
> MAJ : 2026-07-26 — **Plan 175 — Preview de combat (prévision de dégâts détaillée), commit `dc43def`.** À la confirmation d'une attaque, le panneau du lanceur s'étire (3ᵉ colonne + pointe de flèche) et porte le bloc d'attaque (nom + type du move, `min–max PV` coloré selon la létalité, précision, critique arrondi à l'entier, puces de modificateurs, effet secondaire) ; la carte curseur devient la **cible focalisée** (barre de vie à 3 zones — plein/dégradé/restant —, PV restants en %, « Sans effet » si immunité, mention « sauf Ceinture Force / Ténacité / Fermeté » si une survie à 1 PV **connue du joueur** rendrait le verdict létal mensonger — jamais pour le Bandeau, survie probabiliste). Panneau gauche = Pokemon actif seul, carte droite = survolé puis cible. Cycle multi-cibles au survol ou `Tab`. Gaté par le réglage *Prévisualisation dégâts*. **2 changements de règle de jeu, pas seulement de l'UI** : **(1)** `estimateDamage` intègre désormais météo, écrans (Protection/Mur Lumière) et Brise Barrière — auparavant figés à `1.0`, divergeant des dégâts réels et faisant sous-estimer ces situations aux ~10 heuristiques IA qui en dépendent ; effet de bord assumé, le scoring IA change (décision #721). **(2)** Le bonus de type du terrain (×1.15) n'est plus refusé au type natif/immunisé au terrain (`getTerrainTypeBonusFactor` ne consultait `isTerrainImmune` que par erreur — un Arcanin sur magma ne pouvait jamais toucher son propre bonus Feu, contrairement à un Ronflex) : seul un attaquant **aéroporté** en est désormais exclu ; l'immunité continue d'annuler dégâts/statut/malus de déplacement. Vaut pour toute la table (Eau, Magma/Lave, Glace/Neige, Sable, Marais). Décision humaine 2026-07-25 (décision #722). Nouveaux exports core purs `computeEffectiveAccuracy` (extrait de `checkAccuracy`), `effectiveCritChance` (extrait de `calculateDamage`), `BattleEngine.previewMove`, `SurvivalGuardKind`, `MovePreview`. **Corrections annexes** : `initSettings()` manquait au boot (un réglage persisté redevenait le défaut au rechargement) ; `[hidden]` était inerte sur `.ip-panel`/`.ti-panel` (une règle auteur `display` battait celle du navigateur). e2e `combat-preview.spec.ts` + `docs/test-plan.md` §4.14. **Reliquat noté** : le flaky partagé (`knock-off`, décision #712) a récidivé 2× le 2026-07-26 (`knock-off` puis `steel-beam`, isolés verts) — voir `docs/next.md`.
> MAJ : 2026-07-25 — **Plan 177 — Panneau d'info de case (terrain & modificateurs), commit WIP `caf5153`.** Nouveau panneau DOM à droite de l'InfoPanel Pokemon : survol d'une case (ou case du Pokemon actif par défaut) → affiche terrain + hauteur + effets — traversal/franchissabilité, malus de déplacement, statut à l'arrêt/au passage (glyphe 👣 marche vs 🛑 stop selon le trigger réel côté core) + DoT, hazards posés (× couches), champ actif, zones globales (badge durée), bonus de type et immunités (sprites `assets/ui/types/*`, `assets/ui/statuses/icon-*`). **Refonte « zéro texte » actée en human-testing** (v1 textuel rejeté) : icônes + chiffres courts, émoji en placeholders (pack cohérent différé, piste game-icons.net). Nouveaux exports core purs `getTerrainBonusType`/`getTerrainImmuneTypes` (`terrain-effects.ts`). Seed test-only `SandboxConfig.debugTiles` (hazards/champ/zones/distortion sur une case) pour démo + e2e. e2e §4.13 + `docs/test-plan.md` à jour (test-writer). **4 chantiers séparés notés hors périmètre** : pack d'icônes (game-icons.net), Évasion Herbe Haute (core, jamais implémentée), hazards interdits sur liquide sauf Piège de Roc (core), rendu in-world des effets sur tuiles (plan à part). Décision #719.
> MAJ : 2026-07-24 — **Plan 174 — InfoPanel de combat enrichi pour les Pokemon alliés (commit `c3418fe`).** L'InfoPanel allié affiche désormais types (chips), stats (base niveau 50 + nature + EV → crans → valeur effective incluant Brûlure/Paralysie), talent, avec labels de stat colorés selon la nature (boost bleu / baisse rouge, nom de nature non affiché en toutes lettres). PV en pourcentage. Largeur réduite 330→300px + ombre de texte (lisibilité toutes couleurs d'équipe). Ennemi reste minimal (types publics seulement — révélation progressive = plan 176 futur). Nouvelle fonction core `effectiveDisplayStat` (`packages/core/src/battle/display-stat.ts`, mirroir damage-calc/initiative) + `getNatureEffect` exporté. Sélecteur de nature ajouté au Sandbox Studio (joueur + dummy, `SandboxMemberConfig.nature`). Responsive/mobile reporté au Lot 1. Décisions #717–#718.
> MAJ : 2026-07-24 — **Plan 172 — IA : positionnement pour le ring-out (Phase 2 du plan 159).** L'IA sait désormais **se déplacer** pour préparer une éjection fatale par recul (Draco-Queue, Coud'Krâne, Draco-Charge) au lieu de ne jouer le ring-out que quand la géométrie fonctionne déjà depuis sa case (Phase 1, plan 159). **A3 offensif** (`evaluateAttacksFromPosition`) : prédit l'issue du recul depuis chaque case candidate (`BattleEngine.predictKnockback(..., attackerPosition?)`, nouveau 4ᵉ paramètre optionnel, rétrocompat totale) ; lethal-only, fondu dans `bestAttackScore`. **A4 défensif** (`evaluateKnockbackVulnerability`, dans `scoreMove`) : pénalise `−killPotential` une case où un ennemi porteur d'un move à recul pourrait éjecter le mon à mort, lethal-only (pas d'évitement mou des bords non létaux). Heuristiques communes à tous les niveaux (pas de branche par difficulté, cohérent plan 159). Tests unitaires A3/A4 verts, typecheck vert. Voir `docs/ai-system.md` § Positionnement ring-out (A3/A4).
> MAJ : 2026-07-24 — **6 OP sets emblématiques Batch B/D Gen 1 ajoutés (203 → 209 OP sets, 100% `full`, `pnpm op-sets:analyze` validé).** Mettent en valeur Faux-Chage, Ruse, Anti-Air, Poursuite, Corps Perdu (Batch B) et Attraction (Batch D).

> MAJ : 2026-07-24 — **Plan 171 — Zone Magique canonique complète (commit WIP `18e6a06`).** Le chokepoint `effectiveHeldItem(state, pokemon, registry)` passe en adoption complète : ~16 lectures directes `itemRegistry.getForPokemon(...)` d'effets d'objet tenu routées + 2 signatures threadées (`tryMentalHerbCure`, `applyTerrainStatus`/`applyTerrainDot`). Zone Magique neutralise désormais canoniquement tout effet d'objet tenu d'un mon dans la zone (Veste de Combat, Ballon, Lunettes Filtre, Talisman Sain, Grosse Racine, Dé Pipé, Roche Royale/Croc Rasoir, Spray Gorge, Herbe Mentale, verrou Choice, Lancer/Dégommage, bloc terrain…). 2 carve-outs canon documentés (non routés, décision humaine) : manipulation physique de baie ennemie (Calcination, Picore/Piqûre) et helpers stat purs Pierrallégée/Poudre Vite (déjà gérés inline). Décision #715.
> MAJ : 2026-07-23 — **Immunité aux moves poudre pour le type Plante (canon Gén. 6+).** Résout le reliquat signalé à la décision #640 (plan 155, Poudre Fureur) : dans le bloc « Move-property immunity » d'`effect-processor.ts`, une cible de type Plante touchée par un move `flags.powder === true` est désormais entièrement immunisée — le move entier est bloqué (statut **et** stat), event `BattleEventType.StatusImmune` (reason `Type`) émis quand le move applique un statut. Couvre **Poudre Dodo** (`sleep-powder`), **Spore** (`spore`), **Para-Spore** (`stun-spore`), **Poudre Toxik** (`poison-powder`) ; **Poudre Fureur** (`rage-powder`) était déjà géré séparément via `isImmuneToPowderMove`. Tests : unit `sleep-powder.test.ts` + e2e `mechanics-powder-immunity.spec.ts`. Décision #710.
> MAJ : 2026-07-23 — **Boul'Armure ×2 sur Roulade/Ball'Glace + Portée dynamique selon hauteur + sandbox maps de jeu.** Trois changements human-testing/e2e/gate verts. **(1) Boul'Armure ×2** : Boul'Armure (`defense-curl`) pose un flag persistant `PokemonInstance.usedDefenseCurl` (reset au KO, nouveau `EffectKind.MarkDefenseCurl` + `handle-mark-defense-curl.ts`, en plus de son +1 Déf existant) — tant qu'il est actif, Roulade et Ball'Glace infligent le double de puissance via `rolloutEffectivePower(pokemon, moveId)` (`packages/core/src/battle/rollout-streak.ts`), cumulatif avec le snowball (30→480 devient 60→960). Tag tooltip `moveTooltip.tag.markDefenseCurl`. Résout le backlog « Bonus Cabriole ×2 » — **correction de nom** : le move FR officiel de `defense-curl` est **Boul'Armure**, pas Cabriole (erreur du backlog corrigée). Décision #709. **(2) Portée dynamique selon hauteur** : les plans 046/047 n'ajustaient que les **dégâts** selon le dénivelé — nouvelle `getHeightRangeBonus(hAttaquant, hCible) = clamp(0, +2, ⌊(hAttaquant − hCible)/2⌋)` (`packages/core/src/grid/targeting.ts`, `HEIGHT_LEVELS_PER_RANGE_BONUS = 2`, `HEIGHT_RANGE_BONUS_CAP = 2`) ajoute un bonus de **portée** downhill-only (jamais de malus) aux résolveurs Single(>1)/Blast/Cône/Ligne (référence bout-de-portée pour les directionnels) — mêlée/Dash/Tranche exclus. `getValidTargetPositions` élargit les candidats en superset (+2), `resolveTargeting` filtre exactement. Décision #708. **(3) Sandbox — maps de jeu au sélecteur** : les 9 maps de jeu (Arène Simple, Forêt Dense, Grotte Exiguë, Volcan Actif, Tourbière, Dunes et Ruines, Archipel des Pontons, Toundra, Le Mur) ajoutées à `SandboxPanel.ts` sous un séparateur « — Maps de jeu — », dérivées de `MAPS_REGISTRY` ; maps dev conservées. Tests : unit (`height-modifier.test.ts`, `rollout-streak.test.ts`, `defense-curl.test.ts`, `rollout.test.ts`), intégration (`targeting.integration.test.ts` § height range bonus, 6 cas), e2e (`mechanics-dynamic-power.spec.ts` §5.23, `height.spec.ts` §5.17). `docs/test-plan.md` déjà à jour.
> MAJ : 2026-07-23 — **Animation procédurale des liquides.** Nouveau `LiquidShimmerPlugin` (`packages/render-babylon/src/shaders/liquid-shimmer-plugin.ts`, `MaterialPluginBase` mirroré du vent des décorations) anime la surface translucide de chaque liquide par-dessus sa texture statique (le fond ne bouge pas) : **lueur/assombrissement par zones** (`blob` — lave = bulles chaudes qui pulsent en place, marais = 3 patterns de zones sombres fixes cross-fadés sans dérive), **vague de scintillement directionnelle** (`sparkle` — eau claire Ouest→Est, eau profonde en diagonale), **ondulation verticale** (`ripple`). Tunable par liquide (`BABYLON_LIQUID_SHIMMER_BY_GROUP`, `babylon-constants.ts`), échantillonnage monde-XZ snappé à la grille de texels (`BABYLON_LIQUID_SURFACE_PIXELS_PER_UNIT = 24`) pour rester pixel-perfect. Confirmé : ni palette-cycling ni frames — la source PMD n'en fournit pas (voir `docs/tileset-mapping.md` § Provenance) ; le palette-cycling reste non retenu. **Changement connexe** : la lave passe en colonne pleine 0→5/6 (branche `deep_water`), supprimant le seam demi-bloc entre l'ancien fond+nappe. Décision #707.
> MAJ : 2026-07-22 — **Chantier e2e — rattrapage de couverture complet (~62 tests Playwright).** Toutes les familles de mécaniques sans e2e reçoivent une couverture : Stat/state manip (146), Lock-in (149), Manip talent Batch C (153), Buff/statut Batch D (154), Phazing, Sacrifice/Self-KO (147), Batch E grille (155, via harness N-vs-N plan 167), physique terrain §5.17-5.20. Nouveaux specs `mechanics-{stat-state-manip,lock-in,ability-manip,buff-status,phazing,self-ko,grid-tactics,content-fill-unlocked,terrain-physics}.spec.ts` + extensions `{height,mechanics-terrain,mechanics-traversal}.spec.ts`. **Extension `SandboxConfig` test-only** (`stockpileCount`/`unburdenActive` sur `SandboxMemberConfig`, view-core) débloquant les cas exacts Relâche/Avale/Stockage (162) + Délestage (163). Lecture des PV en e2e via la barre de vie InfoPanel (`role="progressbar"`/`aria-valuenow`) — débloque table de chute, modificateur dégâts hauteur, DoT terrain. Helper `readHp` centralisé (`e2e/pages/combat-queries.ts`). `docs/test-plan.md` §5.39-5.46 créées + §5.17-5.20 révisées (👁→🤖). **2 points infra signalés** (code-review) : le gate ne typecheck pas `e2e/` (ajouter `tsc -p e2e/tsconfig.json`) ; wrapper `pnpm exec biome` cassé en env dev (binaire natif du store OK). Détails → `docs/next.md`.
> MAJ : 2026-07-22 — **Bump dépendances : TypeScript 6.0.3 → 7.0.2 (compilateur natif Go, typecheck ~3x plus rapide, tsconfig déjà compatible) + Babylon.js 9.12.1 → 9.17.1 (core/loaders/inspector/gui/materials/serializers/addons, zéro régression).** Bump secondaires : vite 8.0.16→8.1.5, vitest+coverage 4.1.9→4.1.10, biome 2.5.0→2.5.5, playwright 1.61.0→1.61.1, tsx 4.22.4→4.23.1, sharp 0.35.1→0.35.3, fast-xml-parser 5.9.1→5.10.1. Décisions #703–#704.
> MAJ : 2026-07-22 — **Plan 167 — Studio sandbox N-vs-N par équipes + harness e2e IA « scoré ».** Le studio sandbox passe d'un 1v1 fixe Joueur/Dummy à des **équipes N-vs-N** (1 à 6 membres chacune). Nouveau schéma `SandboxConfig` v2 : `teams: [TeamConfig, TeamConfig]`, chaque équipe `{ control: "player"|"passive"|"scored", aiProfile?, members: [...] }`, membre `{ pokemon, moves?, hp?, status?, …, defensiveMove? }` — adaptateur `normalizeSandboxConfig` garde 100 % de rétro-compat avec le schéma plat legacy (tous les fixtures e2e existants restent valides). **Contrôle par équipe** : un seul dropdown, 5 niveaux (Joueur / Auto passif / Facile / Moyen / Difficile) — `passive` = `DummyAiController` scripté par membre, `scored` = `AiTeamController` seedé (profils `easy`/`medium`/`hard`) câblant enfin le **vrai scorer IA** en sandbox : **déblocage de l'e2e des heuristiques des plans 159/160/161**, résout l'item next.md « e2e IA bloqué par le harness ». UI : accordéons Équipe 1/Équipe 2, cartes membres repliables, add/trash (cap 6), allié KO au spawn possible (`hp:0`, pour tester Vœu Soin). **2 fixes moteur** découverts en human-testing : (A) un membre KO au spawn n'est plus planifié dans le CT (`rerunBattleStartChecks`) ; (B) une action self-KO (Vœu Soin/Explosion/Souvenir) avance désormais le tour immédiatement (`submitAction`) — corrige l'anomalie signalée au plan 159 (`getLegalActions` offrait encore des actions au « cadavre »). **RNG de création seedée** (`creationRng: createPrng(config.seed)`) — sandbox 100 % déterministe à seed fixe, nature/genre inclus. Fix connexe : `dexNumber` propagé aux entrées Pokemon `custom` (Métamorph reprend sa place #132 dans le picker). Décisions #698–#702. Commit WIP `024c770`.
> MAJ : 2026-07-21 — **Plan 166 — Liquides : transparence + cuvette + immersion des sprites + écume. Chantier « Eau & liquides » clos.** Chaque tuile liquide franchissable (eau/marais/lave) rend un fond opaque 0→3/6 (sable/roche en fusion) + une nappe translucide 3/6→5/6 ; l'**eau profonde** est une colonne translucide 0→5/6 **sans fond**. Surface commune à 5/6 (1/6 d'air = effet cuvette). Opacité par liquide (`BABYLON_LIQUID_ALPHA_BY_GROUP` : eau 0.6, eau profonde 0.5, marais 0.9, lave 0.99). Faces latérales intérieures entre 2 tuiles liquides adjacentes cullées (pas de « murs sous l'eau »). La nappe est dessinée en **rendering group sprite (2)** + `alphaIndex` (diverge du draft groupe 0) — un Pokemon immergé reste lisible à travers l'eau sans déclencher la silhouette X-ray. **Immersion des sprites** : pieds ancrés à 3/6 au sol sur liquide (Volant/Lévitation restent à 5/6). **Anim d'entrée/sortie** : nouveau `MovementVerticalMode` (`flat`/`step`/`jump`, `view-core/movement-animation.ts`) — un demi-bloc (≤0.5, non-rampe) est désormais un **step** (marche) plutôt qu'un `Hop`, le dip liquide force ce mode (plus de glissade diagonale). **Écume de flottaison** (nouveau `shaders/water-foam-material.ts`, ajoutée en human-testing, hors périmètre du draft v1) : `ShaderMaterial` procédural, tri par `gl_FragDepth`, teinte per-liquide, retirée à la sortie du liquide. **Tuiles liquides `full` en `height=1.0`** (`tileset.tsj`) — rendu 6-tranches pleine hauteur ; le demi-bloc ne concerne que le gameplay (enfoncement à 3/6 = `0.5` via submersion, lu comme un `step`). Décision #697. e2e `scene-graph.spec` mis à jour (`liquid_surface_4_2` groupe 2 transparent). 100 % visuel, core inchangé. Décisions #691–#697. Plan `docs/plans/166-liquides-transparence-cuvette.md` clos `DONE`.
> MAJ : 2026-07-21 — **Décorations voxel + vent (annule décision #475 pour les décorations).** Arbre, herbe haute et rochers 1×1/2×2 sont repassés de billboards 2D face caméra à des **meshes voxel `.glb`**, rendus par `packages/render-babylon/src/babylon-decorations.ts` sur le pipeline mirroré des entry hazards (`babylon-entry-hazards.ts`, plan 131) — l'humain a re-modélisé les décorations en voxel dans Goxel, cohérence avec les props voxel déjà en jeu. Lift **auto-mesuré** par bounding box (plus de constante à re-mesurer par asset). Nouveaux fichiers `rock-1x1x1.glb`/`rock-2x2x2.glb` (+ sources `.gox` dans `assets-src/voxel/goxel/`). **Vent procédural** : nouveau `packages/render-babylon/src/shaders/decoration-wind-plugin.ts` (`MaterialPluginBase`, déplacement de sommets pondéré par la hauteur — base figée, seul le haut ondule) ; herbe en rendering group sprite (pas de silhouette X-ray), rochers/arbre en groupe 0. Constantes billboard 2D supprimées (`BABYLON_DECORATION_FOOT_DROP`, `BABYLON_DECORATION_DEPTH_BIAS`, `BABYLON_GRASS_ALPHA_INDEX`). Décision #690. Détails → `docs/references/voxel-tile-placement.md` + `docs/design-system.md`.
> MAJ : 2026-07-21 — **Plan 165 — IA CT-aware, heuristique KO-protégé.** Le CT (Charge Time) était totalement ignoré du scoring IA. Nouvelle const `CT_REFERENCE_COST = 500` + fonction `applyCtWeight(score, securesKo, move)` dans `packages/core/src/ai/action-scorer.ts` : le score d'un move offensif du chemin générique de `scoreUseMove` est pondéré par `min(1, 500/coûtCT)`, **sauf** s'il sécurise un KO (dégât direct ou ring-out létal) — jamais divisé, un KO retirant une menace définitivement. Approche retenue à la place du lookahead multi-tour initialement supposé nécessaire (backlog 2026-04-25) : un essai naïf plan 068 (division de tout le score) avait produit des combats >5000 tours. Tests : 3 unitaires + 1 scénario de régression charge 6v6 CT (8 seeds, 83-303 actions, plafond garde-fou 1000). Résout l'item backlog « IA — CT-aware scoring » (archivé). Décisions à noter — voir `docs/plans/165-ai-ct-aware-scoring.md`.
> MAJ : 2026-07-20 — **Sandbox Studio : mode RNG explicite + sélecteur talent Dummy + 2 fixes.** Résout **les 2 items backlog « Sandbox — pas de sélecteur talent Dummy »** et **« seed absent = déterministe (surprenant) »**. (1) Nouveau contrôle **Aléatoire** (défaut, seed frais généré à chaque mount/replay via `resolveSandboxSeed` — respecte la vraie RNG Pokemon) vs **Déterministe** (seed éditable + bouton 🎲) — `SandboxConfig.rngMode?`, inféré rétro-compat depuis la présence du `seed` si absent (e2e toujours seedés explicitement, non affectés). (2) **Sélecteur de talent du Dummy** ajouté dans `SandboxPanel`, miroir du sélecteur joueur (avant : uniquement via le champ JSON `dummyAbility`). (3) Statut Dummy repositionné au-dessus du Volatil (cohérence colonne joueur). (4) Défaut moveset joueur = `definition.movepool.slice(0,4)`, aligné sur ce que le combat utilise réellement (corrige un décalage panneau↔combat). (5) `debugDecorationsFootprint` retiré (checkbox + champ config) — code mort depuis la migration Babylon (0 consommateur). Décisions #685–#686.
> MAJ : 2026-07-19 — **Migration pipeline assets voxel : Voxigen mort (payant, export GLB verrouillé) → Goxel + gltf-pipeline.** Nouveau pipeline : édition Goxel (open source, Flathub) → export glTF avec « Couleur de somme » cochée (vertex colors `COLOR_0`, requis pour matcher le pipeline de rendu existant) → conversion `.gltf → .glb` via `npx gltf-pipeline`. Sources `.gox` dans `assets-src/voxel/goxel/`, `.vxb` legacy dans `assets-src/voxel/voxigen/`. Nouveau dossier `packages/app/public/assets/decorations/` (arbre, herbe haute — distinct de `assets/ui/`), pour réutilisation future dans un éditeur de map. `arrow.gltf → arrow.glb` (`ARROW_GLB_URL`) pour le direction-picker. Décorations tree/tall_grass **stagées, pas câblées** dans le renderer (lifts mesurés pour le câblage futur : tree 31/24, tall_grass 3/24, scale 1/24 — chantier séparé). Détails → `docs/references/voxel-tile-placement.md`. Décision #681.
> MAJ : 2026-07-19 — **Nettoyage dette technique (3 items) — plan 164 (design-tokens-centralization) clos `abandoned` après audit.** (1) Tag tooltip `superVsWater` hardcodé (plan 113, dette depuis 2026-06-05) → `packages/ui-dom/src/move-tooltip.ts` génère désormais le tag `typeEffectivenessOverride` dynamiquement depuis `against`/`multiplier`, avec 18 nouvelles clés i18n `pokemonType.<id>` (FR+EN) ; générique pour tout type/multiplicateur futur. (2) Code mort ère Phaser purgé de `packages/app/src/constants.ts` (`INFO_PANEL_*`, `ACTION_MENU_*`, `TIMELINE_*`, `BATTLE_LOG_*`, `DEPTH_BATTLE_LOG`, importés nulle part). (3) **Dette « centralisation tokens couleurs/police DOM↔Babylon » (2026-05-19) close** : l'audit a montré qu'elle était surtout du code mort de plus (`BACKGROUND_COLOR`, `BACKGROUND_COLOR_CSS`, `TYPE_COLORS`, `TEXT_COLOR_*`, `FONT_FAMILY` de `packages/app/src/constants.ts`, 0 usage) — purgé. Le résidu vivant (police, couleurs d'équipe) était déjà en source unique (`view-core`/`render-ports` + `tokens.css`) ; verrouillé par un nouveau test de parité `packages/app/src/styles/tokens-parity.test.ts`, sans codegen (jugé sur-ingénierie). Décisions #679–#680.
> MAJ : 2026-07-18 — **Plan 163 — Content-fill : 7 derniers talents Gen 1 (107 → 114 talents) — pool des talents portés par le roster Gen 1 désormais couvert à 100 %. Objets tenus vérifiés 117/117 déjà complets.** Recompté sur la vraie source (le tableau `abilityHandlers`, pas un regex `id:"..."` qui rate les talents créés via factory/const) : les 6 talents crus manquants (Engrais/Brasier/Torrent/Essaim/Cuvette/Corps Gel) étaient **déjà implémentés** (factory `pinchBooster`/`makeWeatherHeal`) — reste réel = 7, pas 13. **Récolte** (`harvest`, Noeunoeuf/Noadkoko) : fin de tour, recrée la dernière baie consommée (100 % Soleil / 50 % sinon, `recycleConsumedItem`). **Délestage** (`unburden`, Kicklee) : Vitesse ×2 (mouvement + CT) tant que le porteur a perdu/consommé son objet tenu, via `effectiveBaseSpeed` + flag `unburdenActive` posé aux mutations d'objet (held-item-transfer). **Piège Sable** (`arena-trap`, Taupiqueur/Triopikeur) : les ennemis **au sol** adjacents (Chebyshev r1) ne peuvent plus se déplacer (gate `getLegalActions` + helper `isArenaTrapped`) — exemptions canon Vol/Spectre/Lévitation/Fuite/Gaz Inhibiteur/Carapace Mue, badge « Piégé » + flottants « Piégé ! »/« Libéré ! » (events `ArenaTrapped`/`ArenaReleased`) ; **Fuite** (`run-away`, plan 158) gagne enfin un effet réel. **Gaz Inhibiteur** (`neutralizing-gas`, Smogo/Smogogo) : neutralise les talents dans un rayon **Manhattan r2** (`recomputeGasSuppression`, flag `abilitySuppressedByGas` lu par `effectiveAbilityId`), sauf lui-même — badge « Talent neutralisé ». **Fouille** (`frisk`, Grodoudou) / **Prédiction** (`forewarn`, lignées Soporifik+Lippoutou) / **Anticipation** (`anticipation`, Évoli) : révélations d'info ennemie (flags `revealedItem`/`revealedTopMoveId`/`revealedAbility` posés à l'entrée en combat, badges InfoPanel au survol : Objet / Menace / Talent) — scaffolding d'information cachée multijoueur, valeur latente en solo plein-info ; Anticipation non-canon (révèle le talent, choix humain assumé). Infra core : nouveaux champs `PokemonInstance`, events `ArenaTrapped`/`ArenaReleased`, helpers `BattleEngine.isArenaTrapped`/`recomputeGasSuppression`, export core `recycleConsumedItem`, helper data `strongestMoveId`. Gate : typecheck monorepo vert, **3627 tests core+data verts**, human-testing validé, **10 e2e §5.37** (Délestage reporté 👁, cf. `docs/next.md`). Décisions #673–#676. **Talents Gen 1 complets — reste ~156 heldItems hors-Gen1 (méga-pierres/objets Phase 9) et les grosses phases (6/7/8) ou du polish, plus aucune « famille » ou reliquat talent/move Gen 1 à combler.**
> MAJ : 2026-07-18 — **Plan 162 — Content-fill : 9 derniers moves apprenables Gen 1 (503 → 512 moves) — Gen 1 movepool essentiellement complet.** Clôt le reliquat recompté (14 apprenables non-impl, 5 écartés canon → 9 réellement faisables). **Relâche** (`spit-up`, Normal Spé, Single r3, dégâts = 100 × paliers Stockage, consomme) et **Avale** (`swallow`, Normal Statut Self, soin 25/50/100 % selon paliers, consomme) débloquent l'upgrade de **Stockage** (`stockpile`) : d'un stopgap (+1 Déf/+1 Déf.Spé fixe) vers un vrai compteur de paliers 1-3 (`PokemonInstance.stockpileCount`), avec tracking du boost réel appliqué pour un undo exact (fidèle canon, edge case cap +6). **Prio-Parade** (`upper-hand`, Combat Phys, Single r1, réutilise `failsUnlessTargetAggressive` — fraîcheur d'action, pas de priorité canon — + flinch 100 %). **Piège de Venin** (`venom-drench`, Poison Statut, Single r3, baisse Atq/Atq.Spé/Vit d'une cible EMPOISONNÉE, échec sinon). **Rayon Lune** (`moonlight`) / **Aurore** (`morning-sun`) : soin météo-dépendant, core déjà câblé (`WEATHER_HEAL_MOVE_IDS`). **Partage Garde** (`guard-split`, Psy Statut, Single r3) : moyenne Déf+Déf.Spé lanceur↔cible via override brut par-instance (`defenseStatOverride`/`spDefenseStatOverride`, mirror `speedStatOverride`). **Métalaser** (`steel-beam`, Acier Spé, Ligne 3, dégâts + recul 50 % PV max via extension `Recoil.ofMaxHp`, peut auto-K.O.). **Grêle** (`hail`, Glace Statut Self) : pose la météo Neige 5 tours — premier setter de Neige jouable en Gen 1 (`snowscape` avait 0 learner), sans dégâts (aligné système météo). Infra core : 5 nouveaux `EffectKind` (Stockpile/SwallowHeal/ConsumeStockpile/VenomDrench/GuardSplit), `DynamicPowerKind.StockpileLayers`, events `Stockpiled`/`StockpileReleased`/`GuardSplit`, flag `MoveDefinition.failsWithoutStockpile`, extension `effectiveCombatStats` (overrides Déf/Déf.Spé), flottant générique « Échec ! » désormais sur tout `BattleEventType.MoveFailed`. Human-testing validé (6 scénarios), 15 e2e verts (§5.36). **503 → 512 moves.** Décisions #666–#672. **Le pool apprenable Gen 1 des moves est essentiellement complet — reste ~13 talents portés non-impl (Engrais/Brasier/Torrent/Essaim/Cuvette/Corps Gel/Fouille/Piège Sable/Prédiction/Récolte/Délestage/Anticipation) ; la suite = grosses phases (6/7/8) ou polish, pas une nouvelle famille de mécaniques.**
> MAJ : 2026-07-17 — **Session content-fill OP sets emblématiques (189 → 203 OP sets).** 14 sets ajoutés à `packages/data/op-sets/op-sets.json`, mettant en valeur des moves récemment implémentés (reliquats différés des batches Misc A/C/D + famille Priorité/timing plan 150) : **Arbok** — Suc Digestif — Restes, **Ronflex** — Cognobidon — Restes, **Tygnon** — Mitra-Poing — Restes, **Alakazam** — Échange — Ceinture Force, **M. Mime** — Imitation — Restes, **Florizarre** — Soucigraine — Restes, **Ectoplasma** — Malédiction — Boue Noire, **Ectoplasma** — Coup Bas — Orbe Vie, **Persian** — Bluff — Orbe Vie, **Kangourex** — Bluff — Restes, **Kangourex** — Puissance — Orbe Vie, **Aquali** — Bâillement — Restes, **Rattatac** — Croc Fatal — Orbe Flamme, **Colossinge** — Mitra-Poing — Restes. Écartés : Empiflor (quota 2 sets atteint), Tartard/Poliwrath (variété), Florizarre + Suc Digestif (learnset illégal), Vol Magnétik (0 learner Gen 1, aucune cible — définitivement impossible tant que Gen 1 est le seul roster). Résout les bullets `docs/next.md` OP sets Batch C, OP sets emblématiques Priorité/timing (plan 150), OP sets Puissance (plan 151) ; Batch D partiellement couvert (Malédiction/Cognobidon/Bâillement, Attraction/Vol Magnétik restent) ; Batch B reste « si besoin » (Croc Fatal désormais couvert).
> MAJ : 2026-07-14 — **Plan 161 — Passe IA Phase 3 (dernières familles différées) — CLÔT TOUT LE CHANTIER IA (plans 159 → 160 → 161).** Suite du plan 160 (reliquat Phase 3). Toutes les modifs dans `action-scorer.ts` (scorer pur, pas de nouveau chemin moteur). Familles nouvellement valorisées (auparavant scorées ~0/négatif, donc jamais jouées) : **Stat/state manip** (plan 146 — Boost/`psych-up`, Renversement/`topsy-turvy`, Permugarde/Permuforce/Permucœur via `SwapStatStages`, Permuvitesse/`speed-swap`) — `scoreStatManip` réécrit en gain net signé (somme des crans cible − somme des crans soi), ×1.5 sur la menace n°1 ; **Buée Noire** (`haze`, `ResetStatStages` zone r3) — nouveau `scoreHazeReset`, bilan team-agnostic signé ; **Bain de Smog** (`clear-smog`) — bonus reset si la cible est boostée ; **Recyclage** (`recycle`, `RecycleItem`) — valorisé si un objet a été consommé ; **Calcination** (`incinerate`, `BurnTargetItem`) — ajouté à la valorisation item-manip ; **Attraction** (`attract`) — threat mult si la cible touchée est la menace n°1 ; **Barrage/Regard Noir** (`block`/`mean-look`, pièges purs `positionLinked`) — bonus épinglage menace n°1 ; **Cognobidon** (`belly-drum`) — garde anti-mort supplémentaire (`wouldKoUs → −1`, l'IA ne se suicide plus juste avant de profiter du +6). crit-manip Batch A (151) et Gaz Corrosif déjà couverts par les plans précédents ; objets légers (158) = passifs neutres pour l'IA, rien à faire. Gate : typecheck vert, **3442 tests core verts** (3099 unit +7 nouveaux, 343 intégration), zéro régression. e2e/human-testing toujours bloqués par le harness sandbox (mode `dummyControl: "scored"` manquant, cf. `docs/next.md`). **Toutes les familles d'heuristiques IA de la roadmap sont désormais traitées — le chantier IA (159 → 160 → 161) est clos.**
> MAJ : 2026-07-14 — **Plan 160 — Passe IA groupée Phase 2 (suite du plan 159, toutes les familles restantes en un seul plan).** Décision humaine « fait toute l'IA » : plutôt que d'itérer batch par batch sur des dizaines de sessions, toutes les familles de moves différées côté scoring IA sont traitées ici. Nouveau fichier core `ai/move-reach.ts` (extraction `getMoveMaxReach`) + **8 primitives** ajoutées à `threat-detection.ts` (`anyEnemyCanStrike`, `anyEnemyPhysicalStriker`, `abilityNeutralizeValue`, `abilityCopyValue`, `bestGroundThreatFraction`, `survivesLethalHit`, `isImmuneToMoveType`, `occupantAt`). Le scorer (`action-scorer.ts`) valorise désormais finement des familles auparavant scorées ~0/−1 (donc jamais jouées) : **Faux-KO/immunité transverse** (l'IA ne croit plus tuer un porteur de Ceinture Force/Bandeau/Baie Sitrus/Fermeté à 1 PV, ni sur-évaluer un move Sol contre Lévitation/Ballon), **Sacrifice/Self-KO** (Explosion/Destruction/Explo-Brume, Souvenir, Tout ou Rien, Vœu Soin — le revive d'allié KO était injouable —, Lien du Destin/Rancune), **Lock-in** (Mania/Danse Fleurs/Colère/Grand Courroux/Brouhaha/Ball'Glace), **Priorité/timing** (Bluff, Coup Bas, Mitra-Poing/Bec-Canon/Carapiège — risque de charge modélisé), **Dégâts-util** (Croc Fatal et Faux-Chage étaient injouables, Ruse, Anti-Air, Poursuite ×2 de dos), **Manip-talent** (Suc Digestif/Soucigraine neutralisent un talent défensif, Imitation/Échange copient/échangent un talent offensif), **Buff/statut** (Bâillement différé sur menace, Acupression self-cast corrigé, Vol Magnétik fuite de menace Sol), **Grille** (Par Ici/Poudre Fureur back-setup allié, Après Vous promeut un allié prêt à frapper, Interversion esquive défensive), **Field global** (Gravité/Zone Étrange/Zone Magique/Vent Arrière — Vent Arrière était injouable, bug de routage corrigé), **Phazing** (Cyclone/Hurlement/Projection, déni de setup par éjection), **Move-copy** (Métronome/Blabla Dodo/Mimique/Photocopie, garde-fous + valorisation minimale). **Décision archi héritée du plan 159** : heuristiques COMMUNES à tous les niveaux de difficulté (pas de branche par difficulté). Gate : typecheck monorepo vert, **3435 tests core verts (+20)**. Biome injouable dans la session (contrainte environnement) — lint reporté au prochain `/ci-gate`. **Reporté** : Stat/state manip (plan 146), item-interaction utilitaires (Tour de Magie/Passe-Passe/Recyclage, plan 142), pièges purs position-linked (Barrage/Regard Noir), crit-manip Batch A (Puissance/Affilage/Cri Draconique, plan 151), Cognobidon/Attraction (plan 154), objets légers (plan 158, passifs neutres pour l'IA), e2e Playwright des nouvelles heuristiques.
> MAJ : 2026-07-14 — **Plan 159 — IA compétente sur « Le Mur » (ring-out par recul) + passe heuristiques haut-impact + carte disponible.** Nouvelle carte **« Le Mur »** (`le-mur.tmj`, 16×16) ajoutée au `MAPS_REGISTRY` : mur pyramidal central (h≈4.5), rampes latérales seules praticables, glace partout ailleurs — **distincte de l'ancienne carte `le-mur` du plan 066** (remplacée par la Toundra en 2026-04-24), celle-ci conçue spécifiquement pour la mécanique de ring-out. **L'IA maîtrise désormais le ring-out par recul** : nouveau module core pur `battle/knockback-prediction.ts` (`predictKnockbackOutcome`/`resolveKnockbackDestination`/`resolveIceSlide`/`getKnockbackDirection`) extrait de `handle-knockback.ts` (source unique, handler refactoré, **31 tests knockback/chute verts, zéro régression**) + `BattleEngine.predictKnockback`. `scoreKnockbackRingOut` (`action-scorer.ts`) valorise un move à recul (Draco-Queue, Coud'Krâne, Draco-Charge) qui éjecte un ennemi dans le vide ou sur terrain létal = KO (score **× 9,4** vs alternative sans recul en validation), malus dur si un allié serait éjecté. **Lookahead relief corrigé** : `BattleEngine.estimateDamage` accepte `attackerPosition?` (hauteur/terrain/facing calculés depuis la case candidate, pas la position actuelle — vrai bug corrigé) + `hasLineOfSightFrom` garde dans `evaluateAttacksFromPosition` (élimine les positions « sniper fantôme » à travers le mur) — l'IA grimpe désormais le plateau pour tirer en surplomb (estimation **× ~1,9**). **Heuristiques haut-impact** (`action-scorer.ts` + primitives `threat-detection.ts` `highestThreatEnemy`/`wouldKoUs`/`isHealthyTarget`) : OHKO (déni de menace ×1.5/×2), Malédiction (DoT Spectre valorisé sur cible en forme), Transform (préfère copier la menace n°1), crit-manip (conditionné à un move offensif présent), item-interaction (valorisé si la cible porte un objet). Décision actée : la glace n'est PAS décorative — mécanique intentionnelle de ring-out (recul→glissade→chute), cohérente avec la Toundra. Human-testing validé (6 scénarios). Gate CI full vert. Décisions #660–#664. **Reporté** : positionnement offensif/défensif pour le ring-out (Phase 2), heuristiques fines des 10 familles restantes + objets plan 158 (Phase 2), e2e Playwright des nouvelles heuristiques. **Anomalies pré-existantes signalées (hors 159)** : test `magic-room`/Life Orb flaky (jets non seedés) ; `BattleEngine.getLegalActions` n'exclut pas un acteur auto-KO au recul (garde `currentHp > 0` manquante).
> MAJ : 2026-07-13 — **Plan 158 — Content-fill : objets tenus légers + talents ability1 silencieux (106 → 117 objets tenus, 105 → 107 talents, pool talents corrigé 115 → 114).** Deux volets réutilisant l'infra existante à faible coût. **Volet A — 11 objets tenus légers** : **Bandeau** (`focus-band`, 10% survie à 1 PV depuis tout PV, non consommé, roll PRNG déterministe), **Griffe Rasoir** (`razor-claw`, +1 cran crit permanent), **Poing Chance** (`lucky-punch`, +2 crans crit, Leveinard uniquement), **Dé Pipé** (`loaded-dice`, multi-frappe forcée au max), **Cape Obscure** (`covert-cloak`, protège des effets secondaires entrants — miroir objet d'Écran Poudre), **Pierrallégée** (`float-stone`, poids ÷2), **Bande Étreinte** (`binding-band`, ×2 dégâts/tour des pièges du porteur), **Accro Griffe** (`grip-claw`, durée des pièges partiels forcée au max), **Carapace Mue** (`shed-shell`, immunité totale au piégeage), **Poudre Métal** (`metal-powder`, Déf + Déf.Spé ×1.5, Métamorph non transformé), **Poudre Vite** (`quick-powder`, Vitesse ×2, Métamorph non transformé). Infra core : 6 flags déclaratifs sur `HeldItemHandler` (`immuneToTrapping`/`maximizesTrapDuration`/`doublesTrapDamage`/`maximizesMultiHit`/`survivesLethalHitChance`/`suppressesIncomingSecondary`), lecture engine dans `effective-weight.ts`/`effective-base-speed.ts` (via `heldItemId` direct), `handle-status.ts` (3 items pièges), `handle-damage.ts` (Bandeau + Dé Pipé), `effect-processor.ts` (Cape Obscure). Fix journal : `BattleLogFormatter` `StatusBlocked` reason `HeldItem` → « L'objet de {X} le protège ! » (au lieu de « Rune Protect »). **Volet B — 2 talents ability1 silencieux** : **Fuite** (`run-away`, 7 porteurs — Rattata, Rattatac, Ponyta, Galopa, Doduo, Dodrio, Évoli — pas de fuite/combat sauvage), **Ramassage** (`pickup`, Miaouss — pas de loot hors-combat). Marker handlers vides (précédent Magné-Contrôle) : ces 8 mons cessent d'avoir un talent fantôme (le loader ne charge que le slot ability1, ces talents étaient équipés sans handler → aucun effet). **Correction de comptage** : le pool réel de talents = **114** (union ability1/ability2/hidden des 151 Gen 1), pas 115 — dérive cumulée dans `implementations.md` §Récapitulatif, corrigée. Gaps documentés (non bloquants) : Zone Magique ne supprime pas Pierrallégée/Poudre Vite/Cape Obscure/items-pièges (lecture `heldItemId` directe hors chemin `fieldGlobal`) ; heuristiques IA fines non ajoutées (objets passifs neutres pour l'IA). **Reporté** : e2e Playwright des 9 objets non couverts.
> MAJ : 2026-07-12 — **Plan 157 — Batch B-META : Morphing / Imposteur / Métamorph (502 → 503 moves, +1 talent, 150 → 151 Pokemon jouables — roster Gen 1 complet).** Copie d'identité complète : **Morphing** (`transform`, Normal Statut, cible `normal`, Single **r3** — portée étendue au playtest, décision #658 ; PV/niveau du lanceur inchangés, décision #649) copie stats de combat/crans/types/4 moves/talent/poids/genre/sprite de la cible, appris par **Mew**. **Imposteur** (`imposter`, talent) déclenche Morphing à l'entrée en combat sur l'ennemi le plus proche — traduction canon-fidèle de « celui d'en face » (décision #652). **Métamorph** (Ditto, dex 132, dernier Gen 1 manquant du roster plan 135) rejoint le roster : entrée `custom` (movepool `[transform]`, genderless, stats 48 partout, talent `imposter`), sprite 0132 extrait, bundle repacké (152 sprites). Architecture : champ unique `PokemonInstance.transformState` (snapshot `definitionId`/`combatStats`/`baseSpeed`/`types`/`abilityId?`/`moveIds`/`weight`/`gender`), reset au KO (décision #648). Priorité **« manip écrase »** (décision #656) : `override spécifique > transformState > espèce` — un Échange/Détrempage/Permuvitesse ultérieur repose son override qui gagne sur le morph, mais `applyTransform` purge ces overrides au cast pour que le morph soit la couche active immédiatement après. Helpers `effectiveCombatStats`/`effectiveMoveIds`/`effectiveWeight`/`effectiveGender` (nouveaux) + extension `effectiveAbilityId`/`resolveBaseTypes`/`effectiveBaseSpeed`. Vitesse copiée pilote aussi la portée de déplacement (décision #647). Le **type copié s'applique pleinement au terrain** (décision #659, playtest) : un morphé en Léviator lévite/nage pleinement (lave/eau profonde OK, pas de malus marais/sable) — aucune décorrélation nécessaire, tous les chemins terrain sont déjà transform-aware. Gates d'échec (décision #653) : Clone, lanceur/cible déjà transformé, cible portant Imposteur (anti-boucle Ditto vs Ditto). Renderer : event `Transformed` + port `setSpecies` (swap sprite, interaction Substitut gérée). IA : garde-fou obligatoire `scoreTransformApplication` (décision #657, pas différé — évite de gâcher 900 CT pour copier un mon plus faible). Human-testing validé. Décisions #647–#659. **Reporté** : e2e Playwright (via `test-writer`, en cours), heuristiques IA fines (passe groupée).
> MAJ : 2026-07-11 — **Plan 156 — Buff de stat multi-allié (500 → 502 moves).** Dernier item résiduel de la Phase 4 « mécaniques complexes », clos avec ce plan. Réutilise `StatChange` (0 nouveau `EffectKind`) : nouveaux champs `radius?`/`abilityGate?` sur l'effet (mirror `HealTarget.radius` de life-dew), helper core `resolveRadiusAllies`, preview `selfPreviewRadius` étendu. **2 moves**, tous les 2 `targeting: Self` : **Grondement** (`howl`, Normal Statut, Attaque +1 lanceur + alliés en zone diamant Manhattan **r2 auto-centrée** — réinterprétation grille du « adjacent allies » canon, learnable Gen 1 : Arcanin, Feunard, Caninos) et **Magné-Contrôle** (`magnetic-flux`, Électrik Statut, Défense + Défense Spéciale +1 lanceur + alliés en r2 **ayant le talent Plus ou Minus** (`abilityGate: ["plus","minus"]`) — gate fidèle au canon conservé, **no-op actuel dans le roster Gen 1** (aucun porteur Plus/Minus, talents Gen 3), codé par complétude pour de futures gens, learnable Gen 1 : Électrode, Zapdos). Nom FR officiel **Magné-Contrôle** (pas « Influx Magnétik », roadmap corrigée). **500 → 502 moves.** Décisions #644–#646. **Ferme l'item roadmap « Buff de stat multi-allié (~2) »** et une bonne part des résidus Phase 4.
> MAJ : 2026-07-11 — **Plan 155 — Misc Batch E : grille-problématiques (496 → 500 moves).** Cinquième et **dernier** des 5 batches du chantier « Misc volatile / utility » (A crit ✅, B dégâts utilitaires ✅, C manip talent ✅, D buff/statut ✅, E grille-problématiques ← ici). **Chantier Misc CLOS (5/5) — clôture aussi la Phase 4 « mécaniques complexes ».** **4 moves**, tous hors-pool Gen 1 (0 learner, codés par complétude — précédent Bec-Canon/Carapiège #620) : **Par Ici** (`follow-me`, Normal Statut Self) + **Poudre Fureur** (`rage-powder`, Insecte Statut Self, poudre) — zone diamant r4 auto-centrée, réinterprétation « centre de l'attention » canon en **manip d'orientation one-shot** (pas de redirection de cible) : tous les ennemis en zone pivotent pour faire face au lanceur, exploitant le back-attack universel ×1.15 ; l'orientation persiste de fait jusqu'au tour de l'ennemi (pas de volatile à gérer). Poudre Fureur ajoute l'**immunité poudre** (nouveau helper `isImmuneToPowderMove` = type Plante OU Envelocape OU Lunettes Filtre — gap latent signalé pour les moves poudre-statut existants, retrofit optionnel différé). **Après Vous** (`after-you`, Normal Statut, allié r3) : la cible passe strictement prochaine dans le CT via nouvelle méthode **non-destructive** `ChargeTimeTurnSystem.promoteToImmediateNext` (`ct = max(tous) + 1`, ne touche aucun autre CT), flag `PokemonInstance.pendingCtPromotion?` consommé au début d'`advanceTurn()` (mirror Dépit). **Interversion** (`ally-switch`, Psy Statut, allié r3) : échange de positions lanceur↔allié + **terrain re-déclenché** aux 2 cases d'arrivée (mirror SmackedDown/AbilityChanged). Infra : 3 `EffectKind` (`DrawAttention`/`ActAfterUser`/`SwapAllyPositions`), 3 events, 1 champ instance, 1 méthode CT, 1 helper poudre. IA : garde-fous minimaux seulement, heuristiques fines différées (passe IA groupée). Pas d'OP set. **496 → 500 moves.** Décisions #639–#643. Gate local — **à confirmer** (run `/ci-gate` en cours au moment de la doc ; ordre de grandeur attendu ~3413 unit / ~356 intégration). **Reporté** : e2e Playwright des 4 moves, heuristiques IA fines, retrofit immunité poudre Plante sur les moves poudre-statut existants. **Prochaine étape à décider avec l'humain** : passe IA groupée (rattrapage heuristiques fines sur toutes les familles Misc + précédentes), session content-fill (OP sets manquants), ou nouvelle famille de mécaniques.
> MAJ : 2026-07-10 — **Plan 154 — Misc Batch D : buff/statut (490 → 496 moves).** Quatrième des 5 batches du chantier « Misc volatile / utility » (A crit ✅, B dégâts utilitaires ✅, C manip talent ✅, D buff/statut ← ici, E grille-problématiques à venir). **6 moves** : **Malédiction** (`curse`, Spectre, **ciblage conditionnel par type du lanceur** — première du genre : lanceur Spectre → Single r3 ennemi, sacrifie 50% PV max du lanceur + inflige un DoT 25%/tour illimité sans rupture (`StatusType.Cursed`, fire-and-forget) à la cible ; lanceur non-Spectre → Self, Vit −1 / Atq +1 / Déf +1 canon historique pré-Spectre), **Bâillement** (`yawn`, Single r1, sommeil différé — la cible garde une fenêtre de répit d'un tour avant de s'endormir), **Cognobidon** (`belly-drum`, Self, sacrifie 50% PV max → Attaque directement +6, fidèle au canon sans levier grille), **Acupression** (`acupressure`, Single r1 self/allié, +2 stat aléatoire parmi les 5 stats de combat — exclut Précision/Esquive), **Attraction** (`attract`, Single r1, infatuation sexe-opposé), **Vol Magnétik** (`magnet-rise`, Self, lévitation complète temporaire 5 tours — immunité Sol + survole lave/eau profonde/pièges au sol, annulé par Gravité/Anti-Air, grounding-terrain immédiat à l'expiration si la case n'est plus praticable, miroir Anti-Air/Suc Digestif décisions #627/#630). Infra core : 6 `EffectKind` + handlers dédiés, 2 champs `PokemonInstance` (`drowsyTurns`, `magnetRiseTurns`), volatile `StatusType.Cursed`, 3 tick handlers (DoT Malédiction, fin de répit Bâillement, décompte + grounding Vol Magnétik), helper `applyStatStage` extrait (dédup écriture de cran), ciblage conditionnel par type via `resolveEffectiveTargeting` + nouveau flag `MoveDefinition.targetingByCasterType?`. Human-testing validé. Gate local vert : **3393 unit + 352 intégration**. **490 → 496 moves.** Décisions #632–#638. **Reporté** : e2e Playwright (via `test-writer`) pour les 6 moves, heuristiques IA fines (garde-fous minimaux en place : Cognobidon anti-suicide + Attraction gate genre ; le reste = scoring statut générique), OP sets emblématiques (data-miner en content-fill). **Prochaine étape : Batch E — grille-problématiques** (dernier batch du chantier Misc) OU passe IA groupée OU session content-fill.
> MAJ : 2026-07-10 — **Plan 153 — Misc Batch C : manipulation de talent (486 → 490 moves).** Troisième des 5 batches du chantier « Misc volatile / utility » (A crit ✅, B dégâts utilitaires ✅, C manip talent ← ici, D/E à venir). Première mutation runtime du **talent** (miroir conceptuel du `typeOverride` du plan 143). Infra core : `PokemonInstance.abilityIdOverride?`/`abilitySuppressed?` (reset au KO) + helper central `effectiveAbilityId(pokemon)` ; chokepoint `AbilityHandlerRegistry.getForPokemon` routé via le helper (couvre ~20 sites d'un coup) ; 9 sites id-check direct migrés (Lévitation, Infiltration, Moiteur, Garde-Ami, Tension, Brise Moule, Scrappy, Glu). 4 `EffectKind` (`SetAbility`/`SuppressAbility`/`CopyAbility`/`SwapAbility`) + module `handlers/ability-manip/`, event `AbilityChanged`. **4 moves**, tous Statut Single r1 ennemi : **Soucigraine** (`worry-seed`, Plante, remplace le talent cible par Insomnie + réveille), **Suc Digestif** (`gastro-acid`, Poison, supprime le talent cible reste du combat), **Imitation** (`role-play`, Psy, le lanceur copie le talent effectif de la cible), **Échange** (`skill-swap`, Psy, échange les talents lanceur↔cible — ne cible pas un allié, cohérence famille swap). **Interactions complètes** : (a) aura Intimidation retirée quand la source perd son talent ; (b) **grounding immédiat** — dé-lévitation par Suc Digestif au-dessus de lave/eau profonde = noyade/brûlure instantanée (miroir Anti-Air, décision #627). Pas de Magnépiège (talent inexistant côté implémenté). Badge InfoPanel « Talent {nom} »/« Talent scellé », tags tooltip 🧬, i18n FR/EN. Coût CT option A (900, cohérent Vampigraine). **486 → 490 moves.** Décisions #628–#631. **Reporté** : e2e Playwright (via `test-writer`), heuristiques IA fines (passe IA groupée), OP sets emblématiques (data-miner en content-fill). **Prochaine étape : Batch D — buff/statut** (chantier Misc, dernière ligne droite avant E).
> MAJ : 2026-07-07 — **Plan 152 — Misc Batch B : dégâts utilitaires (480 → 486 moves).** Deuxième des 5 batches du chantier « Misc volatile / utility » (Baston/`beat-up` déjà implémenté avant ce plan). Infra core : 2 `EffectKind` (`HalveTargetHp`, `SmackDown`) + handlers dédiés ; flags move `cannotKo` (jamais de KO direct, plancher 1 PV) / `pursuitBackstab` (×2 quand le coup atteint la zone Back de la cible) ; volatile `PokemonInstance.smackedDown?` + helper commun `isEffectivelyGrounded` (= Gravité OU `smackedDown`), routé aux 2 gates d'immunité Sol + gate hazard ; réutilise `bypassProtect` (Ruse) et `bypassAccuracy` (Corps Perdu, never-miss). **6 moves** : **Faux-Chage** (`false-swipe`, Normal Phys, `cannotKo` — laisse toujours ≥1 PV), **Croc Fatal** (`super-fang`, Normal Phys, dégâts fixes = ⌊PV cible/2⌋), **Ruse** (`feint`, Normal Phys, `bypassProtect` — ignore Protection/Détection), **Anti-Air** (`smack-down`, Roche Phys, pose `smackedDown` sur les types Vol — cloue au sol, immunité Sol/hazards levée), **Poursuite** (`pursuit`, Ténèbres Phys, `pursuitBackstab` — ×2 si le coup touche la zone Back de la cible, STACKE avec le bonus de dos universel ×1.15 → **×2.3 effectif de dos**), **Corps Perdu** (`vital-throw`, Combat Phys, `bypassAccuracy` — never-miss, priorité -1 canon abandonnée, coût CT 900 assumé comme move de niche). **480 → 486 moves.** Décisions #625–#627. Noms FR officiels obligatoires. **Reporté** : e2e Playwright (via `test-writer`) + heuristiques IA fines (passe IA groupée). **Prochaine étape : Batch C — manip talent** (Soucigraine/Suc Digestif/Imitation/Échange).
> MAJ : 2026-07-05 — **Plan 151 — Misc Batch A : manipulation de coups critiques (475 → 480 moves).** Premier des 5 batches du chantier « Misc volatile / utility » (~26 moves restants, découpage validé humain : **A crit** (ce plan) → **B dégâts utilitaires** → **C manip talent** → **D buff/statut** → **E grille-problématiques**). Infra core : 2 `EffectKind` (`RaiseCritStage`, `ArmGuaranteedCrit`) + handlers `handle-raise-crit-stage.ts`/`handle-arm-guaranteed-crit.ts` ; flags move `alwaysCrit` (Yama Arashi) / `ignoresDefensiveStages` (Dark Lariat) ; volatile one-shot `PokemonInstance.guaranteedCritArmed?` (distinct du `critStageBoost` persistant réutilisé de la Baie Lansat) ; patch `damage-calculator.ts` (crit forcé sauf `preventsCrit` Coque Armure/Muscle Coque, crans défensifs cible ignorés dans les 2 sens) ; `BattleEngine` consommation post-action + cleanup KO. **5 moves** : **Puissance** (`focus-energy`, Normal Statut Self, crans crit +2 persistants, in-pool Gen 1), **Affilage** (`laser-focus`, Normal Statut Self, prochain coup offensif = crit garanti one-shot, hors-pool), **Cri Draconique** (`dragon-cheer`, Dragon Statut allié r1, crit allié +1/+2 si Dragon, hors-pool), **Yama Arashi** (`storm-throw`, Combat Phys 60, Single 1-1 contact, crit toujours garanti, hors-pool), **Dark Lariat** (`darkest-lariat`, Ténèbres Phys 85, Single 1-1 contact, ignore les crans défensifs de la cible, hors-pool). Renderer : badges InfoPanel, tags MoveTooltip, journal, i18n FR/EN. Pas d'OP set (Puissance niche support, 4 autres hors-pool). **475 → 480 moves.** Décisions #621–#624. **Reporté** : e2e Playwright (via `test-writer`), heuristiques IA fines (passe IA groupée), OP sets Puissance (data-miner si besoin).
> MAJ : 2026-07-05 — **Plan 150 — Famille Priorité / timing conditionnel (469 → 475 moves).** Priorité canon abandonnée partout (le CT ordonnance seul, aucun champ priorité ajouté). « 1er tour » (canon switch-in) → **1ʳᵉ action du combat du lanceur** (`MoveDefinition.firstActionOnly?`, filtré `getLegalActions` + garde `submitAction`). « La cible prépare une attaque » (Coup Bas) → **fraîcheur d'action** : `MoveDefinition.failsUnlessTargetAggressive?` + stamp `PokemonInstance.lastOffensiveActionAtAction?` (mirror `lastActedAtAction`) — réussit ssi la **dernière** action de la cible était offensive (anti-collant `lastUsedMoveId`, contre-jeu par temporisation préservé), fizzle CT payé sinon. « Charge + interruption si frappé » (Mitra-Poing/Bec-Canon/Carapiège) → réutilise la charge 2-tours (plan 094) + nouveau champ groupé `MoveDefinition.chargeReaction? ("focus"|"beak"|"shell")` + module `battle/charge-reaction.ts` (hook réactif dans `handle-damage`, après application des dégâts) + flags transitoires `PokemonInstance.focusInterrupted?`/`shellTrapArmed?`. **6 moves** : **Bluff** (`fake-out`, Normal Phys 40, Single 1-1 contact, `firstActionOnly` + Flinch 100%), **Escarmouche** (`first-impression`, Insecte Phys 100, Single 1-1 contact, `firstActionOnly`), **Coup Bas** (`sucker-punch`, Ténèbres Phys 70, Single 1-1 contact, fraîcheur), **Mitra-Poing** (`focus-punch`, Combat Phys 150, Single 1-1 contact, charge 2-tours, échec T2 si dégât direct ennemi OU allié pendant la charge — indirect/météo/poison/recul ne casse pas), **Bec-Canon** (`beak-blast`, Vol Phys 120, Single 1-1 contact, charge 2-tours, brûle l'attaquant au contact pendant la charge sans interrompre, frappe T2 quand même), **Carapiège** (`shell-trap`, Feu Spé 150, Zone r1 centré lanceur, Manhattan, friendly-fire inclus, charge 2-tours, armé seulement si frappé par un move physique). Bec-Canon/Carapiège = signatures Gen 7, **0 learner Gen 1** — codés quand même par complétude (injouables en Team Builder pour l'instant, prêts pour futures gens). **469 → 475 moves.** Décisions #617–#620. **Reporté** : e2e Playwright (via `test-writer`) + heuristiques IA fines + surveillance playtest (Mitra-Poing/Carapiège = « trap moves » potentiels en formats multi-mons, non bloquant).
> MAJ : 2026-07-04 — **Plan 149 — Famille Lock-in multi-turn (464 → 469 moves).** Verrou multi-tour forcé — adaptation grille + CT : déplacement libre pendant le verrou, seul le CHOIX du move est forcé (comme Provoc/Encore) ; le joueur choisit sa cible librement chaque tour, l'IA cible l'ennemi le plus proche. Infra core : `PokemonInstance.lockInMoveId?`/`lockInTurnsRemaining?` (champ dédié, distinct de `lockedMoveId` — charge 2-tours/Choix — pour éviter toute collision) ; `MoveDefinition.lockIn? { minTurns, maxTurns, confuseOnEnd }` + `uproarAura?` ; module `battle/lock-in.ts` (`beginOrContinueLockIn`, appelé après résolution du move, décrément post-résolution, Confusion self à l'expiration si `confuseOnEnd`) ; module `battle/uproar-aura.ts` (`isUnderNoSleepAura` r3 Manhattan mobile autour du lanceur, sans gate grounded, sonore ; réveille les dormeurs au 1er cast) ; filtre `getLegalActions` + garde `submitAction` ; cleanup KO ; blocage sommeil `handle-status.ts` (`ProtectionReason.UproarNoise`) ; event `BattleEventType.LockInStarted`. **5 moves nouveaux + 1 redesign** : **Mania** (`thrash`, Normal Phys 120, Single 1-1), **Danse Fleurs** (`petal-dance`, Plante Spé 120, Single 1-2), **Grand Courroux** (`raging-fury`, Feu Phys 120, Single 1-1) — verrou 2-3t → Confusion ; **Brouhaha** (`uproar`, Normal Spé 90, Single 1-3 sonore) — verrou 3t SANS confusion + aura anti-sommeil r3 ; **Ball'Glace** (`ice-ball`, Glace Phys 30, Dash 2) — clone Glace de Roulade (snowball volontaire `RolloutStreak`), PAS un verrou ; **Colère** (`outrage`, Dragon Phys 120, Single 1-1) — **REDESIGN, déjà existant** : l'ancien stopgap appliquait la Confusion self immédiate à chaque cast, désormais vrai verrou 2-3t → Confusion (ne compte pas dans le total de moves nouveaux). Confusion « soft » conservée (pas de dégâts self, cf. game-design §7m) → downside = perte de tempo, choix assumé. Équilibrage validé sans changement (coût CT 900/cast ×2-3 gate déjà la famille). **464 → 469 moves.** Décisions #611–#616. **Reporté** : e2e Playwright + heuristiques IA fines.
> MAJ : 2026-07-04 — **Plan 148 — Famille OHKO / K.O. en un coup (460 → 464 moves).** Première mécanique de KO instantané. Flags `MoveDefinition` `isOhko`/`ohkoIceAccuracyRule`/`ohkoIceImmunity` ; module pur `battle/ohko.ts` (`ohkoAccuracy` = 30 % plat / 20 % Glaciation non-Glace — formule canon niveau-collapsée, tous niveau 50 ; `ohkoImmunityReason` = type/Glace/Fermeté) ; `consumeLockedOn` extrait de `accuracy-check.ts`. Branche dédiée dans la boucle de résolution du `BattleEngine` : immunités pré-checkées AVANT le jet (« sans effet »/Fermeté, jamais « raté »), jet plat ignorant crans/talents/objets/Gravité, Verrouillage garantit + consomme une fois. Dégâts **routés via `handle-damage`** (injection `damage = maxHp` sur `isOhko`) → Protection / Ténacité / Baie Ceinture / Clone / immunité de type **gratuits** ; Fermeté = immunité totale (bypass Brise Moule). Multi-cible = jet 30 % indépendant par cible. Event `OneHitKo` (« C'est un K.O. direct ! »). IA : garde-fou `scoreOhko` + `BattleEngine.ohkoImmunityAgainst` (malus dur sur cible immunisée). Tag tooltip ☠. 4 moves : **Abîme** (`fissure`, Sol, Ligne 3), **Guillotine** (`guillotine`, Normal, Single 1-1 contact), **Empal'Korne** (`horn-drill`, Normal, Ligne 2 contact), **Glaciation** (`sheer-cold`, Glace, Cône 1-2, cible Glace immunisée). +3 OP sets (Scarabrute Brise Moule/Guillotine, Lokhlass Glaciation, Rhinoféros Empal'Korne). **460 → 464 moves. 186 → 189 OP sets.** Décisions #607–#610. **Reporté** : e2e Playwright + heuristiques IA fines.
> MAJ : 2026-07-03 — **Plan 147 — Famille Sacrifice / Self-KO (454 → 460 moves).** Généralisation du self-KO (`MoveDefinition.selfKo?`, distinct d'`isExplosion`, non bloqué par Moiteur) + 2 mécaniques neuves : **KO-trigger** (attribution du coup fatal via `PokemonInstance.lastHitBy {attackerId, moveId}` posé dans `handle-damage.ts`, lu dans `handleKo`) et **revive** (premier move de résurrection du jeu). 7 moves : **Explosion** (`explosion`, self-KO + AoE zone r2, mirror Destruction, bloqué par Moiteur), **Souvenir** (`memento`, self-KO + cible Atq/Atq.Spé −2, portée r3, non bloqué par Moiteur), **Tout ou Rien** (`final-gambit`, dégâts fixes = PV actuels du lanceur, typés Combat/typechart complet, portée contact 1, self-KO conditionnel au hit — divergence assumée avec Effort qui n'applique aucun multiplicateur), **Vœu Soin** (`healing-wish`, RÉINVENTÉ « Second Souffle » — self-KO + revive un allié KO à 50% PV / soigne un vivant à 100% + nettoie les statuts, ciblage tuile r3), **Lien du Destin** (`destiny-bond`, volatile self : lanceur KO avant son prochain tour → son tueur tombe aussi), **Rancune** (`grudge`, RÉINVENTÉ — volatile self : lanceur KO par un move → ce move verrouillé chez l'attaquant jusqu'à la fin du combat, le PP-compteur d'usage n'existe plus dans le jeu). Décisions #603–#606. **Reporté** : e2e Playwright + heuristiques IA fines.
> MAJ : 2026-07-03 — **Famille Phazing / forçage (452 → 454 moves).** Réinterprétation du switch-out canon sur la grille (pas de banc) → éjection vers la zone de spawn. Nouveau `EffectKind.PhazeToSpawn` + handler `handle-phaze.ts` réutilisant `ejectToSpawn` (famille forced-teleport, décisions #564-565), réutilise l'event `Teleported`. Décision humaine : éjection SEULE — crans de stats + volatiles CONSERVÉS (pas de reset complet de switch). 3 moves : **Cyclone** (`whirlwind`, Zone r1 auto-centrée, 0 dmg), **Hurlement** (`roar`, Cône 1-3 sonore, 0 dmg — CONVERTI depuis un ancien −1 Atk stopgap, hors OP sets), **Projection** (`circle-throw`, Combat Phys Single 1-1 contact, dégâts puis éjection). Décisions #600–#602. **Reporté** : e2e Playwright + heuristiques IA fines (l'IA score ces moves en neutre — éjecter un ennemi n'est jamais un contresens).
> MAJ : 2026-07-02 — **Plan 146 — Famille Stat/state manip (444 → 452 moves).** Première manipulation partagée des crans de stats (reset / copie / inversion / échange). Infra core : champ d'instance `speedStatOverride?` (mirror `typeOverride`, override Vitesse brute persistant, reset au KO) + helper `effectiveBaseSpeed` routé sur les 2 chemins Vitesse (`computeMovement` ×site hazard + `getCtGainForPokemon`) ; 5 nouveaux `EffectKind` (`ResetStatStages`/`CopyStatStages`/`InvertStatStages`/`SwapStatStages`/`SwapRawSpeed`) + handlers `battle/handlers/stat-manip/` (helper partagé `applyStageWrite`) ; `EffectContext.pokemonInRadius` (voisinage Manhattan, vivants) ; helper Clone `statManipBlockedBySubstitute` (override tactique du flag `bypasssub`, précédent Dépit) ; 5 events chapeau + formatter FR/EN. **Buée Noire** (`haze`, zone diamant r3 auto-centrée, reset team-agnostic lanceur inclus, ignore le Clone/Brume), **Boost** (`psych-up`, copie les crans cible), **Bain de Smog** (`clear-smog`, Poison Spé 50, dégâts puis reset cible), **Renversement** (`topsy-turvy`, inverse les crans), **Permugarde/Permuforce** (échange Déf-DéfSpé / Atq-AtqSpé), **Permuvitesse** (`speed-swap`, échange Vitesse brute canon Gen 7), **Permucœur** (échange les 7 crans). Ciblage allié émergent supporté (Boost copie un allié, Permu* redistribuent). Garde-fou IA minimal (anti-contresens Renversement/Copy/Swap). 6 hors-pool gardés pour Team Builder libre. Gate : 3270 unit + 332 intégration verts. Décisions #595–#599.
> MAJ : 2026-07-02 — **Plan 145 — Famille Field global (440 → 444 moves).** 4 moves canon « pleine arène » relocalisés en mécaniques positionnelles (seule la météo reste full-arène). Infra core : `FieldGlobalZone` générique paramétrée par `kind` (mirror `DistortionZone` plan 130), `state.fieldGlobalZones[]`, `field-global-system.ts` (pose façon Champs, 5 tours du lanceur + horloge fantôme), `EffectKind.PostFieldGlobal`. **Gravité** (`gravity`, zone diamant r3 : Volants cloués au sol via `isGroundedByGravity` sur 3 sites exacts — immunité Sol/hazards/knockback —, précision ×5/3 vs défenseur en zone, bloque Vol/Rebond/Pied Voltige du lanceur en zone, Tunnel/Plongée légaux). **Zone Étrange** (`wonder-room`, zone diamant r3, échange Déf↔DéfSpé du défenseur, crans restent sur leur slot — canon Gen 6+). **Zone Magique** (`magic-room`, zone diamant r3, neutralise les effets d'objet tenu via helper unique `getActiveHeldItem` routant 5-6 sites de lecture d'objet, objet non consommé). **Vent Arrière** (`tailwind`, champ directionnel global unique N/S/E/O, pas une zone — ctGain ×1.5 pour tout Pokemon orienté dans le sens du vent, snapshot frontière de tour, décompte round-global, cumul après Distorsion). Hors moves : boussole 3D always-on (`compass.glb`) près du portrait actif + HUD flèche Vent Arrière (rotation iso 45° + caméra). Décisions #591–#594.
> MAJ : 2026-06-29 — **Plan 144 — Famille Move-copy (434 → 440 moves).** Première vraie réentrance moteur. Infra core : `CallMoveSourceKind` enum, `MoveDefinition.callMove`, `PokemonInstance.pendingCalledMove`, `BattleState.lastMoveUsedGlobally`, `EffectKind.CopyMoveToSlot`, helpers `move-copy/callable-moves.ts`, events `MoveCopied`/`MoveCopyFailed`. Réentrance = `BattleEngine.prepareCalledMove` (query : roll/résout → pose `pendingCalledMove`, idempotent verrou anti-reroll, n'avance pas le tour) + swap dans `resolveEffectiveMove` + lazy-prepare/clear/global-last dans `executeUseMove` + cleanup KO/fin de tour. 6 moves : **Métronome** (random total − exclusions), **Blabla Dodo** (random moveset propre, gate sommeil), **Mimique** (dernier move cible), **Photocopie** (dernier move global), **Copie** + **Gribouille** (remplacent leur slot). Renderer : UI 2-temps réutilise `select_attack_target` via swap pending, masquage `???` (chrome source + flag `masked`), divert Mimique dans `tryPickTarget`. IA : `enrichCalledMove`. Tags tooltip 🎲/🪞/📋. `lastUsedMoveId`=source, `lastMoveUsedGlobally`=move appelé (jamais metamove). Mimique/Photocopie/Copie/Gribouille hors-pool (0 learner). 3566 unit verts. Décisions #587–#590. **Reporté** : heuristiques IA fines (deferred, non bloquant) ; e2e Playwright (à ajouter via test-writer).
> MAJ : 2026-06-28 — **Famille Pièges (Trapping) — 423 → 429 moves.** 4 pièges partiels (dégâts + Trapped 4-5t + 1/8 PV/tour, attaque possible, déplacement bloqué) : Étreinte (`bind`, Contact 1-1), Danse Flammes (`fire-spin`, 1-2), Siphon (`whirlpool`, 1-2), Tourbi-Sable (`sand-tomb`, 1-2). 2 pièges purs position-linked (lock tant que le lanceur reste à distance Chebyshev ≤ 1) : Barrage (`block`), Regard Noir (`mean-look`). Nouveau champ `positionLinked?: boolean` sur `Status` dans `effect.ts` ; handler `position-linked-statuses.ts` ; réutilise mécanique Magnépik. Périmètre in-pool (hors-pool 0 learner ignorés). Décisions #578–#580.
> MAJ : 2026-06-27 — **Plan 142 — Item interaction (411→423 moves, 103→104 talents, 104→106 objets).** Infra core : champs `PokemonInstance` (`consumedItemId`, `ateBerryThisBattle`, `critStageBoost`), helpers `held-item-transfer.ts` (5 fonctions), refacto sites de consommation vers `consumeHeldItem`, lecture `critStageBoost` dans `damage-calculator.ts`. 7 nouveaux `EffectKind` (`RemoveItem`/`StealItem`/`SwapItems`/`FlingItem`/`EatTargetBerry`/`BurnTargetItem`/`RecycleItem`). Table `FLING_EFFECT` + fling power depuis la référence. Flags moves `knockOffBoost`/`requiresEatenBerry`/`requiresFlingableItem`. 12 moves : Sabotage (`knock-off`), Larcin (`thief`), Implore (`covet`), Tour de Magie (`trick`), Passe-Passe (`switcheroo`), Dégommage (`fling`), Picore (`pluck`), Piqûre (`bug-bite`), Calcination (`incinerate`), Gaz Corrosif (`corrosive-gas`), Recyclage (`recycle`), Éructation (`belch`). +2 baies : Baie Lansat (`lansat-berry`, crit-stage +2), Baie Frista (`starf-berry`, stat aléatoire +2). +1 talent : Glu (`sticky-hold`, bloque tout transfert d'objet du porteur). Flottants + ligne objet InfoPanel + e2e §5.25. Décisions #566–#577.
> MAJ : 2026-06-27 — **Bouton Fuite + Carton Rouge (102 → 104 objets tenus). Worktree `objets`.** Famille « téléportation forcée vers la zone de spawn » — réinterprétation du switch-out canon (pas de banc dans ce jeu). Nouveau champ `PokemonInstance.spawnPosition` (position initiale estampillée au constructeur du moteur). Helper `ejectToSpawn` dans `battle/forced-teleport.ts`, câblé dans `handle-damage.ts` ; flags déclaratifs `ejectsHolderOnHit` (Bouton Fuite — porteur éjecté vers SA zone de spawn) / `ejectsAttackerOnHit` (Carton Rouge — attaquant éjecté vers SA zone de spawn). Réutilise l'event `Teleported`. Aucune case sûre → pas de téléport, objet non consommé. Décisions #564–#565.
> MAJ : 2026-06-22 — **Plan 141 — 5 talents soutien/objet Gen 1 (98→103 talents).** Gloutonnerie (`gluttony`, seuil baies pincement 25%→50% PV), Tension (`unnerve`, bloque consommation baies ennemies tant que porteur vivant), Moiteur (`damp`, bloque Destruction/Explosion + recul Boom Final depuis n'importe quelle position), Cœur Soin (`healer`, 30% fin de tour soigne statut majeur allié dans rayon Manhattan r2, roll indépendant par allié), Garde-Ami (`friend-guard`, alliés r2 reçoivent ×0.75 dégâts — cumulable Protection/Mur Lumière, non cassable par Brise Moule). Décisions #557–#561.
> MAJ : 2026-06-22 — **Plan 140 — Brise Moule (97→98 talents Gen 1).** Nouveau module `packages/core/src/battle/ability-suppression.ts` — helper `resolveDefensiveAbility(registry, target, attacker)` : si l'attaquant a Brise Moule (`mold-breaker`) et que le talent de la cible est `breakable`, le talent est traité comme absent. Flag `breakable` injecté dans `AbilityDefinition` depuis `abilities.json` (80 talents). 8 sites défensifs migrés (immunités type, Écran Poudre, damage-calculator, survie OHKO, statuts, stat-changes, évasion). Talents réactifs non-cassables (Statik, Corps Ardent, Synchro, Boom Final) toujours actifs. Silencieux (pas d'`AbilityActivated`). Porteur Gen 1 : Scarabrute (`pinsir`, slot 1). Gaz Inhibiteur reporté (pas de porteur Gen-1). Décisions #553–#556.
> MAJ : 2026-06-22 — **Plan 139 — Sans Limite + Sérénité (95→97 talents Gen 1).** Nouveau module `packages/core/src/battle/secondary-effect.ts` exposant `isSecondaryEffect` + `moveHasSecondaryEffect` (extrait depuis `effect-processor.ts`, réutilisé par Écran Poudre). Sans Limite (`sheer-force`) : supprime les effets secondaires d'un move et lui confère ×1.3 puissance (`onDamageModify`) — porteurs Gen 1 : Nidoking, Nidoqueen, Krabby, Krabboss, Tauros. Sérénité (`serene-grace`) : double la chance des effets secondaires (plafond 100 %) — porteur Gen 1 : Leveinard. Sérénité ne touche pas la chance de flinch des objets Roche Royale / Croc Rasoir (hook `onFlinchChance` porté par l'objet, pas le move — canon Showdown). Décisions #550–#552.
> MAJ : 2026-06-22 — **SandboxPanel — sélection talent par pré-sélection nommée.** Option vide « (défaut) » supprimée du `<select>` talent : `getFirstAbility()` pré-sélectionne désormais un talent concret affiché par son nom. Clé i18n `sandbox.abilityDefault` retirée. `SandboxConfig.playerAbility`/`dummyAbility` restent optionnels (fixtures e2e inchangées). InfoPanel combat différé. Décision #549.
> MAJ : 2026-06-22 — **Plan 138 — 17 talents Tier C (78→95 talents Gen 1).** A-pinch factory `pinchTypeBoost` : Engrais/Brasier/Torrent/Essaim. B-météo (thread `weather` dans `DamageModifyContext`+`StatusBlockContext`, nouveau hook `onMoveImmunity`) : Force Soleil (×1.5 spé + chip 1/8, cumul plein météo Feu — décision #547)/Force Sable/Peau Sèche/Feuille Garde/Envelocape. C-réactif (champ déclaratif `statusSpeedBoost`, hooks `onAccuracyModify`+`onEvasionModify` ability) : Armurouillée/Pieds Confus/Pied Véloce (surveiller combo Orbe Flamme — décision #548)/Anti-Bruit/Télécharge/Peau Miracle. D-medium (hook `onDrainAttempt`, `targetAlreadyActed` dans DamageModifyCtx, bypass `infiltratorBypass`) : Agitation/Analyste/Puanteur/Suintement/Boom Final/Infiltration. 7 changements moteur. Tests : 3035 unit / 295 intégration / 165 e2e. Décisions #547–#548.
> MAJ : 2026-06-20 — **Plan 135 — Sprite bundle packing + roster Gen 1 complet (150 Pokemon).** Pipeline sprites refondu : **3 fichiers bundle** (`sprites.bin` atlas PNG+JSON concaténés ~33 Mo, `sprites-manifest.json` index léger byte-ranges + offsets PMD + index portraits, `portraits.png` sheet unique 40×40) au lieu de 4 fichiers par Pokemon. Dist Gen 1 ≈ **120 fichiers** (mur itch.io 1000 résolu — scale à 1000+ indépendant du roster). `pnpm extract-sprites` (dossiers per-Pokemon gitignorés, source/cache) + `pnpm pack-sprites` (génère le bundle shippé). Runtime : `SplashScreen` au boot (titre + barre progression octets), loader engine-agnostic `view-core/sprite-bundle.ts`, blob URLs permanentes par session (`disposeAtlasUrl` retiré — décision #541). Portraits via sheet unique croppé en data URL. **+70 pré-évolutions Gen 1 → 150 Pokemon jouables** (Métamorph/Ditto exclu). Movepool dérivé auto ; gag canon accepté (Magicarpe/Chenipan/Aspicot à 3 moves). Décisions #539–#543. Prochaine étape : Phase 4 mécaniques restantes ou à décider.
> MAJ : 2026-06-20 — **Session objets tenus — 5 batches livrés (42→86 objets).** (1) 29 baies : anti-type ×18 (`typeResistBerry`), pincement stat ×4, soin statut ×7. (2) 4 objets simples : Orbe Toxique, Bandeau Muscle, Lunettes Sages, Grelot Coque — refacto Orbe Flamme → factory `selfStatusOrb`. (3) 4 réaction au coup : Bulbe, Pile, Boule de Neige, Lichen Lumineux — helper `raiseStatStage` dédupliqué. (4) 3 roches météo durée : Roche Humide/Lisse/Glace via mapping core `WEATHER_EXTENDER_ITEM`. (5) 4 granules terrain : Graine Électrik/Herbe/Psychique/Brume — factory `terrainSeedItem` + gate Vol `isOnFieldTerrain`. Tous gate CI verts, human-testing validé par batch, e2e ajoutés. **Familles différées (hooks manquants)** : Veste de Combat/Grosse Racine/Gant de Boxe/Métronome, Carton Rouge/Bouton Fuite/Spray Gorge, Baies Lansat/Frista, familles précision/RNG/herbes — voir `docs/next.md`.
> MAJ : 2026-06-20 — **Batch objets boost de type (18 items, 24→42 objets tenus).** 18 objets canon ×1.2 dégâts du type correspondant (1 par type, non consommés), miroir Gemme Normale sans consommation. Factory `typeBoostItem`. Fichiers : `held-item-id.ts`, `item-definitions.ts`, `type-boost-items.test.ts`. implementations.md mis à jour (tableau Récapitulatif + lignes tableau Objets).
> MAJ : 2026-06-19 — **Plans 133 + 134 — Delayed/countdown + Power conditionnel restant (8 moves livrés, familles closes).** Plan 133 : Prescience/`future-sight` (ciblage sol r4, AoE r1 au landing 2 tours plus tard, horloge fantôme, offense figée/défense live — décision #535), Requiem/`perish-song` (redesign aura mobile r2 caster-bound, décompte 3 tours, KO zone à l'expiration incluant le lanceur — décision #536), Balance/`pain-split` (moyenne PV instantanée), Effort/`endeavor` (PV cible ← PV lanceur si supérieur), Coup d'Main/`helping-hand` (×1.5 prochain move offensif allié adjacent). Fix playtest : match nul double K.O. (`winnerId: null`, `battle.draw`). **403 → 408 moves.** Plan 134 : Hommage Posthume/`last-respects` (`AllyFaintCountScaled` — hors-pool, couverture Team Builder libre — décision #538), Branchicrok/`fishious-rend` + Prise de Bec/`bolt-beak` (`TargetIdleSinceLastAction` ×2 tempo-based — décision #537). **408 → 411 moves. Famille Power conditionnel close.** Décisions #535–538. Prochaine étape : Phase 4 — mécaniques restantes ou roster Gen 1 complet.
> MAJ : 2026-06-19 — **Plan 132 — Famille Contrôle close (3 moves livrés).** Possessif/`imprison` : volatil persistant sur le lanceur, filtre inverse `getLegalActions` (ennemis ne peuvent utiliser les moves que le lanceur connaît aussi), meurt avec le lanceur, coût CT MajorStatus (700). Dissonance Psy/`psychic-noise` : Psy Spé 75 BP, Cône 1-3, move sonore (perce le Substitut via flag `sound`), secondaire Anti-Soin (`StatusType.HealBlocked`) 2 tours garanti — bloque l'usage des moves de soin ET l'application de tout soin entrant (Drain : dégâts OK / soin annulé ; HoT suspendu ; Vœu gaspillé). Dépit/`spite` : taxe CT one-shot (`SPITE_CT_PENALTY = 350`, `PokemonInstance.pendingCtPenalty?`) sur la prochaine action de la cible, bloqué par Substitut. 3 OP sets data-miner (Ectoplasma Possessif/Dépit, Mewtwo Possessif/Dissonance Psy, Alakazam Possessif/Désactivation). **400 → 403 moves. 183 → 186 OP sets.** Décisions #529–534. Famille Contrôle close. Prochaine famille : Delayed/countdown.
> MAJ : 2026-06-19 — **Plan 131 — Entry Hazards (Pièges au sol) livrés.** Nouvelle famille mécanique Phase 4 « Hazards » : 4 setters (Picots/`spikes`, Pièges de Roc/`stealth-rock`, Pics Toxik/`toxic-spikes`, Toile Gluante/`sticky-web`) + 2 removers (Tour Rapide/`rapid-spin`, Anti-Brume/`defog`). `TargetingKind.GroundTarget` portée ≤ 4 Manhattan. Déclenchement à l'entrée de chaque case traversée, permanent, **team-agnostic** (alliés inclus — décision #526). Stacking : Picots ×3, Pics Toxik ×2. Vol immunisé sauf Pièges de Roc (type-efficacité Roche). Absorption Pics Toxik par type Poison. Removers : Tour Rapide r1, Anti-Brume r2. Rendu voxel GLB empilés par couche (`render-babylon/babylon-entry-hazards.ts`), bob idle Pièges de Roc. Source voxel `assets-src/voxel/hazards.vxb`, technique documentée `docs/references/voxel-tile-placement.md`. 4 OP sets data-miner (leads Crustabri/Ptéra, spinner Staross, Anti-Brume Rapasdepic). **394 → 400 moves. 179 → 183 OP sets.** Décisions #525–528.
> MAJ : 2026-06-18 — **Plan 130 — Distorsion (`trick-room`) livrée.** Nouvelle mécanique Phase 4 (famille « mécaniques complexes restantes ») : **zone statique diamant Manhattan r3**, move statut Self, inversion CT par vitesse en entrée (`speedInZone = max(1, 160 − baseSpeed)`, pivot 160), pose façon Champs (re-cast même case = remplace, ailleurs = coexiste, pas de toggle d'annulation), 5 tours du lanceur + horloge fantôme. `FieldTerrainKind.TrickRoom` ajouté. 2 OP sets (Noadkoko Distorsion, Flagadoss Distorsion). **393 → 394 moves. 177 → 179 OP sets.** Décisions #522–524.
> MAJ : 2026-06-18 — Chantier Post-Babylon **g** livré. (1) **Input Dash directionnel** : confirmation par direction survolée (comme Cône/Ligne/Tranche) — `resolveTargetAction` cible la tuile la plus loin, portée auto (moteur s'arrête au premier obstacle), outline d'atterrissage retirée, Dash ajouté à `isDirectionalPattern`. Décision #518. (2) **Preview Dash couleur par tuile** : traînée jaune (`#ffdd44`) ; couleur d'intention (rouge/vert/…) uniquement sous tuile occupée par un Pokemon — dash dans le vide = tout jaune. Décision #519. (3) **Rebalance portées Dash** : Vitesse Extrême → 5 ; nukes/charge/recul lourds → 4 (Giga Impact, Coud'Krâne, Métalliroue, Électacle, Boutefeu, Rapace, Aquatacle) ; Taurogne → 3 ; Roue de Feu/Désherbaffe/Nitrocharge 3 → 2. Décision #520. (4) **Roulade snowball** (`DynamicPowerKind.RolloutStreak`) : portée base 2 → cap 5 (+1/cast consécutif) ; puissance 30 → 60 → 120 → 240 → 480 (×2/cast, cap 480). Helper `rollout-streak.ts`. Décision #521. Tests : 2686 unit + 249 intégration verts. Chantier h (POC Cobblemon) reste.
> MAJ : 2026-06-17 — Chantier Post-Babylon **f** (alignement couleurs previews tooltip ↔ sol) livré. Rouge attaque `#cc4444` (était `#ff6644` — corrigé). Nouveau : vert soin `#44dd44` (`TILE_PREVIEW_HEAL_COLOR`, `--color-preview-heal`, `HighlightKind.PreviewHeal`) + jaune dash `#ffdd44` (`TILE_PREVIEW_DASH_COLOR`, `--color-preview-dash` = `--color-accent`, `HighlightKind.PreviewDash`). Helpers partagés `moveIntent`/`selfPreviewRadius` dans `packages/view-core/src/move-intent.ts`. Grille MoveTooltip : `data-intent` (attack/buff/heal) + `data-cell` ∈ {empty, target, dash, caster, caster-target} ; croix ✖ = marqueur lanceur. Sol : tile lanceur exclue des AoE (Séisme centre vide) ; chemin dash jaune + arrivée en couleur d'intention. `TOOLTIP_CELL_COLOR_*` supprimés (code mort).
> MAJ : 2026-06-17 — Chantier Post-Babylon **e** (plan 128 — drop round-robin) livré. CT seul désormais : `TurnSystemKind`/`TurnManager` supprimés, `activePokemonId: string` remplace `turnOrder[]`+`currentTurnIndex`. Notion de round entièrement retirée (`roundNumber`, dédups auras/météo/champs, HUD). PP usage retiré (`currentPp`, décrément, `NoPpLeft`) — `MoveDefinition.pp` conservé comme poids coût CT. Modèle de durée **« tours du lanceur »** : météo/barrières/champs/Protection décomptent sur les tours propres du caster. **Horloge fantôme** : caster météo/champ mort reste ghost dans le scheduler CT jusqu'à expiry des effets ; barrières (Protection/Mur Lumière/Brume/Rune Protect) meurent avec leur caster. `lastEndureRound` → `lastEndureAtAction`. Décision #517.
> MAJ : 2026-06-17 — Chantier Post-Babylon **d** (curseur voxel FFTA) livré. Curseur de survol remplacé par un modèle voxel `.glb` chargé depuis `assets/ui/cursor.glb` (voxigen.io), rendu en vrai mesh 3D (renderingGroupId 3, bob conservé, `hasVertexAlpha=false`). Mécanique de switch de variantes supprimée entièrement (touche H, `HOVER_CURSOR_OPTIONS`, setting « Curseur », 4 PNG, clé i18n `settings.cursor`). Ecran Paramètres réduit à 2 lignes (Langue, Prévisualisation dégâts). Convention `assets-src/voxel/` créée pour les sources `.vxb` éditables hors bundle. Décisions #514-516.
> MAJ : 2026-06-16 — Release **v2026.6.3** publiée (Babylon.js officialisé). Scrub Phaser complet (docs + commentaires + tooling, commit `b039c3d`) ; topics GitHub mis à jour (`phaser` → `babylonjs`). Phase 5 livrée.
> MAJ : 2026-06-16 (Phase 5 — **Suite harness e2e — ~97 tests (96 passants + 1 fixme)** — Décision #511. Migration complète CSS sélecteurs → `getByRole`/`getByText`/`getByTestId` (reco Playwright Context7) + convention `data-testid` (`.claude/rules/e2e.md`). Hook scène enrichi : `hoverTile(x,y)` (survol info-panel §4.7) + `confirmDirection()` (fin de tour pilotable). **Push couverture §5/§8** : interactions de combat pilotées à travers le renderer — statuts, stats, volatiles, auras, champs, charge, Clonage, semi-invul, météo, **10 patterns de ciblage**, Téléport/hit-and-run — assertées via le **journal FR** (`mechanics-*.spec`, `patterns.spec`) ; les **8 cartes du jeu montent sans crash** + Le Mur multi-niveaux (`maps.spec`). Plafond assumé : couleur/anim du texte flottant + physique terrain fine (LoS/chute/glace §5.17-20, SENS unit core) restent 👁. `test.fixme` + `biome-ignore` justifié pour régression « Tout vider » modale (migration Phaser→Babylon, backlog).)
> MAJ : 2026-06-15 (Phase 5 — **Loader Babylon + fixes rendu**. Écran de chargement porté Phaser→Babylon (oubli de migration, plan 097) : `LoadingOverlay` DOM (`styles/loading-overlay.css`) affiché jusqu'à scène peinte, via nouvelle méthode contrat `CombatScene.whenReady()`. Fix FOUC sprites : préload des atlas d'équipe avant combat (`preloadCombatSprites`) + billboard caché jusqu'au binding de l'atlas (plus de flash blanc). Fix crash console teardown : flèche du direction-picker PBR→`StandardMaterial` (plus d'`environmentBRDF` RGBD) + guard scène disposée. RNG combat live seedé (`createPrng` via `BattleSetup`, replay ≡ live ; `Math.random` = seam test-only). Décision #509.)
> MAJ : 2026-06-15 (Phase 5 — **`render-three` retiré** — Le package POC Three.js a rempli sa mission (valider l'agnosticité du contrat, plan 125-126) et est supprimé. **Babylon est désormais le seul moteur de rendu.** Le seam reste : `RendererBackend` + `getRendererBackend()` dans `packages/app/src/renderer-backend.ts` — ajouter un futur moteur = 1 const + 1 branche. La route `?engine=three` est supprimée. Décision #508.)
> MAJ : 2026-06-15 (Phase 5 — **Plan 126 TERMINÉ** — Revue archi finale + renommages packages : `renderer-contract` → `render-ports`, `presentation` → `view-core`, `render-shared` → `render-canvas2d`, `renderer` → `app`. Relocation `SemiInvulnerableDisplay` → `packages/core/src/types/` (anti-cycle `render-ports`↔`view-core`). Architecture confirmée : Hexagonal/Ports&Adapters + Humble Object + Dependency Inversion. Schéma de nommage `<type>-<tech>` documenté. Décisions #506-507.)
> MAJ : 2026-06-14 (Phase 5 — **POC Three.js VALIDÉ par l'humain** → le découpage plan 125 est prouvé : un 2e moteur complet (`render-three`) tourne le jeu entier via `?engine=three` en réutilisant `presentation` + `ui-dom` intouchés. Recette POC : facing OK (worldFacing brut + swap X/Z `terrain-math` = parité Babylon), ombres rondes occultées par le sprite (passe transparente partagée), sprites ne passent plus sous le terrain (ordre par profondeur du pied), flèches placement voxel (`arrow.gltf` via `GLTFLoader`). **Défauts mineurs résiduels assumés (limites POC, pas le jeu Babylon)** : pas de silhouette X-ray ni `SpriteDepthPlugin` → un sprite derrière un mur haut se dessine par-dessus ; outlines `LineBasicMaterial` en hairline 1px. Décision #505 documente tout. Commits `8fbccbd`→`efa5a43`. Gate vert. **Reste : merge `--ff-only` worktree → main (attente GO humain) ; sort de `render-three` à trancher (garder comme preuve vivante / retirer).**
> MAJ : 2026-06-14 (Phase 5 — **Moteur de rendu sélectionnable + POC Three.js jouable**. Suite du POC : (1) le jeu COMPLET tourne sur Three via `?engine=three` — interface `RendererBackend` (les 2 moteurs structurellement compatibles) sélectionnée au boot et threadée dans `combat-screen`/sandbox ; tout le FSM (menus → team → placement → combat) marche sur render-babylon OU render-three. (2) Bugs corrigés : **clic plateau Babylon** (overlay chrome `.bc-root` avalait les clics — fix `:where(.ui-screen) > *` zéro-spécificité + import CSS direct car Vite-dev ne charge pas les `@import` imbriqués d'un package) ; **orientation sprites Three** (yaw billboard `π/2 − azimuth` au lieu de `azimuth`) ; **sprites sous le terrain Three** (depthTest off + ordre par profondeur du pied, stand-in du `SpriteDepthPlugin` Babylon non porté). Limites POC Three assumées : pas de silhouette X-ray, sprites dessinés au-dessus du terrain haut (pas d'occlusion par mur — coût d'un depth-plugin shader). Commits `15e11b7` (backend) `1f10326` (clic) `ca69fbc` (yaw) `50eab6e` (depth). Gate vert. **Reste : merge `--ff-only` worktree → main (attente GO humain).**
> MAJ : 2026-06-14 (Phase 5 — **POC `render-three` (Three.js) RÉUSSI — le découpage est validé**. Nouveau package `@pokemon-tactic/render-three` implémentant le contrat de rendu en Three.js : terrain extrudé, billboards PMD directionnels, HP bar/statut in-engine, textes flottants, décorations, highlights, picking raycaster, champs/auras, BoardView complet + `createCombatScene`/`createBattleBoardView`. Route `?engine=three` lance un **combat IA vs IA complet** qui tourne de bout en bout, 0 erreur console. **`presentation` (orchestrator/IA/setup) + `ui-dom` (chrome/log/timeline/info/victoire) réutilisés SANS modification** — seule la couche dessin (~moitié) est réécrite. 6 modules portés en parallèle par sous-agents (mirror 1:1 de render-babylon). 6 morceaux de logique pure hissés vers presentation/ui-dom au passage (sprite-facing, sprite-atlas clock, champs borders, aura layout, `mountGameStage`, `loadTiledMap`) → render-babylon allégé aussi. POC skips assumés : silhouette X-ray + depth-plugin (pas d'équivalent Three direct), outlines hairline (LineBasicMaterial). Commits `8fbccbd` (hoists) → `be52478` (scaffold+terrain) → `c3a4833` (combat complet). Gate vert (typecheck ×9, biome 1183, unit 2670, integ 251, build). **Reste : merge `--ff-only` worktree → main (attente GO humain).**
> MAJ : 2026-06-14 (Phase 5 — **Migration Babylon officiellement validée (recette humaine OK)**. Review/cleanup post-découpage (commit `989bf93`) : `weather-hud` asset hardcodé → DI `getWeatherIconUrl` ; `I18nContext` commun (`renderer-contract`) factorise `translate`/`getLanguage`/`getPortraitUrl` entre `PresentationContext` + `UiDomConfig` ; helper DOM `el()` → `ui-dom/src/dom-helpers.ts` ; re-exports morts élagués (`renderer/constants.ts`) ; `tsconfig.base` nettoyé (`declaration`/`sourceMap` morts retirés) ; décision #505 enrichie (point f : trick lib WebWorker anti-DOM). **4 reliquats backlog Babylon clos par acceptation recette** (curseur FFTA, silhouette X-ray P↔P, anims idle, divergence HUD sandbox/FSM → `backlog-archive.md`). **Reste : merge `--ff-only` worktree → main (attente GO humain).** En cours : **POC `render-three`** (Three.js) pour valider le découpage/contrat avant le sceau final.)
> MAJ : 2026-06-14 (Phase 5 — **Plan 125 IMPLÉMENTÉ** — Découpage renderer en 6 packages : `core` (inchangé), `data` (inchangé), `renderer-contract` (ports BoardView/BattleChrome/BattleFeedback, view-models, PresentationContext, RenderBackend, TEAM_COLORS, HighlightKind), `presentation` (battle-orchestrator, view-builders, IA, setup, constants ; agnostique moteur), `render-babylon` (scène/board Babylon, GameStage lifecycle), `ui-dom` (chrome HTML réutilisable, UiDomConfig DI), `renderer` (app-shell : boot, i18n, settings, team UI, câblage DI). Gate CI vert (commit `0b8b8e1`). Décision #505 enregistrée. **Reste : recette visuelle humaine** — vérifier parité combat complet avec la nouvelle structure.)
> MAJ : 2026-06-13 (Phase 5 — **Recette parité lot 3 TERMINÉE** (plan 124) : B1 Clonage ✅, M2 Hurt ✅, M4 outline soutien ✅, M5 preview CT + refonte timeline ✅, M7/M8/M9 events terrain/KO ✅, M10 bob curseur ✅, tier cosmétique K1-K8 ✅, vérifs V1-V10 ✅. Écartés : M1 Sommeil 1-dir, M3 orientation, M6 icône statut timeline, K6/K7/K9. Décisions notables : flux KO/Faint dans `directional-billboard` (Faint joué une fois sur l'edge KO, advance gardé jusqu'au freeze) ; timeline + info panel en colonne flex partagée ; K7/K9 gardés (blend Champs normal, flash émissif) ; `turnSystemKind` ajouté à `SandboxConfig`. Décisions #501-504. **3 correctifs post-recette (combat normal FSM)** : R1 pan caméra libre après déplacement (`cameraTargetGoal` écrasé par drag manuel) ; R2 météo sous le banner de tour (wrapper `.bc-top` flex-column) ; R3 bouton "Retour au menu" placeholder **supprimé** (collision burger BattleLog — sortie mid-combat via victoire/défaite uniquement). **Note archi** : sandbox studio rend le HUD de combat in-canvas, combat normal FSM utilise le chrome DOM — divergence à investiguer (→ backlog). **Suite : J5** — suppression renderer Phaser, merge `--ff-only` sur main.)
> MAJ : 2026-06-12 (Phase 5 — **Jalon 4b EN COURS** (plan 121) — 4b-1 ✅ InfoPanel+WeatherHud ; 4b-2 ✅ ActionMenu+sous-menu+MoveTooltip ; 4b-3 ✅ TurnTimeline DOM (RR+CT) ; 4b-4 ✅ BattleLog DOM ; 4b-5 ✅ previews ciblage (directionnel/statique/blast, alphaIndex). **Jalons 4b + 4c TERMINÉS**. 4c (plan 122) : 4c-1 ✅ textes flottants DOM, 4c-2 ✅ tweens glide déplacement, 4c-3 ✅ anims attaque (catégorie+facing). Reportés 4c : preview dégâts numérique, flash confirm, tweens impact (knockback/ice/teleport), nuance movement-animation (→ J5 parité). **Jalon 4d EN COURS** : Team Builder + Team Select déjà réels (4a étapes 4-5) ; **4d-1 ✅ barres PV monde + icônes statut par sprite** (`babylon-sprite-overlays.ts`, projetées par frame comme les pills Champs, drain live sur DamageDealt, masquées au KO) ; **4d-2 ✅ tweens d'impact** (knockback = Hurt + glide, glissade glace = glide, knockback bloqué = shake ; teleport/Hit&Run = snap, déjà parité Phaser). **4d-3 ✅ flow confirmer-attaque + flash de prévisualisation** (parité : Phaser `confirmAttack:true` par défaut ; `BATTLE_CONFIRM_ATTACK` on ; pulse émissif sur cibles verrouillées, éteint à toutes les sorties confirm). **4d-4 ✅ preview dégâts numérique** (bandes min-max sur barre PV + nombre en phase confirm via `engine.estimateDamage`, setting `damagePreview`). **4d-5 ✅ nuance movement-animation** (Hop/glide/rampe par pas, port de `animateAlongPath` ; slopeData exposé ; dash ne force plus le Hop ; volant plane) ; **4d-6 ✅ wobble confusion** (best-practices : pivot billboard + roll `rotation.z` sur le plan enfant ; state-based depuis volatileStatuses ; stoppe au KO). **4d-7 ✅ Sandbox Studio** (`mountSandboxStudio` : chrome éditeur `SandboxPanel` DOM + cycle combat, tear-down/re-mount par changement de config façon `resetSandbox` ; `initSandboxStudioDom(host?)` généralisé Phaser `#game-container` / Babylon `#game-root` ; CSS `body[data-sandbox] #game-root` fixed→flex ; ancien `mountSandboxCombat` supprimé). **Jalon 4d TERMINÉ — parité Babylon ↔ Phaser atteinte (combat + Sandbox Studio) → ISO-PHASER.** Plan 123. Suite : **phase test/retours humains**, puis **J5** (suppression Phaser, `grep -ri phaser packages/` = 0, bundle, merge `--ff-only`). Jalon 4a TERMINÉ (plan 120). Run autonome jusqu'à iso-Phaser : 4b→4c→4d) — **Phase retours en cours** : port HUD monde DOM→moteur ✅ (commit 9d1f731, décision #487 appliquée) : `babylon-sprite-overlays.ts` + `sprite-overlay.css` + `floating-text.css` supprimés → `babylon-sprite-hud.ts` (barre PV DynamicTexture roundRect NEAREST, icône statut, preview dégâts), `babylon-text-plane.ts`, `babylon-champ-pill.ts` (pastille Champ billboardée). Textes flottants et pastilles Champ portés en moteur. `BATTLE_TEXT_DURATION_MS` 3500→1000, `BATTLE_TEXT_QUEUE_DELAY_FACTOR` 0.5 (nouveau), `BABYLON_FLOATING_TEXT_LIFT` 1.5→1.0. Centrage caméra sur milieu map. Police `body` globale. **Phase recette (suite)** : saut de falaise/chute ✅ — easing vertical asymétrique par axe (`jumpVerticalProgress` + `BABYLON_JUMP_VERTICAL_LEAD = 0.45`) : le sprite monte/chute dans la moitié sûre du pas pour franchir le bord sans clip (la silhouette X-ray révélait la pénétration que Phaser 2D cachait) ; `impactGlide` (knockback off-cliff) utilise le même arc ; `BABYLON_MOVE_JUMP_ARC` supprimée. Hauteur curseur sur décorations ✅ — `decorationHeightAt` expose la hauteur art rendu (worldHeight − FOOT_DROP) ; `surfaceHeightAt` (terrain + déco) utilisé par tileWorldTop, highlights et mouvement → curseur posé sur le dessus de la déco, volants montent dessus. Self/AoE confirmables partout ✅ — `resolveTargetAction` (battle-orchestrator) : patterns statiques (Self/Cross/Zone) centrés sur le lanceur confirment sur tout clic (parité Phaser `resolveAttackAction`). Auras (Murs) in-engine ✅ — icônes à gauche de la barre de vie (`babylon-sprite-hud.ts setLeftIndicators`, triées par tours restants) + icônes au sol au survol du lanceur (`babylon-aura-ground-icons.ts` NOUVEAU — pivot billboardé par tuile, layout croix, groupe sprite pour occlusion par les Pokemon, masquées sur tuiles occupées) ; câblage orchestrator `refreshAuraVisuals`/`showAuraHoverFor` ; constantes `BABYLON_HUD_AURA_*` et `BABYLON_AURA_HOVER_*`. Champs dédup + pastille centrée ✅ — `refreshFieldTerrainVisuals` peint zones depuis l'état moteur ; dédup overlap (chaque tuile peinte par le champ le plus récent, bordures recalculées, zones non superposées) ; pastille `babylon-champ-pill.ts` centrée via métriques texte ; groupe sprite pour occlusion ; lift 0.6 → 0.25. **Phase recette (lot 2, 2026-06-12)** — 15 corrections de parité (15 fichiers, +274/-114) : move-destination fill seul (plus d'outline rouge) ; occlusion herbes hautes (groupe terrain → sprite, `BABYLON_GRASS_ALPHA_INDEX` supprimée, **#496**) ; enemy-range au survol (`BoardHighlight "enemy"`) ; couleurs preview au sol par type (plus de flood rouge ; outline cibles seules ; footprint buff bleu/attack rouge ; confirm garde la couleur) ; directionnels Cône/Ligne/Slash = clic n'importe où ; Dash exclu de `isDirectionalPattern` (traînée + confirm tile cliquée, **#498**) ; bordure portée épaissie (`CreateGreasedLine`, `BABYLON_TILE_RANGE_OUTLINE_WIDTH`) ; crash FSM "Retour au menu" sandbox corrigé (`manager.start` + `teardownSandboxStudioDom`, **#500**) ; dummy sandbox accède à tous les moves (`allMoves`) ; raccourcis debug retirés (z/x/c/…/i) ; attaques 2-temps portées (⚡ barre HP, texte "charge" `MoveCharging`, preview lanceur, `isChargeT1` confirm anywhere) ; anim dégâts dash/mur/champ (`WallImpactDealt`/`TerrainDamageDealt` flash+HP) ; occlusion pendant attaque (`setAttacking` biais `BABYLON_ATTACK_DEPTH_BIAS = 0.012` ; coplanaire OK, plus haut occlude, **#499**) ; perspective tuiles (caméra 35.26° conservée, hauteur aplatie `BABYLON_TILE_HEIGHT_SCALE = 0.866`, diamant inchangé, **#497**) ; slot Team Builder vide = hauteur slot rempli (CSS). Décisions #496-500.
> Dernière release : **v2026.6.4** (2026-06-22) — Roster Gen 1 complet (150 Pokemon), 50 objets tenus (42→92), 42 talents Gen 1 (52→95), pièges au sol (Picots/Pièges de Roc/Pics Toxik/Toile Gluante), Distorsion, moves delayed/countdown (Prescience/Requiem/Balance/Effort/Coup d'Main), moves contrôle (Possessif/Dissonance Psy/Dépit), Dash directionnel + Roulade snowball, sprites en bundle 3 fichiers + chemins relatifs (fix itch.io/Pages). Plans 130→138.
> v2026.6.3 (2026-06-16) — Migration renderer Babylon.js (rotation caméra ←/→, vision X-ray à travers le relief, jeu plein écran). Renderer Phaser retiré définitivement.
> v2026.6.2 (2026-06-12) — Système Champs (Herbeux/Électrifié/Brumeux/Psychique), 4 moves poseurs + 6 dépendants du Champ, 11 moves soin (rôle support), 17 moves dégâts conditionnels, objet Champ'Duit, plans 115→118
> v2026.6.1 (2026-06-05) — ~181 nouveaux moves, Clonage/Provoc/Encore/Entrave, plans 097→114
> Worktree `phase5-babylon` rebasé sur main (2026-06-09) : main a Champs/B4 (plans 117-118, 393 moves). Plan-maître Phase 5 renommé plan 119. Décisions Phase 5 renumérotées #449-481 (3d ajoutées).
> Point d'entrée pour reprendre projet après pause.
> Dire "on en était où ?" → Claude Code lit ce fichier.

---

## Phase 5 — Migration renderer Babylon.js 2D-HD — **TERMINÉE / livrée en v2026.6.3 (2026-06-16)**

**MAJ 2026-06-11** : Phase 5 repriorisée avant Phase 7 (décision humaine). Worktree dédié `phase5-babylon` (`.worktrees/phase5-babylon/`, port Vite 5220). Plan-maître : **plan 119**. **Jalon 3 terminé** (1 + 2a-c + 3a→3f, 3d partiel). 3f : 8 maps OK (dont le-mur multi-niveaux), picking + occlusion + Champs sur relief validés, **gate 60fps PASS** (60 fps verrouillé, pire cas rotation+silhouettes 0 frame >20 ms). **Gate parité Phaser reporté** : comparaison quand combat complet fonctionnel (J4+, décision humaine). **Jalon 3.5 pixel-art ABANDONNÉ (2026-06-10)** : 4 approches testées et rejetées le même jour (pipeline plein-écran, liseré tileset, liserés géométriques, spike crénelage contrôlé) — verdict humain « ça rend déjà super bien comme c'était », rendu full-res conservé, zéro code conservé. Détails : décision #486 + `docs/babylon/babylon-pixel-art-pipeline.md`. **Jalon 4a EN COURS (plan 120)** : FSM écrans DOM + menus — **étapes 1-6 livrées**. Étapes 1-3 : ScreenManager 9 états + 9 tests Vitest, écrans DOM réels main-menu/battle-mode/settings/credits, placeholders team-select/my-teams/team-edit, combat-screen wrappant démo J3, map-select avec 8 cartes + preview Babylon live, navigation complète testée. Étape 4 : écran DOM `team-select` minimal (FormatPicker/PlayersColumn/TeamList, toggle Humain/IA, CT/RR, Lancer → combat) ; logique slots extraite dans `slot-state.ts` (partagée Phaser ↔ DOM, TeamSelectScene −100 lignes) ; fix scrollbars fines globales + flex-wrap portraits. Étape 5 : `MyTeamsView`+`TeamEditView` extraites en `ui/team/` (vues DOM partagées Phaser ↔ FSM, scènes Phaser −560 lignes). **Étape 6 (2026-06-11)** : phase de placement jouable — `babylon/placement-flow.ts` pilote le core `PlacementPhase` (mode Alternating) ; tours alternés humain/IA (IA auto-place) ; **picker direction en moteur** (`babylon-direction-picker.ts` — 4 quads sol texturés `arrows.png`, meshes de scène, suit caméra/zoom/resize gratuitement, décision #487) ; undo Échap ; option Placement auto (`autoPlaceAll`) ; bandeau bas `placement-roster.ts` (portraits, compteur placés/max, Terminer, couleur équipe) ; highlights zones de spawn (`setSpawnZones` `babylon-tile-highlights.ts`) ; constantes `TILE_SPAWN_ZONE_INACTIVE_ALPHA`/`OCCUPIED_ALPHA` + `BABYLON_DIRECTION_ARROW_*` (×4) ; i18n `placement.counter` FR/EN ; fix bug Dardargnan (alias `<CopyOf>` → PNG manquant → atlas sans Idle → bruit) via résolution PNG source dans `extract-sprites.ts`. Composant DOM `direction-picker.ts` + CSS `.pl-direction-*` + i18n `placement.direction.*` supprimés. Validé humain (2 montages combat d'affilée sans fuite). **Étape 7 suivante** : boucle combat (`battle-orchestrator`, sous-étapes 7a–7d). Tints terrain, texte chute et barrière Champ Psychique reportés (polish / J4). Idée humaine en exploration : décorations en assets voxel (plus tard).

**Décisions verrouillées :**
- Babylon.js 8.x retenu (décision #269, confirmé). Worktree unique long-lived — **pas de merge partiel sur main**, merge `--ff-only` seulement à parité atteinte (décisions #449-450).
- Tiled conservé (`loadTiledMap` → `MapDefinition` inchangé). UI = **HTML/CSS overlay** (pas `@babylonjs/gui`) — compat navigateur clavier/souris/tactile + mobile (décisions #451-452).
- Code dans `packages/renderer/src/babylon/`. Résolution design de référence **1920×1080**. Map de référence parité = **volcano** (décisions #453-454).
- **Échelle sprite = 24px/u (= densité tile, pixel parity ≈1:1)** : le "0.5×" intermédiaire était un artefact d'un `baseScale=2` fantôme (corrigé). Doc : `docs/babylon/babylon-2d-overlay-scaling.md` (décision #455).
- **Hauteurs tiles** : full = 1 (cube unité), half = 0.5, ratio **2:1** (décision #456).
- **Orientation grille→monde transposée** : gridX→worldZ, gridY→worldX (décision #457).
- **Terrain visuel ≠ terrain gameplay** : `parseTiledMap` estampille `obstacle`+hauteur sous décorations ; renderer texture le visuel brut (GID/rangée → `VisualTerrainGroup`) via `loadTiledMap().visualTiles`. 15 textures plates PMD dans `public/assets/tilesets/terrain-3d/` (décision #458).
- **Ombres** = par `shadowSize` PMD (offsets.json : 0.14/0.2/0.3) (décision #459).
- **Sprites en `renderingGroupId 0`** depuis Jalon 3a (partagé avec le terrain, occlusion native depth-buffer via `SpriteDepthPlugin`) — décision #473 révise #460.
- **Jalon 3.5 pipeline pixel-art ABANDONNÉ** (décision #486 révise #461) — 4 essais rejetés 2026-06-10, rendu full-res conservé. Historique : `docs/babylon/babylon-pixel-art-pipeline.md`.

**Intact** : core, data, IA, LoS 3D, CT, statuts, format Tiled, i18n, tokens CSS.

---

## Jalon 1 (plan 119) — DONE 2026-06-08 (+ fix ancrage post-Jalon 1)

- Spike Babylon 063 porté en production dans `packages/renderer/src/babylon/`.
- Terrain volcano rendu (extrusion 3D depuis `MapDefinition`). 15 textures tiles PMD copiées dans `public/assets/tilesets/terrain-3d/`.
- 1 sprite PMDCollab billboard directionnel animé opérationnel.
- Caméra orthographique dimetric + curseur tile.
- Paramètres figés : orientation grille→monde transposée, hauteurs 2:1 (full=1/half=0.5), échelle sprite 24px/u (1:1), terrain visuel via `VisualTerrainGroup`, ombres par `shadowSize` PMD.
- Sprites `renderingGroupId 1` (au-dessus terrain).
- Docs de référence créées : `babylon-2d-overlay-scaling.md`, `babylon-asset-lifecycle.md`, `babylon-pixel-art-pipeline.md`.
- Plan parité complet : `docs/plans/119-parity-checklist.md` (307 items).

**Fix ancrage sprite (livré après Jalon 1) :**
- Cause racine : `footAnchor = 0.34` était une constante inventée non branchée sur les données PMD. Première tentative de correction (lift 1 unité) aggravait le flottement.
- Resolution : `directional-billboard.ts` lit `footOffsetY` depuis `offsets.json` (`BABYLON_SPRITE_GROUND_OFFSET_PX = 5` fallback). `footOffsetY = 4` px constant sur tous les sprites (pixel BLANC PMD = `centerY + 4`). Ancre HUD = `headOffsetY` + `BABYLON_HUD_ANCHOR_MARGIN_PX = 20`. Décisions #462-463.

## Jalon 2a — Fondation overlay — DONE 2026-06-08

- `game-stage.ts` : `mountGameStage(root)`, letterbox CSS `aspect-ratio`/`min()`, `ResizeObserver` → `--ui-scale` (référence 1920×1080). Décision #465.
- `world-projection.ts` : `projectAnchors()`, viewport CSS (pas backing-store), batch read→write, `Math.round` pixel-snap, visibilité z∈[0,1]. Décision #464.
- `game-overlay.css` : layers CSS, `pointer-events`, `contain:layout`, `will-change:transform`. Tokens `--blue-1000` + `--color-letterbox`.
- Demo HP bars (1 par sprite, ancrée tête) : valide projection + 60fps N éléments DOM. Check bloquant §4 Jalon 2 validé. 0 erreur console.
- Grille debug touche `g` (`createTileGrid`, wireframe arêtes tiles). Décision #466.

## Jalon 2b — Démonstrateur chrome InfoPanel DOM — DONE 2026-06-08

- `packages/renderer/src/ui/dom/info-panel.ts` : composant DOM InfoPanel, view-model `InfoPanelData` découplé du core. Sémantique HTML (progressbar aria, ul/li badges, textContent). Décision #469.
- `packages/renderer/src/styles/info-panel.css` : scaling cat. B via container-query units (`--ip-px = calc(100cqw / 1920)`), résolu contre `#game-stage` (`container-type: size`). Reflow mobile `@container stage (width < 768px)`. Badges flex-wrap, panneau ancré bas. Décision #467.
- `tokens.css` enrichi : tokens équipe `--team-1`..`--team-12`, `--color-badge-{buff,debuff,volatile}-bg`, `--color-border-faint`. `@font-face` PokemonEmeraldPro déplacé ici (babylon.html ne tombait plus en monospace).
- **Rename** : `.ui-chrome` → `.ui-screen` / `chromeLayer` → `screenLayer` — paire claire `.ui-world` / `.ui-screen`. Décision #468.
- Wire démo dans `babylon-preview.ts` (InfoPanel Pikachu team 1). Validé : desktop + mobile reflow + 10 badges wrap. 0 erreur console.

**Jalon 2c DONE (2026-06-08) — Team Builder sous contrat + pivot full-canvas :**
- **Pivot majeur : abandon du letterbox 16:9** (retour humain — pas tous les écrans sont 16:9, surtout mobile portrait). `#game-stage` remplit 100% du viewport, la caméra ortho dimetric comble selon le ratio (zéro bande noire). L'UI DOM utilise tout l'écran, tout ratio. Option `aspectRatio` retirée de `game-stage.ts`. Révise les décisions #464-468 (le contrat cqw reste valide, mais résolu contre le viewport plein). Décision #472.
- **Team Builder rapatrié** (placeholder, rien câblé) : `team-edit-harness.ts` réutilise les sous-composants prod (`SlotCardsRow`/`EditLeftPanel`/`EditRightPanel`) + `generateRandomTeam`. `team-builder-overlay.css` = adapter prod-safe (override tokens cqw scopé `@container stage`, raw px wrappés `var(--tb-*, <px>)`, reflow mobile <768px). Toggle `t` dans le harness. Décisions #470-471.
- Best-practices (agent) : approche cqw validée marché 2026. 4 pistes différées (plancher font-size 480-767px, `--stage-scale` pour modales top-layer, cap ultrawide, `--ui-scale` barres PV 4K) → voir `docs/next.md`.
- Validé visuellement : wide + portrait + reflow mobile + non-régression prod (fallback px hors stage). Gate formel 60fps : à formaliser au Jalon 3.

**Jalon 3b DONE (2026-06-09) — Picking ray-cast multi-niveaux + surbrillances de portée :**
- **`babylon-picking.ts`** (NOUVEAU) : `pickTile(scene, x, y)` ray-cast → coordonnée grille via `metadata.tile`. En 3D ortho la surface frontale visible est retournée automatiquement ("colonne la plus haute gagne" gratuit). Pas de désambiguïsation Alt — rotation caméra remplace (décision #477).
- **`babylon-tile-highlights.ts`** (NOUVEAU) : surbrillances de tiles (move bleu / attack rouge / retreat cyan / enemy orange) + contour de portée (arêtes externes) + curseur de survol. Fills flush sur face top via `material.zOffset` (`BABYLON_TILE_HIGHLIGHT_Z_OFFSET = -2`), pas de lift world-Y (décision #478). Contour = lignes avec micro-lift Y (`BABYLON_TILE_OUTLINE_Y_OFFSET = 0.02`). Bornage à la grille.
- **`terrain-extruder.ts`** : `isPickable = true` + `metadata = { tile: {x,y} }` sur les tuiles.
- **`combat-scene.ts`** : hover + clic (distinct du pan via seuil `BABYLON_PICK_DRAG_THRESHOLD_PX = 5`), API `setTileHighlights / setTileOutline / clearHighlights / onTileHover / onTileClick`.
- Démo `babylon-boot.ts` : clic → portée Manhattan r3 + contour + anneau attaque (wiring jetable, réel en J4).
- Nouvelles constantes `babylon-constants.ts` : `BABYLON_TILE_HIGHLIGHT_Z_OFFSET`, `BABYLON_TILE_OUTLINE_Y_OFFSET`, `BABYLON_TILE_HIGHLIGHT_ALPHA`, `BABYLON_TILE_CURSOR_ALPHA`, `BABYLON_PICK_DRAG_THRESHOLD_PX`.

**Jalon 3a DONE (2026-06-09) — Caméra combat + occlusion native + silhouette X-ray + extrusion multi-niveaux :**
- **`combat-scene.ts`** (NOUVEAU) : scène combat prod. Caméra dimetric orthographique rotative ←/→ par snaps 90°, zoom molette, pan clic-glisser. Harness dev `babylon-preview` reste accessible via `?preview=1`.
- **Occlusion native depth-buffer** (`SpriteDepthPlugin`, `MaterialPluginBase`) : terrain + sprites en `renderingGroupId 0`. Le plugin écrit `gl_FragDepth` = profondeur des pieds du sprite (CPU en ortho), biais `BABYLON_SPRITE_DEPTH_BIAS = 0.0025`. Résout l'auto-occultation (le billboard n'enterre plus son bas dans son propre cube de tile). Technique bgolus HD-2D. Décision #473.
- **Silhouette X-ray** : 2e plane couleur d'équipe, `depthFunction=GREATER`, `renderingGroupId 1`, depth buffer préservé (`setRenderingAutoClearDepthStencil(1, false)`). Unités lisibles derrière relief. `BABYLON_SILHOUETTE_ALPHA = 1`. Décision #474.
- **Extrusion terrain multi-niveaux** : `load-tiled-map.ts` empile désormais les `elevationLayers` (`terrain_2`/`terrain_4`/…). Les reliefs multi-couches (ex: desert) s'extrudent correctement.
- **Constantes centralisées** dans `babylon-constants.ts` : `BABYLON_VIEW_SIZE`, `BABYLON_CAMERA_*`, `BABYLON_DIMETRIC_ELEVATION`, `BABYLON_AZIMUTH_STEP`, `BABYLON_ROTATION_LERP`, `BABYLON_SPRITE_PIXELS_PER_UNIT`, `BABYLON_SPRITE_DEPTH_BIAS`, `BABYLON_SILHOUETTE_ALPHA`.

**Jalon 3c DONE (2026-06-09) — Curseur FFTA billboard 2D (curseur seul ; previews de ciblage → J4b) :**
- **`babylon-hover-cursor.ts`** (NOUVEAU) : curseur FFTA de sélection = billboard 2D `BILLBOARDMODE_ALL` (facing caméra) flottant au-dessus de chaque tuile survolée. Levé à la hauteur de la tête si un Pokémon occupe la tuile (`spriteTopOffsetY`). 4 variantes cyclables touche H, persistées localStorage. Rendering group dédié 2 (au-dessus terrain 0 / silhouettes 1) — toujours lisible. Décision #479.
- **`combat-scene.ts`** : wiring hover (curseur suit la tuile, lift tête si occupant), touche H cycle, lookup tuile→billboard.
- **`babylon-constants.ts`** : `BABYLON_HOVER_CURSOR_GAP = 0.35`, `BABYLON_HOVER_CURSOR_RENDERING_GROUP = 2`.
- **Previews de ciblage repoussées en Jalon 4b** : cône/ligne/slash/dash/blast/self-radius/spawn-zones/flash sprites/auras dépendent du move sélectionné (FSM → J4a).

**Jalon 3d PARTIEL (2026-06-09) — Animations directionnelles + états sprite intrinsèques :**
- **`directional-billboard.ts`** : système animation complet — `LOOPING_ANIMATIONS` (Idle/Walk/Sleep/FlapAround/Hover/Special0/Special10/FlyingIdle) ; `setAnimation` (looping, pas de reset même clé) ; `playOnce` (one-shot → resting ou freeze Faint/KO) ; `setRestingAnimation` ; `playFirstAvailable` (chaîne glide) ; `advanceFrame` (wrap vs clamp).
- **Synthèse FlyingIdle** : `synthesizeFlyingIdle` extrait frames 0-1 de FlapAround — portage fidèle SpriteLoader 2D. Décision #480.
- **Chaîne glide volants** : `FLYING_GLIDE_CANDIDATES = [FlyingIdle, Hover, Special0, Special10]`, fallback **Walk** (décision #480 — annule feedback playtest 2026-04 "Hop").
- **États sprite** : `setActive` (pulse respiration `BABYLON_PULSE_PERIOD_MS = 900`) ; `flashDamage` (emissive `BABYLON_DAMAGE_FLASH_DIM_EMISSIVE = 0.25`) ; `setKnockedOut` (teinte sombre + freeze) ; `setSemiInvulnerable` (vol = lift `BABYLON_SEMI_INVULNERABLE_LIFT = 1.5` tiles + glide / creuse = caché ombre gardée). `SemiInvulnerableDisplay` renderer distinct du core enum (décision #481).
- **Ré-extraction sprites** : `Hover` (golbat/aerodactyl/fearow/moltres/mewtwo/beedrill), `Special0` (dragonite/golem), `FlapAround` déjà présent → ~208 fichiers régénérés. Décision #481.
- **Touches debug** `combat-scene.ts` (z/x/c/b/n/,/u/k/p/o/i) sur Pokémon survolé — à retirer au câblage moteur J4.
- **Différé (pass 3D-depth)** : wobble confusion (conflit billboard Y-lock), enveloppe profondeur d'attaque, overlay Substitut.

**Jalon 3f DONE (2026-06-10) — tests renderer + gate 60fps (clôt le Jalon 3) :**
- Sweep chrome-devtools sur la scène démo (12 sprites + 2 Champs + pastilles) : **8 maps OK** (desert, volcano, cramped-cave, forest, swamp, toundra, naval-arena, le-mur), zéro erreur console. `le-mur` (multi-niveaux, écartée du roster en attendant Babylon) rend correctement.
- Picking multi-niveaux validé sur le-mur (plateau haut + pied de mur). Occlusion validée (rotation 90° → silhouettes équipe derrière falaise). Champs sur relief validés (falaise, eau, lave) — pastilles DOM suivent la rotation.
- **Gate 60fps PASS** : desert 59.9 fps (p95 18.0 ms), le-mur 60.3 fps, pire cas rotation caméra + silhouettes p95 17.9 ms / 0 frame >20 ms.
- **Gate parité Phaser reporté** (décision humaine) : comparaison côte-à-côte quand le combat complet sera fonctionnel (J4+) — le visuel est déjà jugé au fil de l'eau, et J3.5 changera le look.
- Idée humaine en exploration : remplacer les décorations par des assets **voxel** — **réalisé (2026-07-21)**, voir décision #690.

**Jalon 3.5 ABANDONNÉ (2026-06-10) — pixel-art / crénelage des bords (décision #486) :**
- Besoin réel clarifié en séance : « contrôler le crénelage des tiles » (gros pixels sur les bords des blocs, textures/sprites intacts).
- 4 approches implémentées, testées visuellement et rejetées le même jour : pipeline plein-écran RTT+NEAREST+zoom paliers (resample tout), liseré baké tileset + curseur/outline quads texel (grille partout ≠ contours), liserés géométriques sur cassures de relief (trait ajouté ≠ crénelage), spike `edge-pixelate` profondeur basse réso + mosaïque N×N (le plus proche, mais arêtes internes non couvertes sans normales).
- Verdict humain final : « c'est un détail, ça rend déjà super bien comme c'était ». Rendu full-res conservé, tout le code des essais retiré (zéro code mort).
- 2 recherches best-practices menées (pipeline HD-2D, contours pixel 3D) — sources + enseignements + piste de reprise dans `docs/babylon/babylon-pixel-art-pipeline.md`. **Ne pas rouvrir sans décision humaine explicite.**
- Feedback process acté en mémoire : présenter le résultat visuel attendu AVANT d'implémenter un changement de rendu.

**Jalon 4a TERMINÉ** (plan 120 — étapes 1-10 ✅). Gate minimal validé humain (A combat+Rejouer,
E sandbox en direct ; B/C/D+disposal rapides). Repasse gate complète à l'iso-Phaser (4b/4c).
Route `?map=` preview retirée (non nécessaire). Suite : **Jalon 4b** (chrome combat complet).

- Étape 10 (2026-06-12) — gate. Fix au passage : `babylon/load-tiled-map.ts` skippe
  `requireAllFormats` sur maps dev (mirror loader Phaser) → sandbox-flat se charge ;
  gap ajouté entre boutons Rejouer/Retour menu de l'overlay victoire.

- Étape 9 (2026-06-12) — routes boot (`babylon-boot.ts`) : **sandbox** (`VITE_SANDBOX`,
  `pnpm dev:sandbox '{...}'` sur `/babylon.html`) → `mountSandboxCombat` : combat
  player-vs-dummy direct sans placement, engine `createSandboxBattle(config, map)` (map =
  tmj chargé = source du rendu → positions alignées), billboards spawnés depuis l'état,
  `DummyAiController` si `dummyControl: "ai"`. Cœur board/chrome/orchestrateur factorisé
  (`runBattle`) entre chemin placement et sandbox. `onExit` injecté (FSM dispose vs teardown
  sandbox + nav menu). (Route `?map=` preview retirée à l'étape 10 — non nécessaire.)

- Étape 8 (2026-06-12) — fin de combat : overlay victoire DOM gagne le bouton
  **« Rejouer »** (re-mount interne de l'état combat, même config — `mountContent`/
  `teardown` dans `combat-screen.ts`, PAS une transition FSM) à côté de
  « Retour menu ». `AbortController` recréé à chaque (re)montage → disposal propre
  des listeners clavier entre deux combats. Vérif fuites (2 combats) = au gate 10.

- Étapes 7c/7d (2026-06-12) — couvertes par l'impl générique 7b. **7c** : IA EASY
  déjà câblée (`onTurnReady` → `AiTeamController` + `EASY_PROFILE`) ; `DummyAiController`
  (immobile) reste un construct sandbox → branché au chemin Babylon à l'étape 9.
  **7d** : moteur pilote `turnOrder`/`currentTurnIndex` pour RR **et** CT, l'orchestrateur
  lit `state` sans logique spécifique → les deux systèmes marchent ; startup events
  drainés (`consumeStartupEvents`). TurnTimeline CT différée à 4b (note du plan).

- Étape 7b (2026-06-11) — `BattleOrchestrator` (port découplé du `GameController`
  Phaser, zéro import moteur) : FSM 9 phases combat, `AnimationQueue` réutilisée,
  3 ports réels (`BoardView` Babylon, `BattleChrome` DOM minimal, `BattleFeedback`
  no-op + console.debug). Picker direction voxel, IA EASY, Hit&Run, Échap contextuel,
  overrides team-builder, dialogue victoire. 7 tests FSM. Boucle jouable bout-en-bout
  en minimal (snap, faint sur place, re-sync board depuis l'état moteur).

- Étape 7a (2026-06-11) — audit `GameController.ts` (2798 lignes) : 9 phases combat
  cartographiées (transitions, Échap contextuel, Hit&Run, `onTurnReady`, startup).
  3 ports figés dans le plan : `BoardView` (moteur, ancré-monde), `BattleChrome`
  (DOM, ancré-écran), `BattleFeedback` (textes flottants moteur 4c + log DOM) +
  API `BattleOrchestrator` (séquenceur pur). Écarts #487 tranchés : picker direction
  = voxel in-engine (DOM `direction-picker.ts` abandonné), textes flottants → moteur.

- Étape 5 (2026-06-10) — `my-teams` / `team-edit` réels : `MyTeamsView` + `TeamEditView`
  (`ui/team/`) extraites des scènes Phaser (vues DOM partagées Phaser ↔ FSM jusqu'à J5).
  Scènes Phaser réduites à wrappers ~25 lignes (−560 lignes nettes). Écrans FSM
  `my-teams-screen.ts` / `team-edit-screen.ts`; `team-edit` avec `teamId: null` crée
  une équipe vide avant édition. `placeholder-screens.ts` supprimé ; `babylon-boot.ts`
  branché sur les écrans réels. `bindEscape` guard `dialog[open]` — Échap ferme les
  modales sans déclencher la navigation retour.

- Étape 6 (2026-06-11) — écran combat : placement. `babylon/placement-flow.ts` drive
  le core `PlacementPhase` (Alternating) : tours alternés humain/IA (IA auto-place),
  pose interactive (clic zone spawn → picker direction iso → `submitPlacement`), undo
  Échap (dernière pose du joueur courant), Placement auto (`autoPlaceAll`). À la
  complétion billboards restent, la boucle combat prend le relais (étape 7). API
  `CombatScene` étendue : `ready` Promise, `addPokemon`/`removePokemon`,
  `setSpawnZoneHighlights`. `babylon-tile-highlights.ts` : `setSpawnZones`
  (quads par zone, couleur/alpha). Composant DOM `ui/dom/combat/placement-roster.ts`
  (bandeau bas portraits, compteur placés/max, Terminer, couleur équipe).
  **Picker de direction en moteur** (`babylon/babylon-direction-picker.ts`) : 4 quads
  sol texturés `arrows.png`, posés à plat sur les tiles voisines — suit caméra/zoom/
  resize gratuitement (meshes scène). `CombatScene.showDirectionPicker()`. Composant DOM
  `direction-picker.ts` + CSS `.pl-direction-*` + i18n `placement.direction.*` supprimés
  (décision #487). Constantes `TILE_SPAWN_ZONE_INACTIVE_ALPHA`/`TILE_SPAWN_ZONE_OCCUPIED_ALPHA` +
  `BABYLON_DIRECTION_ARROW_*` (×4). i18n FR/EN `placement.counter`. Fix bug Dardargnan :
  alias `<CopyOf>Walk</CopyOf>` dans SpriteCollab → `extract-sprites.ts` résout le PNG
  source des alias CopyOf, atlas ré-extrait. Validé humain (2 montages combat d'affilée
  sans fuite).

**Jalon 3e-Champs DONE (2026-06-10) — démo statique :**
- `babylon-field-terrains.ts` (NOUVEAU) : API `createFieldTerrains → { set, update, dispose }`. `FieldTerrainSpec` miroir du spec 2D.
- Fill : quads `CreateGround` par tile, blend **alpha standard 0.3** (`BABYLON_FIELD_TERRAIN_FILL_ALPHA`). Blend additif 2D écarté (trop agressif sur textures claires). `alphaIndex = BABYLON_FIELD_TERRAIN_ALPHA_INDEX = 2`. Décision #483.
- Périmètre : `GreasedLine` insetté d'une demi-largeur (`BABYLON_FIELD_TERRAIN_OUTLINE_WIDTH = 0.04`) — anti-clip murs voisins plus hauts. Curseur survol reçoit le même inset. Décision #484.
- Pastille compteur : DOM projeté `.field-terrain-pill` dans `.ui-world`, positionné par frame via `projectAnchors` (première utilisation réelle de cette infra J2). `opacity: 0.5` (limitation DOM : passe toujours devant les sprites). Décision #485.
- `babylon-color.ts` (NOUVEAU) : `hexToColor3`/`hexToCss` partagés (extraction duplication tile-highlights/field-terrains).
- Démo statique `babylon-boot.ts` : 2 zones (Champ Herbu + Champ Électrifié). Wiring moteur = Jalon 4.
- Reportés : barrière Champ Psychique → J4 ; tints terrain → polish ; texte chute → J4.

**Jalon 3e-décorations DONE (2026-06-09) :**
- `babylon-decorations.ts` (NOUVEAU) : décorations 2D billboards `BILLBOARDMODE_Y` — rochers (`rock_1`, `rock_2x2`), arbres (`tree`) depuis Tiled `decorations` object-layer ; tall-grass auto sur tuiles `TallGrass` libres.
- Placement : centré sur footprint, bottom-anchor descendu vers sommet-avant de la tile (`BABYLON_DECORATION_FOOT_DROP = 0.4`).
- Matériau par instance (obligatoire pour `SpriteDepthPlugin` foot-depth per-billboard). Textures cachées par type.
- Rochers/arbres : ALPHATEST + depth write + `BABYLON_DECORATION_DEPTH_BIAS = 0.005` → occluent les sprites et passent devant un Pokémon de la même tile.
- Tall-grass : ALPHABLEND + `disableDepthWrite` + `alphaIndex = BABYLON_GRASS_ALPHA_INDEX = 1` → ne déclenche jamais la silhouette X-ray, dessinée après l'ombre.
- Ombre Pokémon : `alphaIndex = BABYLON_SHADOW_ALPHA_INDEX = 0` → l'herbe (alphaIndex 1) la couvre. Profondeur géométrique (pas de foot-depth → reste derrière le sprite).
- Picking : décos `isPickable = false` → le ray tape la tuile de terrain dessous (curseur/picking gratuit sur la bonne tuile).
- Validation best-practices (agent) : approche idiomatique Babylon. Convention enregistrée : tout ALPHABLEND = `alphaIndex` explicite obligatoire.
- Reportés : tints terrain → polish ; texte flottant chute → J4 overlay DOM ; curseur au sommet déco pour volant → J4.
- Décision #482.
- **Superseded (2026-07-21)** : ce Jalon décrivait des billboards 2D face caméra. Remplacés depuis par des meshes voxel `.glb` (décision #690) — voir MAJ en tête de fichier et `docs/references/voxel-tile-placement.md`. Les constantes citées ci-dessus (`BABYLON_DECORATION_FOOT_DROP`, `BABYLON_DECORATION_DEPTH_BIAS`, `BABYLON_GRASS_ALPHA_INDEX`) n'existent plus.

---

## Phase actuelle (parallèle) : Phase 4 — Gameplay Pokemon complet

### Fait en Phase 4
- **Workflow worktrees (2026-06-07)** : nouveau tooling pour lancer N sessions Claude en parallèle. Script `.claude/scripts/worktree.sh` (sous-commandes add/list/status/clean/relink/rm) + skill `/worktree`. Deps isolées par reflink-copy CoW (≈0 disk sur btrfs si lockfile identique, sinon pnpm install). Port Vite déterministe par worktree (5174-5253) via fichier `.worktree-port` + `PT_PORT` env. Merge ff-only autorisé pour Claude (hook block-forbidden-commands.sh mis à jour). Décisions #424-426.
- **B2 « Soin » (2026-06-07) — plan 116 DONE** : **11 moves** livrés (371 → **382**). Nouveaux effets : `HealTarget` (soigne la cible résolue du move — allié ou ennemi), `CureTeamStatus` (Aromathérapie/Fontaine de Vie, rayon Manhattan 2 autour du lanceur), `HealByTargetStat` (Vole-Force = Atq effective cible + Atq cible -1), `PostHealOverTime` (HoT volatile), `PostWish` (Vœu différé). Volatils HoT : Racines (1/8 PV/tour, ancrage — perd immunité Vol/Lévitation, déraciné si déplacement/knockback) + Anneau Hydro (1/16 inconditionnel). Vœu différé model-agnostic (`pendingWish` + `actionCounter`, fire au prochain tour de la cible, soin = 50% PV max du lanceur, `targetsAllyOrSelf`). Dévorêve gate cible endormie (`requiresTargetAsleep`). Boule Pollen = Blast r1 dégâts ennemis + soin alliés via `appliesIf`. Fix filtre immunité-type (moves soin/statut ne sont plus bloqués par efficacité de type). Renderer : preview zone moves alliés/soi + Self-radius, badges Racines/Anneau Hydro/Vœu, tooltips, log, i18n FR/EN. **46 tests ajoutés.** Gate CI : **2546 unit + 269 intégration + build + lint + typecheck verts**. **Vague 1 roadmap maître (plan 112) terminée** : B1 (113) + B2 (116) + B3 (115).
- **B3 « Dégâts conditionnels » (2026-06-06) — plan 115 DONE** : **17 moves** livrés (354 → **371**). Cœur = **horloge d'actions** (`actionCounter` + stamps par-mon/équipe) model-agnostic round/CT (réutilisable B7/B9/B10). Nouveaux `DynamicPowerKind` : `DamagedByEnemySinceLastAction` (Avalanche/Vendetta ×2 si touché depuis dernière action), `TargetDamagedSinceLastAction` (Assurance ×2), `EchoCrescendo` (Écho 40→200), `TeamPreviousMoveDouble` (Chant Canon ×2), `TimesHitScaled` (Poing de Colère +50/hit max 350), `AllyFaintedSinceLastAction` (Vengeance ×2), `PreviousMoveFailedDouble` (Trépignement ×2). Mécaniques spéciales : `ConditionKind.TargetBoostedRecently` pour Voix Envoûtante (confusion) et Feu Envieux (brûlure), flag `requiresAsleep` pour Ronflement, `requiresAllOtherMovesUsed` pour Dernier Recours, `teamBeatUp: true` pour Baston (multi-coup allié), volatile `Charged` pour Chargeur (×2 prochain Électrique), trois Pledges en dégâts simples (combo champ différé B4). Gate CI : **2500 unit + 269 intégration + build + lint + typecheck verts**.
- **Analytics events funnel (2026-06-05) — plan 114 DONE** : suivi GoatCounter côté renderer via endpoint pixel (`/count?p=...&e=true`) + beacon `Image` (le script `count.js` étant bloqué dans l'iframe sandboxée itch.io). Helper `packages/renderer/src/analytics/analytics.ts` : const enum `AnalyticsEvent` + `trackEvent()` + `trackGameLoadedOnce()`. No-op en dev et sur hôte inconnu. Path préfixé par plateforme (`/itch/…` vs `/ghp/…`). Phase 1 câblée (funnel parcours) : `game-loaded` (LoadingScene), `main-menu`, `battle-mode`, `team-builder` (TeamSelectScene), `map-select`, `battle-start` (BattleScene), `battle-end` (GameController sur `BattleEventType.BattleEnded`). Core non touché (zéro réseau, découplage respecté). Phase 2 (dimensions : mode/joueurs/difficulté/résultat…) documentée mais non implémentée — reportée à plus de volume.
- **B1 « Quasi-prêt » (2026-06-05) — plan 113 DONE** : premier batch concret de la roadmap maître (plan 112). **6 moves** livrés (348 → **354**) via 4 petites extensions moteur. (1) Override Déf physique — `MoveDefinition.hitsPhysicalDefense` : move spécial qui frappe la Déf physique de la cible (`damage-calculator` + `handle-damage` boost météo) → **Choc Psy** (`psyshock`, 80) + **Frappe Psy** (`psystrike`, 100, signature Mewtwo). (2) Override type-efficacité — `MoveDefinition.typeEffectivenessOverride { against, multiplier }`, `getTypeEffectiveness` 4e param routé sur les 4 call sites (calc/estimate/handle-damage ×2) → **Lyophilisation** (`freeze-dry`, ×2 sur Eau, gel 10%). (3) Multi-hit puissance croissante + précision par coup — `Effect.Damage.escalatingHitPower: number[]` + `MoveDefinition.perHitAccuracy` (re-roll précision pour hit>0, stop au 1er raté) → **Triple Axel** (20/40/60). (4) Crash-on-miss — `MoveDefinition.crashOnMiss { fraction }` : ½ PV max au lanceur si le move ne connecte pas (détection via scan events `DamageDealt` effectiveness>0 dans `BattleEngine.executeMove`, couvre raté/Protection/immunité) → **Pied Voltige** (`high-jump-kick`, 130) + **Talon-Marteau** (`axe-kick`, 120, confusion 30%). 4 tags MoveTooltip + i18n FR/EN. **Sandbox** : ajout contrôles stages **Précision/Esquive** (joueur + dummy) — `STAT_NAMES` étend Accuracy/Evasion (le moteur `checkAccuracy` gérait déjà), grille stat 7 colonnes + token `--spacing-xxs`. 28 tests positionnels (`{psyshock,psystrike,freeze-dry,triple-axel,high-jump-kick,axe-kick}.test.ts`). **3 moves mis de côté** (Frustration/Retour/Puissance Cachée — inutilisables Gen 8/9, watch Pokémon Champions — décision #423). Gate CI : **2430 unit + 269 intégration + build + lint + typecheck verts**.
- **Moves liés au poids (2026-06-05) — plan 111 DONE** : nouveau champ `weight: number` (kg) sur `PokemonInstance`, propagé depuis `PokemonDefinition.weight` à tous les sites de construction d'instance. 2 nouveaux `DynamicPowerKind` dans le core : `TargetWeight` (puissance selon poids cible, 6 paliers parité Showdown) et `WeightRatio` (puissance selon ratio `userWeight >= targetWeight × N`, 7 cas, palier ×3 inclusif → 80), resolvers dans `dynamic-power-system.ts`. **4 moves** livrés via `tacticalOverrides` (344 → **348**) : **Balayage** (`low-kick`, Combat, Phys, Single 1-1, poids cible), **Nœud Herbe** (`grass-knot`, Plante, Spé, Single 1-1, poids cible), **Tacle Lourd** (`heavy-slam`, Acier, Phys, Single 1-1, ratio poids), **Tacle Feu** (`heat-crash`, Feu, Phys, Single 1-1, ratio poids). Renderer/IA : zéro code nouveau (tag MoveTooltip « Puissance variable » auto ; IA route déjà `dynamicPower`). Tests : `dynamic-power-system.test.ts` (TargetWeight 6 paliers + WeightRatio 7 cas) + 4 tests positionnels. Compteur intégration 344 → 348. Gate CI : **2402 unit + 269 intégration + build + lint + typecheck verts**.
- **Moves stat-source (2026-06-04) — plan 110 DONE** : override de la stat offensive dans le calcul de dégâts. Nouvel enum `AttackStatSource` (`UserDefense`/`TargetAttack`) + champ `MoveDefinition.attackStatSource`, lu dans `calculateDamageWithCrit` via helper `resolveAttackStat(move, attacker, defender, isPhysical)` (3 cas : défaut Atk/Atq.Spé lanceur, Déf lanceur, Atq cible). `estimateDamage` (IA + preview) route automatiquement la bonne stat — aucun code IA dédié (audit : zéro lecture stat brute dans `ai/`). **2 moves** (342 → **344**) : **Bodypress** (`body-press`, Combat, Phys, Single 1-1, utilise la Défense du lanceur + crans Déf) et **Tricherie** (`foul-play`, Ténèbres, Phys, Single 1-1, utilise l'Attaque de la cible + crans Atq cible). Crit normal (reference `critRatio:1` = baseline non propagé par `load-moves.ts`). Burn du lanceur s'applique aux deux (physiques). **Bagarre (`guts`) corrigé** : `onDamageModify` ne booste plus les moves à `attackStatSource` défini (l'Atk du lanceur n'est pas utilisée). Fix i18n FR `Bodypress` (reference donnait "Big Splash" → backlog). MoveTooltip tags FR/EN `statSourceDefense`/`statSourceTargetAttack`. Tests : `body-press.test.ts` + `foul-play.test.ts` (positionnels + scaling stat + crans + Clonage absorbe Tricherie) + 3 unit `damage-calculator.test.ts`. Gate CI : **2376 unit + 269 intégration + build + lint + typecheck verts**.
- **Hotfix i18n moves EN/FR (2026-06-04)** : `moves.en.json` était incomplet (297 clés vs 938 en FR) — 47 moves implémentés s'affichaient en slug brut (ex: `rock-slide`, `confuse-ray`) dans l'ActionMenu en mode anglais. Fix : (1) `moves.en.json` régénéré complet (938 clés, parité FR) depuis `reference/moves.json` — zéro valeur existante modifiée ; (2) typo clé `vice-grip` → `vise-grip` dans `moves.fr.json` (seul move slug FR) ; (3) `MovePickerModal.ts` : `entry.info?.name ?? entry.id` remplacé par `getMoveName()` (les moves non-implémentés du picker affichaient aussi le slug). Dette technique : `moves.{en,fr}.json` maintenus à la main — générer automatiquement depuis `reference/moves.json` dans le script `data:update` pour éviter la récurrence. Ajouté dans `docs/backlog.md` (dette technique).
- **Power conditionnel (2026-06-03) — plan 109 DONE** : moteur générique `dynamicPower` dans le core. Registry de resolvers `packages/core/src/battle/dynamic-power-system.ts` (`resolveDynamicPower`, `getEffectivePowerFloor`), enum `DynamicPowerKind`, type `DynamicPowerSpec`, champs `MoveDefinition.dynamicPower` + `MoveDefinition.ignoresBurnAttackDrop`. Branché dans `handle-damage` (dealSingleHit, après resolveWeatherBallMove) et `estimateDamage` (IA + preview). `getEffectivePowerFloor` remplace les `move.power` bruts dans action-scorer/aggressive-ai/threat-detection (moves à power null=0 ne sont plus exclus du scoring). Exemption burn pour Façade. **12 moves livrés** (330 → **342**) : Façade (×2 si statut self + ignore baisse Atk brûlure), Châtiment (×2 si cible a statut), Choc Venin (×2 si cible empoisonnée), Acrobatie (×2 sans objet tenu), Force Ajoutée (20 +20/cran positif), Boule Élek (ratio vitesse), Gyroballe (ratio vitesse inverse +1), Gigotage + Contre (HP% bas), Saumure (×2 si cible ≤50% HP), Pression Extrême (BP selon HP% cible), Giclédo (150×HP% soi). Tag renderer MoveTooltip « Puissance variable » + i18n FR/EN. Gate CI : **2361 unit + 269 intégration + build + lint + typecheck verts**.
- **Couverture tests moves + garde-fou CI (2026-06-02) — plan 108 DONE** : rattrapage dette tests positionnels des batches G1–G6. **210 fichiers `packages/core/src/battle/moves/<id>.test.ts` créés** (scénarios positionnels bout en bout : touche/touche pas selon position, effet appliqué). Couverture atteinte **100% des 330 moves implémentés**. Nouveau meta-test `move-test-coverage.test.ts` : énumère `loadData().moves`, **échoue la CI** si un move implémenté n'a pas de fichier test. Strict, sans allowlist de couverture. Allowlist orphelins uniquement : `hit-and-run`, `teleport-landing`, `teleport-pattern` (mécaniques transverses non liées à un move unique). `docs/methodology.md` : règle dure « un test positionnel par move » ajoutée. Gate CI : **2288 unit + 269 intégration + build + lint + typecheck verts**.
- **Content Batch G6 (2026-06-02) — plan 107 DONE** : 11 derniers moves « simples » du roster Gen 1 (entrées `tactical.ts` + 11 noms EN dans `i18n/moves.en.json`). Total **330 moves** (319 → 330). **Tous batches G clos (G1–G6). Couverture complète moves simples roster.** Aucune mécanique nouvelle. Dégâts (9) : Force/`strength` (Single 1-1), Écrasement/`stomp` (Single 1-1, Flinch 30%), Double Baffe/`dual-chop` (Single 1-1, hits:2), Rafale Feu/`blast-burn` + Végé-Attaque/`frenzy-plant` + Hydroblast/`hydro-cannon` (Line 5, recharge), Giga Impact/`giga-impact` (Dash 3, recharge), Lame Solaire/`solar-blade` (Single 1-1, twoTurnCharge+sunSkipsCharge), Laser Météore/`meteor-beam` (Line 5, twoTurnCharge+chargeEffects AtqSpé+1 self). Statut/no-op (2) : Camaraderie/`play-nice` (Single 1-3, Atq -1 + bypassAccuracy), Trempette/`splash` (Self, no-op). 2 changements core : (1) `validate.ts` exempte `TargetingKind.Self` de la règle ≥1 effet (décision #410) ; (2) `golden-replay.test.ts` movesets figés `GOLDEN_MOVESETS` (55 actions/5 rounds — décision #411). Gate CI : **1624 unit + 269 intégration + build + lint + typecheck verts**.
- **Content Batch G5 (2026-06-02) — plan 106 DONE** : 23 moves pure stat-change + pure statut (entrées `tactical.ts` + 23 noms EN dans `i18n/moves.en.json`). Total **319 moves** (296 → 319). Aucune mécanique nouvelle. Debuffs ennemi : Grimace/scary-face, Charme/charm, Croco Larme/fake-tears, Ondes Étranges/eerie-impulse, Strido-Son/metal-sound, Regard Touchant/baby-doll-eyes, Confidence/confide (Single) ; Chatouille/tickle (Atq+Déf) ; Gros'Yeux/leer, Mimi-Queue/tail-whip, Doux Parfum/sweet-scent (Esquive -2), Sécrétion/string-shot (Cone, « tous ennemis adjacents »). Buffs : Armure/harden (self Déf +1), Rengorgement/work-up (self Atq+AtqSpé, DoubleBuff), Poliroche/rock-polish (self Vit +2, MajorBuff), **Coaching/coaching (allié Atq+Déf via `targetsAlly` — 1er buff-stat-allié générique du projet)**. Statut (MajorStatus) : Poudre Toxik/poison-powder + Para-Spore/stun-spore + Danse Folle/teeter-dance (Zone r1, parité sleep-powder), Gaz Toxik/poison-gas (Cone), Doux Baiser/sweet-kiss (Single), Vantardise/swagger (Atq+2 + Confusion 100%), Flatterie/flatter (AtqSpé+1 + Confusion 100%). Vantardise/Flatterie non bloqués Mist/Sub (boost non-négatif). **4 moves deferred** (mécanique core absente → mini-plans dédiés) : Dépit/spite (PP-reduction), Venimprégne/venom-drench (conditionnel cible empoisonnée), Influx Magnétik/magnetic-flux (gate Plus/Minus + multi-allié), Grondement/howl (buff multi-allié Gen8+). Count moves `BattleEngine.integration.test.ts` corrigé (296 → 319). Gate CI : **1624 unit + 269 intégration + build + lint + typecheck verts**.
- **Content Batch G4 (2026-06-02) — plan 105 DONE** : 36 moves dégâts (stat-drop secondaire / high-crit / recoil / drain) livrés (entrées `tactical.ts` + 36 noms EN dans `i18n/moves.en.json`). Total **296 moves** (260 → 296). Aucune mécanique nouvelle (tous patterns déjà supportés). Count moves `BattleEngine.integration.test.ts` corrigé (260 → 296).
- **Content Batch G3 (2026-06-02) — plan 104 DONE** : 24 moves dégâts + secondaire statut/flinch/confusion livrés (entrées `tactical.ts` + noms FR/EN). Total **260 moves** (236 → 260). Aucune mécanique nouvelle (statuts secondaires + Knockback déjà supportés). 3 moves sortis vers plans dédiés : Lyophilisation (super-efficace vs Eau, override type-chart), Ronflement (gate sommeil), Talon-Marteau (crash on miss). Enforcement noms FR durci : hook `.claude/hooks/french-names-reminder.sh` (UserPromptSubmit), règle dure dans CLAUDE.md + `.claude/rules/data.md` + agents.
- **Content Batch G2 (2026-06-01) — plan 103 DONE** : 23 moves dégâts spécial + multi-hit Gen 1 livrés (entrées `tactical.ts` + 23 noms EN dans `i18n/moves.en.json`, FR déjà présents). Total **236 moves** (213 → 236). Aucune mécanique nouvelle (multi-hit `hits` déjà supporté). Test count corrigé `BattleEngine.integration.test.ts` (213 → 236). 5 moves sortis du batch (psyshock/psystrike → batch « dégâts spé sur Déf physique » ; alluring-voice/burning-jealousy → batch « secondaire conditionnel si cible boostée » ; triple-axel → batch « escalade multi-hit »).
- **Content Batch G1 (2026-05-31) — plan 102 DONE** : 40 moves dégâts physiques Gen 1 livrés (entrées `tacticalOverrides` + noms EN). Total **213 moves** (173 → 213). Shapes validés case-par-case avec le directeur créatif via `move-pattern-designer`. Riders complexes différés : throat-chop (sound-lock), lash-out/temper-flare/fury-cutter (power conditionnel), ice-spinner/steel-roller (terrain), supercell-slam (crash on miss), fell-stinger (KO-boost Atk), psychic-fangs/raging-bull (screen-break), poltergeist (item-check). Bug préexistant ajouté au backlog : `tacticalOverrides.flags` écrase les flags reference (aerial-ace). Aucune mécanique nouvelle, aucun test additionnel.
- **Taunt / Provoc (2026-05-28) — plan 100 DONE** : move disruptor anti-setup livré (`taunt`, Ténèbres, statut, Single r3, acc 100, PP 20). `StatusType.Taunted` volatile 3 tours (mirror Confused/Flinch) dans l'enum + `VOLATILE_STATUSES`. Nouveau `BattleEventType.TauntBlocked` + type event. Check dans `executeUseMove` (avant dépense PP) : caster taunted + `move.category === Status` → `ActionError.InvalidAction` + emit `TauntBlocked`. `getLegalActions` filtre les moves statut si taunted (cohérence UI). Nouveau `taunt-tick-handler.ts` décrement EndTurn (priorité 350). `handleKo` cleanup volatiles déjà câblé (zéro code nouveau). Interactions canon : Substitute BLOQUE Taunt (flag `shouldSubstituteBlock` sans `bypasssub` → absorbé auto plan 099) ; Safeguard NE bloque PAS (Taunted hors liste `SAFEGUARD_BLOCKED_STATUSES`) ; Mist NE bloque PAS (pas stat decrease). Move data `taunt` dans tactical.ts (`EffectTier.Disruption`). 4 OP sets ajoutés (alakazam-taunt, gengar-taunt, aerodactyl-taunt, starmie-taunt — 169 → 173 sets). Reference Showdown a déjà taunt (FR "Provoc", flag `bypasssub` absent → sub bloque). Move count 170 → 171. Renderer : badge InfoPanel `Provoc {turns}t` (`VOLATILE_LABELS` étendu) ; MoveTooltip tag rouge `Bloqué par Provoc` sur moves statut grisés non-cliquables (if caster taunted + `move.category === Status`) ; `BattleLogFormatter` 3 nouveaux cas (StatusApplied filtré Taunted = applied, TauntBlocked = blocked, StatusRemoved filtré Taunted = expired) via `STATUS_LOG_KEY` pattern ; floating text `"Provoc!"` orange sur target à l'application. i18n FR/EN : `status.taunted`, `infoPanel.volatile.taunted`, `battle.taunt.{applied,blocked,expired}`, `moveTooltip.tag.tauntBlocked`. IA : `scoreTauntApplication` dans action-scorer — bonus ×1.8 si target a ≥40% moves statut (helper `statusMoveRatio` dans threat-detection), pénalité ×0.3 si HP < 30%, skip si déjà taunted. Gate CI : **1618 unit + 254 intégration (+8 Taunt) + lint + typecheck + build verts**.
- **Substitute / Clonage (2026-05-25) — plan 099 DONE** : move iconique Gen 1 livré (`substitute`, Normal, statut, self). Architecture pure field — `PokemonInstance.substituteHp?: number` single source of truth, pas de StatusType new. `EffectKind.PostSubstitute` + handler `handle-post-substitute.ts` : pré-check HP > 25% + pas déjà actif (sinon `SubstituteFailed` reason `insufficient_hp` ou `already_active`), coût `floor(maxHp/4)`, sub HP = `floor(maxHp/4)`, emit `SubstitutePosted`. Damage routing `handle-damage.ts` ajoute `shouldSubstituteBlock` check après calc + crit + sturdy/focus-sash (= sub actif + `attacker.id !== target.id` + `!flags.sound` + `!flags.bypasssub`) → `applySubstituteAbsorption` re-route dégâts vers `substituteHp` (Min absorbé), excess non carry-over Gen 5+. Emit `DamageDealt { absorbedBySubstitute }` + `SubstituteDamaged { remaining }` + `SubstituteBroken { breakerId, breakerMoveId }` si break. Status block `handle-status.ts` + stat decrease block `handle-stat-change.ts` après checks Mist/Safeguard → emit `StatusBlocked`/`StatChangeBlocked` reason=`Substitute` via `ProtectionReason` étendu. Sound bypass automatique : `MoveFlags.sound` + `MoveFlags.bypasssub` déjà existent + 32 moves Showdown taggés sound dans reference (5 implémentés roster : sing/supersonic/growl/roar/screech), helper `bypassesSubstitute(move)` lit flags directement. Drain heal + recoil basés sur `shared.lastDamageDealt` cumul (incluant absorbé) → canon Gen 5+ préservé. Self-bypass automatique (recoil/confusion/struggle via `attacker.id === target.id`). Weather/terrain damage hit `currentHp` direct (sub bypass canon). `handleKo` cleanup `substituteHp = undefined`. Move data : `substitute` dans tactical.ts `{ TargetingKind.Self, [{ kind: PostSubstitute }], effectTier: Reactive }`. Reference déjà a FR "Clonage" + EN "Substitute". Move count 169 → 170. **Renderer** : `PokemonSprite.setSubstituteOverlay(active)` swap effectif sprite ID via `getEffectiveSpriteId()` helper → atlas `dummy` (sprite Substitute déjà extrait PMDCollab sandbox) quand actif + replay current animation. `getAnimationKey(this.definitionId, ...)` → `getAnimationKey(this.getEffectiveSpriteId(), ...)` (replace_all). `BattleScene` init : `setSubstituteOverlay(true)` si pokemon load avec `substituteHp > 0`. `extractEngagedPokemonIds` ajoute "dummy" inconditionnel (préload obligatoire). `GameController` câblé 4 events Substitute* + KO désactive overlay. `AURA_BLOCKED_KEY` enrichi avec `ProtectionReason.Substitute`. `InfoPanel` badge volatile `Clone {hp} PV`. `MoveTooltip` ligne tag `🔊 Sonore (ignore Clone)` / `Ignore Clone` selon flags. `BattleLogFormatter` 4 nouveaux cas + différenciation `StatChangeBlocked`/`StatusBlocked` selon `event.reason`. i18n FR/EN 17 nouvelles clés. **IA** `scoreSelfMove` étend PostSubstitute : déjà sub → -1, HP ≤ 25% → 0, base `weights.statChanges × 1.5 × earlyMult (1.2 si round≤3) × threatMult (1.5 si menace status/stat-decrease r5) × lowHpMult (0.5 si HP < 40%)`. **OP sets** : 7 sub variants (mewtwo/alakazam/gengar/jynx/vaporeon/zapdos — 163 → 169 sets). Tests : 10 scénarios intégration. Gate CI : **1618 unit + 246 intégration + build + typecheck verts**.
- **Hotfix BattleScene lazy load (2026-05-24)** : `ensurePokemonAssetsLoaded` sans early-return. Phaser 4 gotcha : `totalToLoad` reste à 0 si le Loader n'a jamais démarré — early-return sur `!loader.isLoading()` bloquait le chargement quand `sandbox` change le Pokemon sélectionné après init. Fix : supprimer le guard, laisser Phaser gérer l'idempotence. `BattleScene.ts` +20 lignes, CI fast verte (1616 unit).
- **Hotfix sandbox config + batch A cleanup (2026-05-24 — commit `45f5840`)** : null-safety sur `SandboxConfig` fallback (évite crash si config partielle). Retrait bulbasaur→venusaur, charmander→charizard du batch A stale data. Fix typo `TurnSystemKind.Charge` dans test enum.
- **Brume + Rune Protect — Team Protections bundle (2026-05-24) — plan 098 DONE** : 2 moves canon Pokemon livrés avec twist tactique aura mobile r3 Manhattan (mirror plan 095). **Brume** (`mist`, Glace, statut) bloque baisses stats infligées par ennemis sur caster + alliés r3 (Growl, Tail-Whip, Charm, Iron-Tail secondary, etc.). 3 learners Gen 1 (Lokhlass, Artikodin, Mewtwo). **Rune Protect** (`safeguard`, Normal, statut) bloque statuts majeurs (Burned/Paralyzed/Poisoned/BadlyPoisoned/Asleep/Frozen) + Confusion sur caster + alliés r3 (Sleep-Powder, Will-O-Wisp, Confuse-Ray, Thunder-Wave, etc.). 12 learners Gen 1. Light Clay non-étendu (5 tours fixes Mist/Safeguard, contrairement Reflect/LightScreen). Rest self-induced bypass Safeguard (canon). Intimidate Atk-1 désormais bloqué par Mist (lifecycle statChangeApplied préservé). Terrain status non bloqué par Safeguard (canon). **Bug critique debuggé** : check friendly fire trop strict (`attacker.playerId === target.playerId` bloquait aussi les statuts self-infligés) → corrigé en `attacker.id === target.id` (self-induced only — ex: Rest bypass Safeguard). **Refactor infra unifié** : `ScreenAura` → `TeamAura`, `ScreenKind` → `AuraKind` (4 kinds Reflect/LightScreen/Mist/Safeguard), `BattleState.screens` → `BattleState.auras`, `EffectKind.PostScreen` → `EffectKind.PostAura`, events `Screen*` → `Aura*` (Posted/Dissipated/Broken). 30 fichiers touchés (core types + handlers + tests + renderer + i18n) sans régression plan 095. Nouveau `aura-system.ts` : helpers `findActiveAurasProtectingTarget(state, target, kindFilter?)` (filtre kind optionnel), `isProtectedFromStatDecrease` / `isProtectedFromStatus` retournent `{ protected: boolean; casterId?: string }`. `auraDurationForCaster(caster, kind)` switch sur kind (Light Clay seulement Reflect/LightScreen). Nouveaux events `BattleEventType.StatChangeBlocked` / `StatusBlocked` avec `ProtectionReason` const enum (Mist/Safeguard) + champ `protectingCasterId?` pour audit. Hook `handle-stat-change.ts` : avant application stage négative ennemie, check Mist → bloque + emit `StatChangeBlocked`. Hook `handle-status.ts` : avant application status, check Safeguard → bloque + emit `StatusBlocked`. `Intimidate` ability (`packages/data/src/abilities/ability-definitions.ts`) mise à jour : check `isProtectedFromStatDecrease` avant baisse Atk -1 ; `statChangeApplied` reste false si bloqué OU si stat déjà capée -6 (lifecycle test corner case préservé). IA `ai/threat-detection.ts` : helpers `enemyHasStatDecreaseMoveInRange(enemies, caster, range)` (set 23 moves) et `enemyHasStatusMoveInRange` (set 17 moves), range r5 Manhattan. `action-scorer.ts` `scoreSelfMove` étend `EffectKind.PostAura` : threatBonus ×1.5 si menace détectée alentour, sinon ×1.0 ; déjà aura kind active → -1. Renderer (`GameController.ts`) : `AURA_INDICATOR_SYMBOL` map (🛡️ Reflect / ✨ Light Screen / 🌫️ Mist / 🕊️ Safeguard) + `AURA_POSTED_KEY`/`AURA_BLOCKED_KEY`. `refreshAuraVisuals` recompute pour 4 kinds. `showTeamAuraHoverIcons` hover-only icônes centre tiles. Nouveaux cases events `StatChangeBlocked` / `StatusBlocked` émettent text coloré sur target. InfoPanel switch 4 kinds. `BattleLogFormatter` : `AURA_LABELS_FR/EN` Record<AuraKind, string> + helper `auraKindLabel`. i18n FR/EN 11 nouvelles clés (`aura.posted/dissipated/kind.{mist,safeguard}`, `aura.blocked.mist/safeguard`). 2 nouveaux fichiers tests intégration (`auras-mist.integration.test.ts` 5 scénarios, `auras-safeguard.integration.test.ts` 6 scénarios) + `threat-detection.test.ts` 7 unit. Gate CI : **1618 unit + 236 intégration + typecheck + biome lint 0 erreur + build verts**.
- **Loading screens + FOUC fixes (2026-05-24) — plan 097 DONE** : 3 bugs chargement itch.io résolus. (1) FOUC font menu : suppression `<link rel="preload">` (warning intentionnel Firefox — canvas-only font non utilisée par layout), `font-display: block` (FOIT court vs FOUT), chargement explicite `document.fonts.load()` via FontFace API avant démarrage Phaser. (2) Écran noir combat : `LoadingScene` Phaser unique réutilisable — queue assets dans `preload()` canonique (progress bar native), lazy sprites stricts — N Pokemon engagés seulement (~12 vs ~80 avant) via `extractEngagedPokemonIds` + `buildEngagedSpritesQueue` (dédupe cross-teams). (3) MapSelect preview noire : `fadeOut(0)` + `fadeIn(150ms)` caméra Phaser. Nouveau composant `LoadingOverlay` : progress bar pixel, label `X/Y fichiers`, tips rotatifs 3s, fadeOut. Helpers partagés : `preloadSharedUiAssets`, `buildEngagedSpritesQueue`, `extractEngagedPokemonIds`. 8 tips i18n FR/EN canon (Casse-Brique, Protection...). Gate CI : **1616 unit (+9) + 220 intégration + build + lint + typecheck verts**.
- **Pipeline deploy itch.io live (2026-05-23) — plan 096 DONE** : Workflow `.github/workflows/itch-deploy.yml` opérationnel, run CI `26342405217` `[ok]` sur release v2026.5.2. URL publique : **https://kekel87.itch.io/pokemon-tactics**. Trigger `release: released` (stable only, exclut prereleases vs GitHub Pages qui prend `published`), action `Ayowel/butler-to-itch@v1.3.0` (JS pure, pas Docker), push channel `html5` vers `kekel87/pokemon-tactics`. Vite config étendu : `base: ITCH_DEPLOY ? "./" : GITHUB_ACTIONS ? "/pokemon-tactics/" : "/"` (paths relatifs requis pour itch CDN, paths absolus pour GitHub Pages). Build : 30 Mo / 590 fichiers (limite itch 500 Mo / 1000 fichiers). Skills `.claude/skills/itch-feedback/` (scrape commentaires page itch via WebFetch, délègue triage `feedback-triager`) + `.claude/skills/itch-devlog/` (génère post devlog depuis release GitHub). Agent `publisher` orchestrant skill `/publish` (compile changelog, vérifie build, publie release, watch itch-deploy, génère devlog, lance wiki-keeper) — release-drafter mergé dedans. Secret CI `BUTLER_API_KEY` configuré côté humain. Topics GitHub corrigés : `tactics-game`/`tactical-rpg`/`turn-based-strategy`/`browser-game`/`html5-game`/`fan-game`/`monorepo`/`pnpm` ajoutés, `roguelike` viré. Décisions #380 (deploy parallèle Option A), #381 (Steam exclu fan-game Pokemon), #382 (titre `Pokemon Tactics` assumé + zéro monétisation + clauses README). Pas de post manuel Reddit/presse/influenceurs : visibilité 100 % passive. Playtest itch a remonté 2 bugs FOUC (font débord boutons menu boot, écran noir DL sprites combat) ajoutés backlog.
- **Barrières mobiles Reflect / Light Screen (2026-05-23) — plan 095 DONE** : 2 moves screen-setter livrés (`reflect`, `light-screen`) avec twist tactique aura mobile r3 **Manhattan** (forme diamant ~25 tiles) qui suit le caster (vs team-wide static Showdown). Core : nouveau `ScreenKind` enum (Reflect/LightScreen), `ScreenAura` interface, `BattleState.screens: ScreenAura[]` (array — 1 caster peut tenir Reflect ET Light Screen simultanément, repose même kind écrase), `screensLastTickRound`. Nouveau `EffectKind.PostScreen` + handler `handle-post-screen.ts` (Light Clay extends 5→8 tours inline, mirror Heat Rock). `screens-system.ts` : `computeScreenMultiplier` (×0.5 Phys via Reflect / ×0.5 Spé via Light Screen, crits percent, ennemis exclus, target inside r3 Manhattan des alliés casters), `computeBrickBreakInteraction` (×2 + break uniquement vs caster d'aura, n'importe quelle distance, casse toutes les auras du caster ; dégâts normaux Reflect ×0.5 vs allié protégé sans cassure). Brick Break ignore le screen multiplier pour son propre hit (canon Showdown break-then-hit). `damage-calculator` étendu avec params `screenMultiplier` + `brickBreakMultiplier`. 3 nouveaux events `ScreenPosted/Dissipated/Broken` + `ScreenDissipatedReason` const enum (`Expired|CasterKo`). End-turn `screens-tick-handler.ts` décrément + emit Dissipated. `handleKo` cleanup auras du caster + emit Dissipated(CasterKo) par kind. `HeldItemId.LightClay` data-only (lecture inline, mirror Heat Rock). Renderer : **système indicateurs générique** `PokemonSprite.setLeftIndicators(specs)` + `setChargingIndicator(active)`, ordre temporel via `seq` monotonic (oldest = adjacent HP, newest = leftmost), Map<id, IndicatorEntry> avec destroy() avant delete (no leak). Drop counter sprite (info dans InfoPanel uniquement). Hover-only zone aura : icônes 🛡️/✨ alpha 0.7 au centre tiles affichées seulement au survol du caster (`IsometricGrid.showScreenAuraHoverIcons` + `hideScreenAuraHoverIcons`). `GameController.refreshScreenVisuals()` recompute idempotent appelé sur ScreenPosted/Dissipated/Broken + PokemonMoved + TurnEnded. InfoPanel badges `{kind} {turns}t` caster / `{kind}` protected (signature étendue avec optional `state: BattleState`). i18n FR/EN : `screen.kind.reflect/lightScreen`, `screen.posted.reflect/lightScreen`, `screen.dissipated.*`, `screen.broken`, `infoPanel.aura.caster/protected`. BattleLogFormatter : 3 nouveaux cas. IA `action-scorer` : `scoreSelfMove` étend pour `EffectKind.PostScreen` (score `× earlyMultiplier × (1 + alliesInRadius)`, -1 si déjà aura même kind active du caster). **Aurora Veil dropped v1** (0 learner Gen 1, backlog post-Z-A). Décisions actées #372–379. Gate CI : **1607 unit + 220 intégration + typecheck + biome clean + build verts**.
- **Charge moves visibles + Flinch core (2026-05-22) — plan 094 DONE** : 3 moves charge Gen 1 livrés : `skull-bash` (Dash+Knockback, T1 +1 Déf `chargeEffects`), `sky-attack` (Single r4 Vol 140BP Flinch 30% critique élevé), `razor-wind` (Cône r3 Normal 80BP critique élevé). `StatusType.Flinch` volatile (1 tour, bloque Move/UseMove) + `BattleEventType.Flinched`. `MoveDefinition.chargeEffects?: Effect[]` (side-effects T1 self-only). `applyChargeEffects` dans BattleEngine. Reorder dashMoveCaster AVANT processEffects (bug skull-bash : animation dash → knockback dans bon ordre). Renderer : `PokemonSprite.setChargingIndicator(active)` symbole ⚡ persistant T1→T2 + helper `isChargeT1` (vérifie `sunSkipsCharge` & `weather===Sun`). InfoPanel : badge volatile "Charge {move}" + MoveTooltip tag "⏱ 2 tours" + variante sun-skip. `learnset-extensions.ts` (`packages/data`) : 60 learners skull-bash + 13 razor-wind Gen 1 canon — override temporaire en attendant intégration Legends Z-A. 3 OP sets ajoutés (pidgeot-sky-attack, snorlax-skull-bash, beedrill-razor-wind). Golden replay régénéré (107→115 actions, round 11→13). Gate CI : **1578 unit (+11) + 211 intégration + typecheck + build verts**.
- **HitAndRun moves (2026-05-21) — plan 092 DONE** : nouveau `TargetingKind.HitAndRun` — caster frappe cible Single, puis choisit une tile de retraite dans `retreatRange` (Chebyshev). 3 moves livrés : `u-turn` (Insecte 70BP contact), `volt-switch` (Élec 70BP non-contact, hitRange 1-2), `flip-turn` (Eau 60BP contact). Miss ou Protect bloque la retraite (parité Showdown). Const enum `HitAndRunRetreatFallbackReason` (Miss/Invalid/Missing). IA : heuristique greedy max-distance. `getLegalActions` expose tiles de retraite, renderer géré en 2 phases. Gate CI : **1566 unit + 202 intégration verts**.
- **Sandbox design system migration (2026-05-21) — plan 091 DONE** : refactor visuel pur sandbox post-code-review plan 090. `SandboxPanel.ts` 1067 → 761 lignes (-29%). Zéro inline `style.cssText` / `style.X` (vs ~140 dans 090). Bloc `!important` overrides `sandbox-studio.css:101-128` supprimé. Helpers DOM extraits dans `packages/renderer/src/ui/dom/` : `form-controls.ts` (createButton, createLabeledSelect, createLabeledCheckbox, createLabeledRange, createOptionalNumberInput, createPickerCard, replaceSelectOptions, types `SelectOption`/`ButtonVariant`), `Stepper.ts` (createStepper avec closure `getValue` / `setValue`, drop hidden input pattern), `MovesList.ts` (createMovesList mutualisé team builder ↔ sandbox via classes existantes `.tb-move-row` + `.tb-moves-list`). `loadData()` cache module-level (`packages/data/src/load-data.ts` : `cachedGameData: GameData \| null` + early return + export `resetGameDataCacheForTests`). `getMovepoolFor` memo `movepoolCache: Map<string, string[]>` sur instance SandboxPanel. `AbortController` instance par SandboxPanel + LanguageToggle, tous listeners passés `{ signal: this.abort.signal }`, `destroy()` appelle `this.abort.abort()` avant `replaceChildren`. `data-control-mode="ai\|player"` sur `.sb-section` remplace `applyDummyControlVisibility` (CSS rules `.sb-section[data-control-mode="ai"] .sb-moves-grid { display: none }` etc.). LanguageToggle : drop 2 blocs `style.cssText`, classes `.sb-language-toggle` + `.lang-toggle-floating`. Hex literals (`#222`, `#1a1a2e`, `#335`, `#444`, `#aac`, `#ddd`, `#557`) → tokens CSS (`--color-bg-elevated`, `--color-bg-surface`, `--color-border-surface`, `--color-text-muted`, `--color-text-secondary`, `--color-btn-*`). Classes `.sb-*` étendues : `.sb-form-row[data-layout]`, `.sb-form-label[data-width]`, `.sb-form-input[data-size]`, `.sb-form-value`, `.sb-picker-card`, `.sb-stepper`, `.sb-stat-stages`, `.sb-stat-cell`, `.sb-radio-group`, `.sb-radio-option`, `.sb-moves-grid`, `.sb-strip-left/-right`, `.sb-section-title`, `.sb-grid`, `.sb-col`. Gate CI : **1552 unit + 191 intégration + biome check 0 warning + typecheck + build**.
- **Sandbox refonte Studio (2026-05-21) — plan 090 DONE** : layout Studio injecté DOM (canvas Phaser flex + 2 colonnes Player/Dummy + bandeau Battle). `SandboxConfig.dummyControl: "ai" | "player"` + `dummyMoves: string[]`. Mode Player : `BattleScene.initSandboxBattle` met `controller.onTurnReady = null` (les 2 équipes en input humain, pas de wiring AI). Mutualisation `MovePickerModal` team builder ↔ sandbox : 4 cartes cliquables au lieu de 4 selects, type/cat icons via `asset-paths.ts`. Bugfix divergence movepool : `SandboxPanel.getMovepoolFor` faisait `toShowdownId(move.id)` (sans tirets) dans un Set kebab (avec tirets) → tous moves multi-mot filtrés. Fix : comparaison directe `legal.has(move.id)`. Drops `dummyLevel`/`dummyBaseStats`/Level field (toujours 50). JSON sandbox-configs migrés (`default.json`, `charmander-test.json`). i18n FR/EN clés `sandbox.studio.*`. Tests : `learnset-source-parity.test.ts`, `SandboxSetup.dummy-player.test.ts`. CI : 1552 unit + 191 intégration. **Code review post-impl** : SandboxPanel reste ~902 lignes avec ~140 inline styles + hex hardcodés contraires aux règles `.claude/rules/{css,html,renderer}.md` → plan 091 (design system migration).
- **Normalisation IDs kebab build-time (2026-05-21) — plan 089 DONE** : `build-reference.ts` : nouveau `buildRawMoveIdToKebabMap(showdown.moves)` (Showdown compressed key → `toKebabCase(move.name)`) passé à `parseLearnset` + `applyLearnsetOverride` Champions. Reference `pokemon.json` learnsets désormais 100% kebab (`leech-seed`, `phantom-force`, `close-combat`). Indexes `pokemon-by-move` régénérés. Downstream nettoyé : `buildShowdownToKebabIndex` supprimé, paramètre `showdownToKebab` retiré de `LoadPokemonOptions`/`initializeLearnsetResolver`/`team-builder-registry`/`load-data`, `learnset-resolver.translateMoveId` supprimée. Zéro mention `showdown` côté `packages/data/src/`. Golden replay régénéré (107 actions / round 11). Gate CI : **1542 unit + 191 intégration + lint clean**. Bug récurrent ID mismatch (3 incidents : plans 081/087/phantom-force team builder) éliminé à la racine.
- **TP moves + semi-invulnerable (2026-05-21) — plan 088 DONE** : nouveau `TargetingKind.Teleport` — cible tile libre in-range (LoS/terrain/hauteur bypassés). Caster atterrit directement sur target (tile doit être vide) ; dégâts sur tile si occupée par ennemi. 7 moves livrés : `teleport` (0BP pur TP), `fly` (r4 Vol 90BP), `dig` (r3 Sol 80BP), `bounce` (r3 Vol 85BP Para30%), `phantom-force` / `shadow-force` (r4 Spectre 90/120BP bypassProtect), `dive` (r3 Eau 80BP). États semi-invulnérables `Flying`/`Burrowing`/`Diving`/`Vanished` : T1 charge bloque movement, T2 attack-before-move ; vulnérabilités canon Bulbapedia (Thunder/Hurricane vs Flying, Earthquake/Magnitude vs Burrowing, Surf/Whirlpool vs Diving) avec ×2 dégâts si vulnérabilité match. Cancel via EndTurn = land. Solar-Beam aligné même règle (no-move T1) + flag `sunSkipsCharge`. Renderer : sprite Flying h+2 + ombre au sol via shadow Y offset ; Burrowing/Diving/Vanished invisible. Bugfixes inclus : terrain target lava filtré dans `getValidTargetPositions`, freeze post-self-KO (handleKo après LethalTerrainKo + HP bar update), phantom-force/baton-pass invisible team builder (learnset-resolver translate showdown→kebab). `resolveTeleportLanding` implémenté (4 cardinales + BFS fallback + fallback target). Nouveaux fichiers : `resolve-teleport.ts`, `resolve-teleport-landing.ts`. Gate CI final : build + typecheck + biome lint + 1542 unit + 191 intégration ✅.
- **Gender toggle TeamEditScene (2026-05-20 — hors plan)** : bouton ♂/♀ à droite du nom Pokemon dans `EditLeftPanel`. Hidden si genderless. Disabled + icône fixe si genre forcé (Kangaskhan F-only). Click = toggle M↔F. Default = genre majoritaire selon `genderRatio` ; 50/50 = random (RNG injectable pour déterminisme). Nouveau `gender-helpers.ts` : `pickDefaultGender(ratio, rng?)`, `resolveSlotGender(pokemonId, current?, rng?)`, `toggleGender(current)`. `TeamEditScene` : `handleGenderChange` + init slot via `resolveSlotGender` + `setOp` preserve gender. `team-generator` : init gender via `resolveSlotGender` propageant RNG seedé. CSS : tokens `--color-gender-male/female` (alignés constantes), `.tb-gender-toggle` + `.tb-edit-name-row` dans `edit-panels.css`. i18n FR/EN : `teamBuilder.gender.male/female`. Tests : 10 unit `gender-helpers.test.ts` (genderless, skewed, forced, 50/50 deterministic). Gate CI verte : 1524 unit + 189 intégration + typecheck + lint + build.
- **Bugfix hors plan data reference (2026-05-20)** : 2 bugs résolus dans le pipeline `pnpm data:update`. (1) 10 Pokemon Gen 1 (Pidgey/Pidgeotto/Rattata/Raticate/Spearow/Fearow/Paras/Parasect/Weedle/Kakuna) avaient `learnset.levelUp/tm/tutor = []`. Cause : `parseLearnset` dans `build-reference.ts` calculait `maxGen` global sur **toutes** les entrées du Pokemon (y compris `8V` Virtual Console). V/E/S/R skip ensuite → tous les vrais moves L/M/T des générations antérieures filtrés. Fix : `maxGen` restreint aux types `L`/`M`/`T` (constante `LEARNABLE_TYPES`). (2) Kangaskhan `genderRatio: { male: 50, female: 50 }` au lieu de `{ male: 0, female: 100 }` (canon : exclusivement femelle). Cause : `transformPokemon` gérait `gender === "N"` (genderless) mais pas `gender === "F"` / `"M"` (default 50/50 forcé). Fix : ajout des branches. Régénération via `pnpm data:update:skip-fetch` (3.2s). Résultats post-fix : Pidgey 14L+23M+6T, Rattata 13L+29M+10T, Paras 12L+29M+8T, Kangaskhan gender 0/100. Note : Kangaskhan learnset Champions = 67 TM uniquement (pas levelUp/tutor en Gen 9 mod), comportement attendu. Gate CI : 1514 unit + 189 intégration + typecheck + build + lint.
- **Bugfix hors plan flaky Hyper-Fang (2026-05-20)** : `buildMoveTestEngine` migré en options object `{ gridSize?, random? }`. `hyper-fang.test.ts` passe `{ random: createPrng(0) }` explicite. 13 call sites positionnels migrés. Cause racine : helper passait `undefined` à `BattleEngine` → fallback `Math.random()` → 90% accuracy flaky selon ordre tests (backlog 2026-05-17 résolu). Gate CI : 1514 unit + 189 intégration + typecheck OK.
- **Bugfix hors plan caméra (2026-05-20)** : `BattleScene.recenterOnActivePokemon` étendu avec param `instant` (center direct, bypasse Pan effect). Appelé en fin de `transitionToBattle` et `initSandboxBattle`. `GameController.pan()` passe `force=true` dans handlers `refreshUI` et `battleLogPanel` click. Cause racine : `Phaser.Cameras.Pan` ignore silencieusement tout appel si `isRunning=true` — chaque recentrage per-turn ratait quand tours chaînés rapidement ou pan utilisateur encore actif. Bug "Caméra hors-écran 12v1" (backlog 2026-05-19) résolu. Gate CI : 1514 unit + 189 intégration + build + typecheck + lint.
- **Bugfix hors plan MapSelect preview noire (2026-05-20)** : reset état instance Phaser dans `MapSelectPreviewScene.create()` (`currentUrl`/`isometricGrid`/`decorationsLayer`) et `MapSelectScene.init()` (`selectedIndex`/`listItems`/refs). Bug backlog 2026-05-19 résolu.
- **Refactor roster-poc → playable-pokemon (2026-05-19) — plan 087 DONE** : module `packages/data/src/playable/` créé (`playable-pokemon-entry.ts` + `playable-pokemon.ts` 80 Pokemon + dummy custom). Concept "POC roster" tué. Dossier `packages/data/src/roster/` supprimé. `PlayablePokemonEntry { id; custom? }` — `movepool` et `abilityId` retirés des entries (curation vit dans OP sets). `loadPokemonFromReference` accepte `{ implementedMoveIds, showdownToKebab, getOpSetMoveIds }` et **dérive** : `PokemonDefinition.movepool` = union OP sets (curé, ordonné par OP set 1 first) ∪ learnset legal filtré par moves implémentés ; `PokemonDefinition.abilityId` = `reference.abilities.ability1`. `slice(0, 4)` fallback BattleSetup donne donc OP set 1 quand disponible. `buildShowdownToKebabIndex` mappe `leechseed` → `leech-seed` via `movesReference[].id`. `load-data.ts` simplifié : `allMoveIds = Object.keys(tacticalOverrides)`. Call sites migrés : `team-builder-registry` (rosterIds → playableIds), `implementation-flags`, `team-builder-data` renderer, `analyze-op-sets` script. Test `implementation-flags.test.ts` mis à jour. Script `audit-learnsets.ts` supprimé (tautologique). Golden replay régénéré (107 actions / round 11). Tests `SandboxSetup.test.ts` et `scored-ai-smoke.test.ts` adaptés (default movepool = OP set 1 ordering). Gate CI verte : 1514 unit + 189 intégration + typecheck + build + lint.
- **Refonte TeamSelectScene (2026-05-19) — plan 086 DONE** : nouveau flow `MapSelect → TeamSelect (équipes saved) → Placement (sous-pick) → Battle`. Core : `PlacementTeam.pokemonIds` → `availablePokemonIds` (6 mons) + `placedPokemonIds` (tracké via `placedByPlayer`). Nouveaux API : `tryPlace`/`finishPlayer`/`canFinishPlayer`/`removePlacement`/`isPlayerDone`/`getPlacedPokemonIds`. `isComplete` flexible (chaque player : ≥1 placé ET (cap atteint OU `finishPlayer`)). `buildTurnQueue` dimensionné `maxPokemonPerTeam` au lieu de `maxTeamSize`. `recomputeTurnIndex` (au lieu de `++`/`--` brittle). `autoPlaceAll` finit player exhausted si placedCount ≥ 1. Renderer : `TeamSelectScene` réécrit en DOM (mount root `<div class="ts-root">`, 3 colonnes : PlayersColumn left + TeamList center + PlayersColumn right si N>6). 6 composants DOM `ui/team-select/` : `FormatPicker`, `TeamListItem`, `TeamList`, `PlayerCell`, `PlayersColumn`. 3 helpers `team/` : `last-selection` (localStorage `pt:team-select:last-v1`), `refresh-ai-teams` (bulk re-roll IA), `build-overrides` (TeamSlot → BattleSetup overrides). `PlacementRosterPanel` étend signature `show` (callback object avec `onFinish?`). `BattleSetupConfig` étend avec `moveOverrides`/`abilityOverrides`. `TeamSelection` étend avec `slots?: TeamSlot[]`. `BattleScene.startPlacementPhase` câble overrides depuis `teamSelectResult.teams[i].slots`. AI default = équipe Aléatoire ephémère (re-roll à création). Bouton "Remplir IA aléatoire" conservé. CSS modulaire `styles/components/team-select.css` (BEM `.ts-*`, tokens design). 17 nouvelles clés i18n FR/EN (`teamSelect.format/players/controller/teams.random/teams.empty/actions/autoPlacement/turnSystem/warn/launch/placement.subpick`). Tests : 8 nouveaux unit core (sub-pick flex, removePlacement, mirror IDs) + 2 helpers renderer (last-selection 6, refresh-ai-teams 2). Gate CI verte : **1514 unit + 1703 intégration + build + typecheck + lint**.
- **Team Builder CSS/Polish (2026-05-18) — plan 085 post-polish DONE** : CSS monolithique `team-builder.css` splitté en 12 modules sous `packages/renderer/src/styles/`. Bug fix global `* { padding: 0 }` hors `@layer` qui écrasait les paddings natifs partout. `Modal.ts` migré de `<div>` backdrop custom vers `<dialog showModal()>` natif (accessibilité + fermeture Esc + backdrop HTML natif). `SlotCardsRow.ts` : `<div>` → `<button>` + wrapper pour le bouton clear (a11y). Type colors CSS vars calculées depuis pixels dominants des icônes type (`assets/ui/types/*.png` via sharp). Noms FR Pokemon via `getPokemonName(id, lang)` (Florizarre, Dracaufeu, etc.). 4 polish : boutons × centrés (`align-items: baseline` + police Pokemon), type chips couleur icône, gap filtres item picker, noms FR. Code review appliqué : tokens design (stats/feedback/btn-border/ghost-hover/backdrop/font-mono), `fuse.pickLocalized`, flatten nesting modal CSS, simplify `Modal.destroyed` flag, lazy img loading. Nouvelles règles `.claude/rules/css.md` et `.claude/rules/html.md` ajoutées. Gate CI : build + typecheck + lint (737 fichiers) + 1496 unit + 189 intégration verts.
- **Team Builder UI (2026-05-17) — plan 085 DONE** : 2 scènes Phaser DOM + 11 composants UI + 3 helpers data. `MyTeamsScene` : liste teams localStorage triée par date, boutons New / Generate Random, cartes avec 6 portraits + Edit/Export/Delete, empty state, confirm modal delete. `TeamEditScene` : 6 slot cards en row haut (vide + / rempli sprite + types badges + clear ×, highlight orange slot actif), 2 colonnes édition. Colonne gauche : portrait + types + Lv.50, radio Ability, item picker modal, nature dropdown inline (25 entrées ordre Champteams), 4 moves picker modal indexé (search + filtres Phys/Spec/Stat), bouton Set OP avec dropdown 1-3 sets curés appliquant slot complet. Colonne droite : 6 stats bars (colorées HP/Atk/Def/SpA/SpD/Spe), 6 SP sliders 0-32 (total clamp 66), compteur SP, 4 presets (Phys Sweeper 2HP/32Atk/32Spe, Spec Sweeper 2HP/32SpA/32Spe, Tank Phys 32HP/32Def/2SpD, Tank Spé 32HP/2Def/32SpD) + Reset. Modals : `Modal` base DOM (overlay + esc + backdrop + close), `Dropdown` base. Pokemon picker : grid 72px portraits avec search + 18 type filters. Move picker : list via `resolveLearnset(pokemonId)`, filtres categories + type. Item picker : 5 catégories, items non-implémentés grisés + tag, choix "(aucun)" en tête. Showdown io modal : 2 tabs Import/Export, textarea read-only export + copy clipboard, textarea import + bouton Parse + erreurs/warnings affichés. Auto-save débounce 300 ms via `SaveDebouncer` + indicateur "Sauvegardé" 1s. `team-generator.ts` : pick 6 mons random parmi 81, applique OP set 1 (fallback custom abilityId). `team-builder-data.ts` : agrégateur cache lang-aware (Pokemon playable triés par dex, moves implémentés, items + flag impl, abilities + flag impl). Data : `getCatalogAbilities/Items/Moves/PokemonAbilities` + `getAllOpSets/getOpSetsForPokemon` ajoutés à `@pokemon-tactic/data`. Core : `SP_TOTAL_MAX` / `SP_PER_STAT_MAX` exposés via index. tsconfig data : `op-sets/*.json` inclus. i18n FR/EN ~100 clés `teamBuilder.*` (nature 25 entrées + presets + sections + pickers + Showdown + delete confirm). Menu : entrée `Constructeur d'équipe` dans MainMenuScene. Smoke test Playwright : random Moltres → Choice Band / Intimidate / Lance-Flammes+Déflagration+Aéropique+Cru-Ailes / SpA 31 + Spe 31 (62/66) / export Showdown correct (248 Atk / 248 Spe = SP×8). CI : **1496 unit + 189 intégration + build + lint + typecheck verts**.
- **Système Météo (2026-05-13) — plans 084 + 084b DONE** : Météo complète core + renderer + i18n + sandbox. Core : enum `Weather` (None/Sun/Rain/Sandstorm/Snow), `BattleState.weather` + `weatherTurns` + `lastWeatherSetByPokemonId`, `weather-system.ts` (helpers + `effectiveWeather` avec Cloud Nine), end-turn handler `weather-tick` (Sandstorm 1/16 max HP sauf Rock/Ground/Steel), `applyWeatherWar` (setter le plus lent gagne). Modificateurs BP : Feu ×1.5/×0.5 Soleil/Pluie, Eau ×1.5/×0.5 Pluie/Soleil. Accuracy overrides : Thunder/Hurricane 50%/100% Pluie, Blizzard 100% Neige. Défenses : Rock +50% DéfSpé Sable, Glace +50% Déf Neige. Gel bloqué sous Soleil. Synthèse : soigne 50% normal, 2/3 Soleil, 25% autres. Solar-Beam 2 tours : T1 charge PP + MoveCharging + lockedMoveId + float "Rayonne!", T2 retarget + KO clear + BP halvé Pluie/Sable/Neige + skip charge sous Soleil. `AbilityActivated` events au tour start pour Chlorophylle/Nage Rapide/Voile Sable. Data : 4 moves setter (sunny-day/rain-dance/sandstorm/snowscape), weather-ball (passe au type météo actif), solar-beam revu, `weatherAccuracyOverride` sur thunder/hurricane/blizzard, 4 abilities déclaratives (chlorophyll/swift-swim/sand-veil/cloud-nine), `HeldItemId.HeatRock` (5→8 tours). Renderer : `WeatherHud` top-center sous texte tour (icône 48px + label + compteur tours). i18n FR/EN 22 clés `weather.*` + `sandbox.weather*`. SandboxPanel : dropdown 5 météos + input turns. Flottants weather damage + StatusImmune `reason="weather"`. `BattleLogFormatter` pour événements météo. **op-sets.json 160/160 full (100%)**. CI : **1496 unit + 189 intégration**.
- **Content Batch F (2026-05-12) — plan 083 DONE** : 2 moves ajoutés (giga-drain/Giga-Sangsue, focus-blast/Balle Focus) — total **145 moves**. 9 items ajoutés (choice-specs, eviolite, black-sludge, leek, thick-club, white-herb, flame-orb, salac-berry, normal-gem) — total **21 items**. Nouveau hook core `onStatLowered` sur `HeldItemHandler` (White-Herb — restaure une stat baissée, consomme). `op-sets.json` : **157/160 sets `full` (98%)**, 3 partials restants = weather-related (heat-rock, weather-ball, solar-beam) — débloqués plan 084.
- **Roster Batch E (2026-05-12) — plan 079 DONE** : 14 Pokemon Gen 1 ajoutés (butterfree, beedrill, pidgeot, raticate, fearow, golbat, venomoth, farfetch-d, seaking, articuno, zapdos, moltres, mewtwo, mew). Roster 67 → **81 Pokemon jouables** — roster Gen 1 final complet (hors Ditto). 8 nouveaux moves (leech-life/Vampirisme, mega-drain/Méga-Sangsue, twineedle/Double Dard, aerial-ace/Aéropique, feather-dance/Danse Plumes, hyper-fang/Croc de Mort, quiver-dance/Papillodanse, roost/Atterrissage) — total **143 moves**. 6 nouvelles abilities (compound-eyes/Œil Composé via `accuracyMultiplier`, swarm/Essaim, water-veil/Ignifu-Voile, pressure/Pression via `targetedCtBonus`, shield-dust/Suintement via `onSecondaryEffectBlocked`, inner-focus/Attention stub) — total **52 abilities**. Nouveaux mécanismes core : `EffectKind.Drain` + `handle-drain.ts` (soigne attaquant = fraction × dégâts infligés), `AbilityHandler.accuracyMultiplier`, `AbilityHandler.targetedCtBonus`, `MoveDefinition.bypassAccuracy` (Aéropique touche garantie), `filterShieldDustTargets` dans effect-processor (`onSecondaryEffectBlocked`). Sprites extraits pour les 14 Pokemon.
- **Roster Batch D (2026-05-11) — plan 078 DONE** : 16 Pokemon Gen 1 ajoutés (arbok, clefable, parasect, dugtrio, persian, victreebel, rapidash, dodrio, muk, onix, weezing, chansey, tangela, seadra, mr-mime, tauros). Roster 51 → **67 Pokemon jouables**. 8 nouveaux moves (poison-fang/Crocs Venin, coil/Enroulement, glare/Regard Médusant, cosmic-power/Pouvoir Cosmique, spore/Spore, leaf-blade/Lame Feuille, drill-peck/Picpic, barrier/Barrière) — total **135 moves**. 2 nouvelles abilities (poison-touch/Contact Venéneux via hook `onAfterDamageDealt`, filter/Filtre via `onDamageModify`) — total **46 abilities**. Nouveau hook core `onAfterDamageDealt` ajouté à `AbilityHandler`. Sprites 15 Pokemon téléchargés via PMDCollab (`pnpm extract-sprites`). **Bugfixes post-Batch C inclus dans cette session** : recoil bug corrigé (`handle-recoil.ts` utilisait `maxHp * fraction` → `lastDamageDealt * fraction`, ajout `SharedEffectState.lastDamageDealt`) ; drill-peck pattern corrigé (`TargetingKind.Slash` → `TargetingKind.Line, length: 2`) ; ordre Pokédex réécrit dans `roster-poc.ts` ; golden replay régénéré (108 actions, round 10).
- **Bugfixes post-Batch C (2026-05-11 — hors plan)** : Haunter retiré du roster (sprites conservés sur disque) — roster 52 → **51 Pokemon**. `DamageDealt` event : nouveau champ `recoil?: boolean` dans `battle-event.ts`. Renderer : Self-Destruct / Explosion affiche "K.O.!" au lieu du grand nombre négatif quand `recoil === true && pokemon.currentHp === 0`. InfoPanel : badge "Verrouillé" (`status.lockedOn`) ajouté dans `VOLATILE_LABELS` pour Lock-On. Backlog : bug "Mode normal — overflow grille 51+ Pokemon" ajouté.
- **Roster Batch C (2026-05-07) — plan 077** : 18 Pokemon Gen 1 ajoutés (sandslash, ninetales, wigglytuff, vileplume, golduck, tentacruel, magneton, dewgong, cloyster, haunter, kingler, electrode, jynx, electabuzz, magmar, lapras, porygon, omastar). Roster 34 → 52 (puis 51 après retrait Haunter). 15 nouveaux moves (will-o-wisp, nasty-plot, sludge-wave, flash-cannon, discharge, screech, icicle-spear, lovely-kiss, crabhammer, self-destruct, tri-attack, lock-on, moonblast, ancient-power, shell-smash) — total 127. 8 nouvelles abilities (effect-spore, cloud-nine, shell-armor, hyper-cutter, oblivious, flame-body, trace, swift-swim) — total 44. `StatusType.LockedOn` volatile + hook `accuracy-check.ts` (consomme et auto-hit). Tri-attack Phase 4 = Para 20% fixe (décision simplification). Ancient-power = 5 chances indépendantes à 10%. Abilities cloud-nine et swift-swim = stubs Phase 9 (météo).
- **Roster Batch B (2026-05-06) — plan 076** : 19 Pokemon Gen 1 finaux ajoutés (nidoqueen, nidoking, primeape, arcanine, poliwrath, golem, slowbro, gengar, hypno, exeggutor, marowak, hitmonlee, hitmonchan, rhydon, starmie, scyther, pinsir, kabutops, aerodactyl). Roster 15 → **34 Pokemon jouables**. 10 nouveaux moves (cross-chop, rock-slide, confuse-ray, energy-ball, bonemerang, blaze-kick, thunder-punch, ice-punch, fire-punch, double-edge) — total 112. 8 nouvelles abilities (vital-spirit, insomnia, cursed-body, rock-head, limber, iron-fist, natural-cure, battle-armor) — total 36. 3 nouveaux hooks AbilityHandler : `blocksRecoil` (handle-recoil.ts), `preventsCrit` (damage-calculator.ts), `onEndTurn` (BattleEngine). Scyther + Aerodactyl : FlyingIdle → Walk fallback (décision #304). Exeggutor sans ability (chlorophyll Phase 9, décision #301). CI : 1188 unit + 166 intégration verts.
- **Bugfixes hors plan (2026-05-06)** : 5 bugs résolus — (1) natural-cure émet désormais `StatusRemoved` avant `AbilityActivated` → icône statut retirée au renderer ; (2) noms Pokemon Batch B (+19) injectés dans `pokemon-names.*.json` et locales renderer, type `Translations` mis à jour — slug disparu en sandbox/team builder ; (3) selects moves sandbox : double fix init (`movepool[i]` par défaut, slice à 4) + changement Pokemon (`select.value` lu avant vidage options, fallback `movepool[i]` dans `rebuildMoveOptions`) ; (4) `dexNumber` ajouté à `PokemonDefinition` + chargé dans `loadPokemonFromReference`, tri par dex dans `SandboxPanel` et `TeamSelectScene` ; (5) `GRID_COLS = 7` dans `TeamSelectScene` — 5 lignes au lieu de 7 sur 34 Pokemon, bouton Launch visible.
- **Fixes post-plan 075 + playtest (2026-05-05 — hors plan)** : Sprites 12 Pokemon Batch A téléchargés via PMDCollab (`pnpm extract-sprites`). Tests intégration corrigés (pokemonTypesMap, compteurs roster). `BattleSetup.ts` limite `moveIds` aux 4 premiers moves du `movepool` (movepool reste réservoir complet pour futur Team Builder). `regenerate-golden-replay.ts` aligné. Sandbox : 4 premiers moves par défaut quand aucun configuré. i18n : anciens non-finaux retirés (bulbasaur, charmander, squirtle, pidgey, pikachu, machop, abra, gastly, geodude, growlithe, jigglypuff, seel, eevee, tentacool, nidoran-m, meowth, magnemite, sandshrew), 12 Pokemon Batch A ajoutés dans `types.ts`, `en.ts`, `fr.ts`. Biome format sprites JSON. Décision #300. CI : 1168 unit + 157 intégration verts.
- **Roster Batch A (2026-05-04) — plan 075** : 18 formes non-finales retirées du roster (bulbasaur, charmander, squirtle, pidgey, pikachu, sandshrew, nidoran-m, jigglypuff, tentacool, geodude, magnemite, abra, machop, seel, gastly, growlithe, meowth, eevee). 12 Pokemon finaux Gen 1 ajoutés : venusaur, charizard, blastoise, raichu, alakazam, machamp, gyarados, snorlax, dragonite, vaporeon, flareon, jolteon. Roster 21 → 15 Pokemon (formes non-finales restent dans reference/, non exposées dans rosterPoc jusqu'au Team Builder). 29 nouveaux moves (102 total) : petal-blizzard, synthesis, growth, fire-blast, flare-blitz, lava-plume, dragon-claw, dragon-dance, air-slash, surf, hydro-pump, waterfall, aqua-tail, ice-beam, thunder, iron-tail, charge-beam, psychic, recover, rest, amnesia, dynamic-punch, close-combat, brick-break, shadow-ball, crunch, outrage, extreme-speed, acid-armor. 8 nouvelles abilities (28 total) : lightning-rod, magic-guard, no-guard, moxie, multiscale, water-absorb, flash-fire, volt-absorb. Mécaniques core ajoutées : `chance?` sur StatChange effect + `loadAllPokemonTypes()` dans data. Décision : lightning-rod sans redirect Batch A — redirect plan dédié.
- **Objets tenus / Held Items (2026-04-30) — plan 073** : `HeldItemId` enum (12 items). `HeldItemHandler` interface avec 8 hooks : `onDamageModify`, `onCritStageBoost`, `onAfterMoveDamageDealt`, `onAfterDamageReceived`, `onEndTurn`, `onTerrainTick`, `onCtGainModify`, `onMoveLock` (nouveau — verrou Choice piloté par hook, plus de check hardcodé ChoiceBand/ChoiceScarf). `HeldItemHandlerRegistry` (miroir `AbilityHandlerRegistry`). `PokemonInstance.heldItemId?` + `lockedMoveId?`. Mini-système critiques intégré à `damage-calculator.ts` (×1.5 + ignore stages défensifs négatifs, Gen 6+ simplifié). `BattleEngine` câblé : heal fin de tour, verrou Choice via `onMoveLock`, modificateur CT gain, blocage terrain. Validateur d'équipe : erreur `DuplicateItem` + champ `heldItems` sur `TeamSelection`. `BattleEventType` : `HeldItemActivated`, `HeldItemConsumed`, `HpRestored`, `CriticalHit`. Renderer : `GameController` fix `HpRestored` → mise à jour HP bar + `BattleLogFormatter` gèrent les 4 nouveaux types. i18n : clés `battle.critical` + `battle.itemConsumed` FR/EN. 12 tests intégration (`held-items.integration.test.ts`, 1 par item). Décisions #288-295.
- **Natures / Stat Alignment (2026-04-29) — plan 072** : mécanique core uniquement, affichage UI reporté. `Nature` enum (25 valeurs), `NatureEffect` type, table `NATURE_TABLE` Bulbapedia Gen 3+ (5 neutres + 20 non-neutres). `applyNatureModifier(stats, nature)` floor(stat × 1.1 / 0.9), HP exclu. `computeCombatStats(baseStats, level, nature?)` étendu (paramètre optionnel pour rétrocompat tests). `rollNature(rng)` uniforme sur 25. `PokemonInstance.nature` non-optionnel. `BattleSetupConfig.genderRng` renommé `creationRng` + ajout `natureOverrides?`. **Affichage InfoPanel reporté à la refonte UI** (étapes 4 + 5 du plan). Pas d'i18n nature, pas de constante couleur nature. 13 nouveaux tests unitaires (`nature-modifier.test.ts` + `roll-nature.test.ts` + extension `stat-calculator.test.ts`). Décisions #284-287. CI verte : 1154 unit + 137 intégration + typecheck + lint clean.
- **Genres Pokemon (2026-04-29) — plan 071** : `PokemonGender` enum (Male/Female/Genderless), `GenderRatio` type, helper `rollGender(ratio, rng)`. `PokemonInstance.gender` rolled à la création (`createPokemonInstance` accepte `rng` + `genderOverride` pour future Team Builder). Loaders `load-pokemon` et `reference-types` exposent `genderRatio` depuis `reference/pokemon.json`. Cute Charm vérifie genre opposé non-genderless (rule Showdown — fix bug latent plan 069). Symboles ♂/♀ Unicode colorés (`#5fa8ff` / `#ff7fb4`) à droite du nameText `InfoPanel`. Genderless → aucun symbole. 5 tests unitaires `roll-gender.test.ts` + 3 nouveaux scénarios intégration Cute Charm (mirror female, vs genderless, male carrier). Décisions #280-283. Bug data Kangaskhan (50/50 vs 0/100 canon) tracé en backlog. CI verte : 1135 unit + 137 intégration + typecheck + lint 0 error.
- **Polish talents/abilities (2026-04-29) — plan 070** : finitions système abilities plan 069. Lévitation traverse lava/deep_water sans malus. Tempo Perso bloque Confusion + Intimidation. Engrais/Brasier/Torrent émettent au seuil 1/3 HP (première traversée + battle start si déjà sous seuil). Cran (Guts) émet quand statut majeur reçu. Matinal (Early Bird) émet au réveil avec `shortenedByAbilityId` sur `StatusEffect`. Refactor hooks passifs (`onStatusBlocked` / `onStatChangeBlocked` / `onTypeImmunity` / `onStatusDurationModify`) → return type `{ blocked|duration, events: BattleEvent[] }` (pattern Showdown). Buffer startup events `BattleEngine` + `consumeStartupEvents()` + `rerunBattleStartChecks()` (sandbox). Helper `isEffectivelyFlying` extrait `packages/core/src/battle/effective-flying.ts`. Battle log + i18n FR/EN pour `AbilityActivated`. 6 nouveaux tests intégration. Doc `docs/abilities-system.md`. Suppression API `onAccuracyModify` (sand-veil dormant Phase 9). CI verte : 1130 unit + 134 intégration + typecheck + lint clean.
- **Système talents/abilities (2026-04-27) — plan 069** : `AbilityHandlerRegistry`, interface `AbilityDefinition` avec 9 hooks. 20 abilities pour 20 Pokemon roster. Statuts volatils `Intimidated` et `Infatuated` (position-linked). Helper `isEffectivelyFlying`. Event `AbilityActivated`. 20 tests intégration verts. Floating text jaune doré `"{abilityName}!"` renderer.
- **Animations vol (2026-04-26)** : refonte animations repos pour Pokémon Vol. `PokemonSprite` : `restingAnim` + `setRestingAnimation()` + `playRestingAnimation()` — volants restent FlyingGlide au repos, après dégâts et knockback. `getFlyingAnimationMode` simplifié (`"glide" | null`). `BattleScene` injecte `setRestingAnimation` à création sprite selon type. CI verte : 1130 unit + 107 integration.

### Fait avant (Phase 3 et antérieur)
- Doc complète : game-design, architecture, decisions (264 décisions), roadmap, references, methodology, roster POC, glossaire
- 21 agents + 7 skills Claude Code (`.claude/`)
- **Plan 001** : monorepo setup (pnpm workspaces, TypeScript bundler, Vite, Vitest, Biome)
- **Plan 002** :
  - 11 const object enums (TargetingKind, Direction, EffectTarget...)
  - 16 interfaces/types (1 fichier = 1 type)
  - Classe Grid avec helpers spatiaux
  - 7 targeting resolvers (single, self, cone, cross, line, dash, zone)
  - Traversée alliés/Vol/Spectre
  - Mocks centralisés (MockPokemon avec 4 Pokemon roster)
- **Plan 003** :
  - `TurnManager` : initiative, ordre tours, cycle rounds, gestion KO
  - `BattleEngine` : `getLegalActions` (BFS pathfinding), `submitAction` (move + skip_turn), event system, `getGameState`
  - `packages/data` : 4 Pokemon, 16 moves complets, type chart 18x18, overrides tactical/balance, deepMerge, loadData()
  - `validate.ts` : validation données au startup
  - `ActionError` enum, `Accuracy`/`Evasion` dans `StatName`, `MockBattle`
  - 117 tests, 100% coverage
- **Plan 004** :
  - `stat-modifier.ts` : multiplicateurs stages, `getEffectiveStat`, `clampStages`
  - `damage-calculator.ts` : formule Gen 5+, STAB (1.5x), type effectiveness, burn penalty
  - `accuracy-check.ts` : accuracy/evasion stages, random check
  - `effect-processor.ts` : 4 processors — processDamage, processStatus, processStatChange, processLink
  - `BattleEngine.executeUseMove` : pipeline complet resolveTargeting → accuracy → processEffects → events
  - 172 tests, 100% coverage
- **Plan 005** : `EffectHandlerRegistry`, pipeline phases tour StartTurn → Action → EndTurn, initiative dynamique
- **Plan 006** :
  - `statusTickHandler` : burn 1/16, poison 1/8, sleep 1-3t, freeze 20%/t, paralysie 25% proc
  - `linkDrainHandler` : drain Vampigraine EndTurn, rupture KO/distance
  - `handleKo` : retrait turn order, libération tile, rupture liens, idempotent
  - `checkVictory` : dernière équipe debout gagne, `BattleEnded`, combat verrouillé
  - 225 tests, 6 tests intégration
- **Tests headless IA** : IA Random (58 rounds), IA Smart (67 rounds) — core validé bout en bout

- **Maintenance 2026-03-22** : fix infra `vite-tsconfig-paths`, "créature" → "Pokemon", README réécrit, agents améliorés, style commit titre seul

- **Plan 007** — Renderer POC : bootstrap Phaser 4 RC6, grille iso 12x12, sprites placeholder, sélection + highlight, déplacement + ciblage hot-seat, queue animations

- **Plan 008** — Move+Act FFTA-like : `turnState { hasMoved, hasActed }`, `EndTurn` avec direction, `getLegalActions` conditionnel, 234 tests

- **Plan 010** — Sprites PMDCollab : script `extract-sprites.ts`, `SpriteLoader.ts`, `PokemonSprite.ts` refactoré, portraits timeline, atlas pour 4 Pokemon

- **Plan 009** — UI FFT-like : `BattleUIScene` overlay, menu action FFT, sous-menu attaque, panel info, timeline, curseur animé, state machine 6 états

- **Post-plan 009 — Bugfixes + features dash (2026-03-24)** : `PlayerId` const enum, fix `HighlightKind`, fix dash targeting, `getValidTargetPositions` Dash toutes directions, Dash repositionne caster, Dash dans vide autorisé

- **Plan 011** — KO body blocking : `koCountdown` supprimé, corps traversable non-stoppable, `playFaintAndStay`, 249 tests

- **Plan 012** — Direction fin de tour : `direction` obligatoire sur `EndTurn`, `DirectionPicker` spritesheet arrows, orientation initiale vers centre grille

- **Plan 013** — Modèle carte + phase placement : `MapDefinition`, `PlacementPhase` serpent/libre/undo/autoPlaceAll, `poc-arena` 12x12, `PlacementRosterPanel`, flow placement → combat

- **Post-plan 013 — Roster 4 → 12, 16 → 48 moves, format 6v6 (2026-03-26)** : 8 nouveaux Pokemon, 32 nouveaux moves, URL `?random`, status icons, stat change indicators

- **Plan 015** — Stats niveau 50 : `computeStatAtLevel(base, 50)`, `PokemonInstance.combatStats`, damage-calculator utilise combatStats, 305 tests

- **Plan 019** — Preview dégâts estimés : `estimateDamage()`, preview dégradée HP bar, "Immune" pour immunités, 316 tests

- **Plan 020** — Canvas responsive + zoom/pan : FIT scaling, 3 niveaux zoom discrets, pan clavier/caméra, `roundPixels`, filtre NEAREST par sprite

- **Plan 021** — Sprite offsets PMDCollab + ombres : `Idle-Offsets.png`, `offsets.json`, HP bar/icône statut positionnés via headOffsetY, ombre ellipse sous pieds

- **Plan 018** — Status icons ZA + HP bar FFTIC + badges stat + sleep anim : `TurnTimeline` icônes ZA, HP bar 3 couleurs, icône statut sur sprite, badges Showdown stat changes

- **Plan 017** — Preview AoE sur grille : preview dynamique (mouse), flow 2 étapes style FFTA (preview → lock → confirm), outline périmétrique portée

- **Plan 016** — Infos attaques UI + type icons : fix `resolveCone` largeur dynamique, fix `Cross` self-centered, type icons Pokepedia ZA, category icons Bulbagarden, `MoveTooltip`, `pattern-preview.ts`

- **Plan 022** — Refonte timeline turn order : `predictedNextRoundOrder` dans BattleState, section haute/basse, séparateur round, KO retirés

- **Plan 023** — Mode Sandbox + 8 moves défensifs : micro-carte 6x6, DummyAiController, 2 panels, Protect/Detect/Wide Guard/Quick Guard/Counter/Mirror Coat/Metal Burst/Endure, `DefensiveKind`, `ActiveDefense`, 56 moves

- **Refonte orchestration agents (2026-04-01)** : agents `commit-message` et `sandbox-url`, flow plan revu, 21 agents actifs

- **Plan 026** — Roster expansion Phase 1 : BadlyPoisoned, Confusion tactique, Bind, Knockback, Multi-hit, Recharge, 17 nouveaux moves, 595 tests

- **Plan 025** — Tests intégration par move + mécaniques : 56 fichiers test moves, 9 fichiers mécaniques transversales, `buildMoveTestEngine`, 582 tests

- **Plan 027** — Roster 12 → 20 Pokemon : 8 nouveaux (Évoli, Tentacool, Nidoran♂, Miaouss, Magnéti, Sabelette, Excelangue, Kangourex), sprites PMDCollab

- **Bugfix DummyAiController (2026-04-02)** : `playTurn()` retourne `BattleEvent[]`, 596 tests

- **Plan 029** — IA jouable : `AiDifficulty`, `AiProfile`, `action-scorer.ts`, `AiTeamController`, Player2 = IA Easy par défaut, 671 tests

- **Améliorations IA post-plan (2026-04-03)** : EndTurn orienté ennemi, scoring tiles réelles, friendly fire penalty, self-buffs intelligents, lookahead move+attack, filtrage scores négatifs

- **Plan 033** — Team Select : `TeamSelectScene`, `validateTeamSelection()`, toggle Humain/IA, bouton Auto/Vider/Valider, zones spawn colorées par équipe, 664 tests

- **Plan 032** — Portée déplacement variable : `computeMovement(baseSpeed, stages)`, paliers 2-7, recalcul dynamique, 705 tests

- **Plan 031** — Feedbacks visuels + refactor statuts volatils : BattleText unifié, knockback slide, confusion wobble permanent, 5 niveaux efficacité, refactor `ActiveLink` → `Seeded`/`Trapped`, 630 tests

- **Plan 030** — i18n FR/EN : système maison 70 lignes, `t()`/`setLanguage()`/`detectLanguage()`, fichiers JSON data, 7 fichiers UI migrés, bouton bascule FR/EN, 687 tests

- **Plan 028** — Replay déterministe : PRNG mulberry32 seedé, `BattleReplay`, `runReplay`, golden-replay.json, 659 tests

- **Plan 024** — Bugfixes sandbox : fix status au spawn, fix Vampigraine HP bar, menu action bas-droite relocalisé, 333 tests

- **Plan 037** — Battle Log Panel : `BattleLogFormatter` (16 event types, i18n FR/EN), `BattleLogPanel` scroll/pliable/clickable, 694 tests

- **Bugfixes post-plan 039 (2026-04-05)** : fix DirectionPicker import, fix portée ennemie hover, fix direction IA, fix self-target direction

- **Plan 039** — Animations combat : `AnimationCategory` (Contact/Shoot/Charge), pipeline sprites 3 nouvelles anims, direction pendant déplacement/avant attaque, `playAnimationOnce` avec fallback

- **Plan 038** — Portée déplacement ennemis hover : `getReachableTilesForPokemon`, layer `enemyRangeGraphics`, overlay orange, `docs/design-system.md` créé

- **Plan 036** — Menu principal + Settings + Disclaimer : `MainMenuScene`, `BattleModeScene`, `SettingsScene`, `CreditsScene`, `GameSettings`, `VITE_SANDBOX`

- **Plan 035** — Sandbox CLI : suppression query params, `VITE_SANDBOX`, `SandboxConfig`, `pnpm dev:sandbox`, agent `sandbox-json`

- **Plan 040** — Hot-seat multi-équipes : `PlayerId` 12 valeurs, serpentine N équipes, `TEAM_COLORS[12]`, poc-arena 12×20 avec 5 formats, `TeamSelectScene` dynamique, 699 tests

- **Publication Phase 2 (2026-04-06)** : README anglais, LICENSE MIT, GitHub Actions CI+Deploy, GitHub Pages, wiki joueur submodule, 5 nouveaux agents, fix lint ~100 violations

- **Plan 041** — Analytics Goatcounter (production uniquement, sans cookies, conforme RGPD)

- **Plan 042** — Bugfixes playtest : HP bar couleur équipe, preview dégâts noir semi-transparent, border blanc badges statut, Espace/C raccourcis clavier, fix IA vs IA victoire

- **Plan 043** — Tileset arène PMD : ICON Isometric Pack (Jao), `IsometricGrid.drawGrid()` texturé, variantes pseudo-aléatoires 15%

- **Plan 044** — Mode pixel art + police Pokemon Emerald Pro : `roundPixels: true`, `FONT_FAMILY`, `TILE_WIDTH=32/HEIGHT=16`, zoom × 2

- **Plan 045** — Format carte Tiled + parser + validation : `TerrainType` 11 valeurs, suppression `isPassable`, pipeline `packages/data/src/tiled/`, `MapPreviewScene`, `pnpm dev:map`

- **Plan 046** — Dénivelés + dégâts chute : `canTraverse` (montée max 0.5), `getHeightModifier` (±10%), `calculateFallDamage` (paliers 33/66/100%), BFS asymétrique, renderer `gridToScreen(x,y,height)`, `TILE_ELEVATION_STEP=8`, Hop/Walk selon `heightDiff`, 45 tests

- **Plan 046 — 6 vagues feedback playtest (2026-04-11)** : `canTraverse` descente max 1.0, `MovementStep`, single diagonal tween, `movement-animation.ts`, SandboxPanel accordéon, 810/810 tests

- **Plan 047** — LoS 3D + collisions terrain : `heightBlocks`, `MoveFlags` aligné Showdown, `hasLineOfSight` Bresenham, LoS dans 9 resolvers targeting, `ignoresLineOfSight` dérivé, dash contre mur `WallImpactDealt`, `resolveBlastEpicenter`, peuplement `moveFlags` ~74 moves, preview AoE filtrée

- **Plan 047 — hotfixes post-playtest (2026-04-11)** : `heightBlocks` min→max corrigé, `withinHeightReach`, blast épicentre = case avant pilier, `getLegalActions` utilise `resolveTargeting`, map unique `sandbox-los.tmj`, 839/839 tests

- **Plan 049** — Migration données vers référence JSON : `packages/data/src/roster/`, 3 loaders, suppression `base/pokemon.ts` etc., balance overrides

- **Plan 048** — Pokedex Reference Knowledge Base : `packages/data/reference/` 5 JSON (1025 espèces, 850 moves, 311 abilities, 948 items, type-chart), 19 index inversés, script `build-reference.ts`

- **Session 2026-04-13 — Plan 050 tileset custom PMD** : `tileset.png` (32×2368px, 74 tiles) + `tileset.tsj`, pipeline Python (4 scripts), 24 `.tmj` migrés, `resolveTileProperties` slope flip, dead code renderer supprimé, 849/849 tests

- **Bugfix depth animations (2026-04-12)** : `PokemonSprite.playAttackAnimation` bump depth, `animateMoveTo` idem, constantes `ATTACK_DEPTH_ENVELOPE_RADIUS=3`, `MOVEMENT_DEPTH_ENVELOPE_RADIUS=1`

- **Bugfix renderer (2026-04-14)** : layering `constants.ts` — tiles (1-125) → highlights (500-510) → Pokemon (520+) → curseur (900) → UI (1000+)

- **Plan 052** — Orientation tactique : `FacingZone`, `getFacingModifier` (0.85/1.0/1.15), intégré damage calc + preview, 28 tests

- **Plan 053** — Undo déplacement : action `undo_move`, snapshot restaure position, bouton "Annuler déplacement", 958 tests

- **Plan 054** — Système CT : `TurnSystem` interface, `ChargeTimeTurnSystem` (CT 600, seuil 1000, `ctGain`), `ct-costs.ts`, `EffectTier`, barre CT timeline, `ActionMenu` CT, 999 tests, CT activé par défaut

- **Plan 051** — Types terrain + modificateurs : 6 nouveaux BattleEventType, `terrain-effects.ts`, BFS Dijkstra malus terrain, bonus type/terrain +15%, brûlure au passage magma, `terrain-tick-handler`, ice slide, KO létal terrain (lava/deep_water), renderer tint supprimé ensuite

- **Plans Phase 3** :
  - **Plan 055** — bug gatling (immunité statut par type, icône terrain, KO anim, HP preview, stagger texte, ombrage flancs)
  - **Plan 056** — pipeline données Champions : `pnpm data:update`, `pnpm data:diff`, 45 moves overridés, `champions-status.json`
  - **Plan 057** — statuts Champions runtime : `StatusRules` injecté `BattleEngine`, paralysie 12.5%, gel 25% max 3t, sommeil sample([2,3,3])
  - **Session 2026-04-16** : fix `PlacementPhase` test, CI `pnpm test:integration`, tileset brightness uniforme
  - **Plan 058** — Preview CT Timeline : `BattleEngine.predictCtTimeline(moveId, count)`, entrées ghost timeline
  - **Plan 059** — CT Timeline prédictive scrollable : 24 slots, scroll molette, auto-scroll confirm_attack, entrée tail
  - **Plan 060 partiel** — Curseur FFTA (livré) + silhouette occlusion SKIPPÉE
  - **Plan 065** — Fix depth + Alt-click picking + OcclusionFader : `DEPTH_RAISED_TILE_BASE`, Alt picking, `OcclusionFader.ts`, fade alpha 0.4, Phase 3.5 repoussée (décision #272)
  - **Plan 066** — 7 maps thématiques (forest, cramped-cave, le-mur, volcano, swamp, desert, naval-arena), multi-format 5 objectgroups spawns
  - **Plan 067** — Écran sélection carte : `MapSelectScene`, flow MainMenu → TeamSelect → MapSelect → Placement → Battle
  - **Session 2026-04-24** : suppression `TERRAIN_TINT` (commit `082240c`)
  - **Plan 068** — Fix IA terrain + pathfinding : `DANGEROUS_TERRAIN_PENALTY=8`, `scoreMove` BFS sans budget, 3 méthodes publiques BattleEngine, fix traversal Flying/Fire sur terrains
  - **Plan 064** — Décorations + obstacles Tiled : Ghost traverse obstacles, parser `decorations`, `DecorationsLayer.ts`, 4 sprites décorations, 6 scénarios Gherkin

### Historique plans Phase 3 finis
*(voir ci-dessus — section "Plans Phase 3")*

### Prochaine étape

**Phase 6.5 « Client jouable : contrôles & UI » CLOSE (2026-08-21).** Les 3 lots sont livrés **et validés à la main** (Lot 3 = plans 174–182 publiés en `v2026.8.1`, Lot 1 = plan 183 tactile, Lot 2 = plan 184 clavier/manette). Les cinq chantiers hors-phase issus de la validation du Lot 2 sont tous **livrés et validés à la main** : légende de contrôles (plan 185), écran de remapping (plan 186), menu de combat (plan 187), refonte sélection d'équipe + passe manette Team Builder (plan 188, 2026-08-26), panoramique clavier + menu de combat au placement + découvrabilité du défilement (plan 189, 2026-08-26). **Trois derniers reliquats soldés avant release** (plan 190, 2026-08-27) : migration i18n complète du journal de combat, points 4K de l'interface de combat, purge de l'échelle morte du Team Builder (`--tb-px`). **Aucune phase active, aucun chantier ouvert.** Suite à trancher avec l'humain : une grosse phase (6 Maps & Éditeur 3D, 7 Multijoueur — désormais débloquée puisque le client est jouable, 8 Équilibrage) — voir `docs/next.md` § À faire maintenant. **19 commits non publiés depuis `v2026.8.1` (2026-08-19), plus celui-ci — aucune release depuis.**

**Points ouverts non bloquants** (`docs/next.md` § Reporté, `docs/backlog.md` § Dette technique) : le filet `try/catch` du poller manette (plan 188, décision #842) n'est couvert par aucun test direct, seul le bug précis qui l'a motivé l'est (la revalidation sur **pad réel** des autres correctifs du plan 188 a, elle, été faite le 2026-08-26) ; deux feuilles de style gardent des valeurs fixes sous la police mise à l'échelle en 4K, `.tb-btn` et `turn-timeline.css` (revue du plan 190) ; le match nul de combat n'a aucun chemin d'exécution, `checkVictory` étant appelé à chaque K.O. individuel plutôt qu'une fois par résolution (plan 190) ; `t()` retombe sur l'anglais avant la clé brute, ce qui rend une clé absente d'une seule locale invisible sur les familles de clés composées (plan 190, comblé pour le journal par `battle-log-keys.test.ts`, règle à retenir pour d'éventuelles futures familles).

#### Historique (obsolète, conservé pour contexte)

**Phase 4 en pause (dans main).** Plans 069–118 terminés. Roster Gen 1 complet : 80 Pokemon jouables + dummy. 177 OP sets. **393 moves implémentés.** Batches G1–G6 + B1/B2/B3 clos. **B4 clos** : plans 117 (Champs/field-terrain, 4 setter moves) + 118 (dépendants B4) terminés sur main (2026-06-09). ~121 moves restants (batches B5+). Reprendre après Phase 5 ou en parallèle sur une autre session.

### Bugs connus non corrigés

*(voir `docs/backlog.md`)* — `le-mur.tmj` désactivée, CT scoring IA différé (nécessite lookahead multi-tour)

### Points à adresser (renderer)
- Représentation visuelle moves défensifs : animation/feedback quand Protect bloque, Counter renvoie
- Menu action : position bas-droite retenue définitive

### Standards code établis
- Pas d'abréviations, variables nommées comme leur type
- Const object enum pattern (`as const` + type dérivé)
- 1 fichier = 1 interface, séparation enums/types/grid/battle
- Pas de commentaires (sauf algo complexe)
- Mocks : `abstract class` + `static readonly` + données pures
- Tests : comportement uniquement, const enums
- Coverage 100% core (threshold bloquant)

### Questions ouvertes
- Formules dérivées (Mouvement/Saut/Initiative depuis Vitesse+Poids) — Phase 1
- Movesets POC : Bombe-Beurk trop forte, Salamèche trop faible — Sludge Bomb 112 dégâts sur Pidgey (40 HP), ratio 2.8x à surveiller
- Friendly fire fréquent avec IA random (comportement attendu, à garder en tête pour équilibrage)
- PP system : IA sans filtre cible gaspille PP — `getLegalActions` peut servir de garde-fou
