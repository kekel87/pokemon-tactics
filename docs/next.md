# Agenda — Pokemon Tactics

Maintenu par Claude Code. Lu via `/next`.

## À faire maintenant

- **Phase 4 en cours.** Plans 069–117 terminés. Roster Gen 1 complet : 80 Pokemon jouables + dummy. 177 OP sets. **386 moves implémentés.** Batches G1–G6 clos. Vague 1 roadmap maître **terminée** (B1/113 + B2/116 + B3/115). Vague 2 entamée : B4/117 terminé.
- **🗺️ Roadmap maître = plan 112** (`docs/plans/112-moves-mechanics-roadmap.md`). **117 moves restants** classés par système moteur → batches B4–B23 + B-META, vagues 2–5. 1 système = 1 batch = 1 plan numéroté (117+). Téra/Z-Move/Dynamax droppés à vie (décision #422). Plan 112 B4 : 4/10 livrés (6 dépendants → plan 118).
- **Prochain — plan 118 (B4 moves dépendants)** : 6 moves exploitant les Champs posés par le plan 117 : **Gliss'Herbe** (`grassy-glide`), **Monte-Tension** (`rising-voltage`), **Vaste Pouvoir** (`expanding-force`), **Explo-Brume** (`misty-explosion`), **Champlification** (`terrain-pulse`), **Force Nature** (`nature-power`). Chaque move ajoute une sous-mécanique distincte (priorité-depuis-position, ciblage dynamique, self-KO, morph de type).
- **Reporté — migrer l'analytics vers backend first-party** (lié au futur mini-backend de matchmaking). GoatCounter (plan 114) est bloqué par les adblockers (domaine `goatcounter.com` sur EasyPrivacy → beacon filtré chez beaucoup de users desktop/Firefox uBlock). Fix : exposer un endpoint `/analytics` sur notre propre domaine backend (invisible aux adblockers) et y router `trackEvent`. À faire quand on attaque le backend matchmaking, pas avant.
- **Décisions tranchées** : Frustration/Retour/Puissance Cachée → mises de côté (inutilisables Gen 8/9, watch Pokémon Champions — décision #423) ; Morphing → batch Métamorph dédié (B-META).
- **Plan 099 terminé — Substitute (2026-05-25)**. Move `substitute` (Normal, statut, self). `StatusType` non utilisé — état pur via `PokemonInstance.substituteHp?: number` field (single source of truth). `EffectKind.PostSubstitute` + handler vérifie HP > 25% + pas déjà actif → coût `floor(maxHp/4)`, sub HP = `floor(maxHp/4)`. Damage routing `handle-damage.ts` intercepte avant écriture HP : `shouldSubstituteBlock(attacker, target, move)` (= sub actif + `attacker.id !== target.id` + flags sound/bypasssub absents) → `applySubstituteAbsorption` re-route dégâts vers `substituteHp`, emit `SubstituteDamaged`/`SubstituteBroken`, excess non-carry-over Gen 5+. `DamageDealt.absorbedBySubstitute?` enrichi. Status block `handle-status.ts` + stat decrease block `handle-stat-change.ts` après checks Mist/Safeguard → emit `StatusBlocked` / `StatChangeBlocked` reason=Substitute. `ProtectionReason.Substitute` enum étendu. Sound bypass automatique via reference flags (32 moves taggés sound dont sing/growl/screech/roar/supersonic implémentés roster, bypasssub aussi). Drain heal + recoil basés sur `lastDamageDealt` cumul → continuent fonctionner (canon Gen 5+). Self-bypass (recoil/confusion/struggle) via `attacker.id === target.id`. Weather/terrain damage hit `currentHp` direct (sub bypass canon). `handleKo` cleanup `substituteHp = undefined`. **Renderer** : `PokemonSprite.setSubstituteOverlay(active)` swap atlas `dummy` (sprite Substitute déjà extrait sandbox) via `getEffectiveSpriteId()` helper + replay anim. `GameController` câblé 4 events Substitute*, `setSubstituteOverlay(false)` aussi sur `PokemonKo`. `extractEngagedPokemonIds` ajoute "dummy" inconditionnel (préload obligatoire). `InfoPanel` badge volatile `Clone {hp} PV`. `MoveTooltip` tag `🔊 Sonore (ignore Clone)` / `Ignore Clone` selon flags. `BattleLogFormatter` 4 nouveaux cas + différenciation `StatChangeBlocked`/`StatusBlocked` selon `ProtectionReason`. **IA** `scoreSelfMove` : si déjà sub → -1, si HP ≤ 25% → 0, sinon base 30 × earlyMult (1.2 si round≤3) × threatMult (1.5 si menace status/stat-decrease r5) × lowHpMult (0.5 si HP < 40%). i18n FR/EN 17 nouvelles clés (`substitute.*`, `infoPanel.volatile.substitute`, `moveTooltip.tag.sound`/`bypasssub`). 7 OP sets ajoutés (mewtwo/alakazam/gengar/jynx/vaporeon/zapdos sub variants — 163→169 sets). 10 tests intégration (post + damage absorb + status block + stat block + sound bypass + KO cleanup). Gate CI : **1618 unit + 246 intégration + typecheck + build verts**.
- **Plan 097 terminé — Loaders + FOUC fixes (2026-05-24)**. `index.html` : `font-display: swap` → `block` + `<link rel="preload" as="font" crossorigin="anonymous">` (kill FOUC menu boot). Composant `LoadingOverlay` Phaser réutilisable (progress bar + label `X / Y fichiers` via `loader.totalComplete + totalFailed` / `loader.totalToLoad` + tips rotatifs 3s). 8 tips i18n FR/EN (mécaniques, stratégie, lore — Casse-Brique, Protection, terrain, météo, hauteur). `BootScene` Phaser dédiée minimal : `await document.fonts.ready` + `preloadSharedUiAssets` (arrows/types/categories/cursors/status/weather/tileset/decorations), transition vers MainMenu ou TeamSelect (sandbox). `BattleLoadingScene` parallèle `launch()` bind sur `BattleScene.load` + tips activés. `BattleScene.preload()` ne charge plus 80 sprites — lazy strict via `extractEngagedPokemonIds` (dédupliqué cross-teams, sandbox player+dummy). `loadEngagedSprites()` async + `createPokemonAnimations` après load.complete. `MapSelectPreviewScene` : camera `fadeOut(0)` instant + `fadeIn(150ms)` post-draw. Tests : `loading-tips.test.ts` (unique ids, FR/EN keys resolve, exclude lastShown), `extract-engaged-ids.test.ts` (sandbox, dedupe teams, 12v1 scale). Gate CI : **1616 unit (+9) + 220 intégration + build + lint + typecheck**.
- **Plan 096 terminé — Visibilité passive itch.io**. Workflow `.github/workflows/itch-deploy.yml` + skills `/itch-feedback` + `/itch-devlog` + agent `publisher` mergé `release-drafter`. Release v2026.5.2 publiée 2026-05-23 → run CI `26342405217` OK push channel `html5` sur `kekel87/pokemon-tactics`. Topics GitHub corrigés (vire `roguelike`, ajoute `tactics-game/tactical-rpg/turn-based-strategy/browser-game/html5-game/fan-game/monorepo/pnpm`). Secret `BUTLER_API_KEY` configuré. Vite `base: ITCH_DEPLOY ? "./" : GITHUB_ACTIONS ? "/pokemon-tactics/" : "/"` (deploy parallèle Pages + itch). Décisions #380-382 (deploy parallèle, Steam exclu fan-game, titre `Pokemon Tactics` assumé + zéro monétisation + clauses README). 2 nouveaux bugs FOUC remontés playtest itch (backlog).
- **Plan 095 terminé — Barrières aura mobile Reflect / Light Screen**. 2 moves screen-setter aura mobile r3 Manhattan (~25 tiles diamant, suit caster vs team-wide static Showdown). Core : `ScreenKind` enum (Reflect/LightScreen), `ScreenAura` interface, `BattleState.screens[]` (1 caster peut tenir 2 kinds simultanément, repose même kind écrase), `EffectKind.PostScreen` + handler. `screens-system.ts` : `computeScreenMultiplier` (×0.5 Phys/Spé selon kind, crits percent, ennemis exclus, target inside r3 Manhattan), `computeBrickBreakInteraction` (×2 + break vs caster d'aura n'importe quelle distance, casse toutes auras caster ; vs allié protégé sans cassure). Brick Break ignore screen multiplier pour son propre hit (canon break-then-hit). `damage-calculator` étendu params `screenMultiplier`/`brickBreakMultiplier`. 3 events `ScreenPosted/Dissipated/Broken`. End-turn `screens-tick-handler`. `handleKo` cleanup auras caster. `HeldItemId.LightClay` (5→8 tours, mirror Heat Rock). Renderer : système indicateurs générique `setLeftIndicators(specs)` + `setChargingIndicator(active)`, ordre via `seq` monotonic, Map<id, IndicatorEntry> avec destroy() avant delete. Hover-only aura icons 🛡️/✨ centre tiles. `refreshScreenVisuals()` idempotent sur ScreenPosted/Dissipated/Broken + PokemonMoved + TurnEnded. InfoPanel badges `{kind} {turns}t` caster / `{kind}` protected. i18n FR/EN 8 clés. IA `scoreSelfMove` étend PostScreen (score × earlyMultiplier × (1 + alliesInRadius), -1 si déjà aura kind active). Aurora Veil dropped v1 (0 learner Gen 1, backlog post-Z-A). Décisions #372-379. Gate CI : 1607 unit + 220 intégration verts.
- **Plan 094 terminé — Charge moves visibles**. 3 nouveaux moves 2-tours (skull-bash, sky-attack, razor-wind) + `MoveDefinition.chargeEffects?: Effect[]` (skull-bash Def +1 T1 self). Nouveau `StatusType.Flinch` core + `BattleEventType.Flinched` + `processFlinch` + `flinchedThisTurn` flag (mirror processConfusion). Sky-attack secondary Status Flinch 30%. Renderer : `PokemonSprite.setChargingIndicator(active)` symbole `⚡` à gauche barre HP (mirror statusIcon), persistant T1→T2, visible pendant semi-invul (HP bar enfant container). InfoPanel badge volatile `Charge {moveName}` pour `pokemon.chargingMove`. MoveTooltip tag `⏱ 2 tours` + variante `⏱ 2 tours (Soleil = instant)` sur moves `twoTurnCharge`/`sunSkipsCharge`. i18n FR/EN 4 clés nouvelles. 3 OP sets ajoutés (pidgeot-sky-attack/snorlax-skull-bash/beedrill-razor-wind). Razor-wind = Cone r3. Gate CI : **1577 unit + 211 intégration + build + typecheck verts**.
- **Plan 093 terminé — Baton Pass**. `EffectKind.TransferStatStages` + `MoveDefinition.targetsAlly` flag. Cible allié r1, transfert 7 stat stages (set parité Showdown). Caster reset à 0. Speed transfer recompute `derivedStats.movement` les 2 mons (`ctGain` auto-dynamique, pas stocké). Bypass Protect/Detect/Wide Guard via `defense-check` skip. AI scoring greedy `casterPositiveSum × 4 + bonus 8 si target vierge - 20 si no positive`. 8 porteurs Gen 1 (Mr-Mime/Mew/Vaporeon/Jolteon/Flareon/Butterfree/Beedrill/Zapdos). Décisions #356-#364. Gate CI : **1566 unit + 211 intégration + 6 scenario + build + lint + typecheck verts**. 9 tests intégration BP verts.
- **Plan 092 terminé — HitAndRun**. 3 moves (u-turn / volt-switch / flip-turn). `TargetingKind.HitAndRun` + variant `{hitRange, retreatRange}`. Resolver délègue à resolveSingle (hit Single LoS standard). Helper `validateHitAndRunRetreat` + `enumerateHitAndRunRetreatTiles`. `ActionUseMove.retreatPosition?` field. Events `HitAndRunRetreat` + `HitAndRunRetreatFallback{miss/invalid/missing/no-tile-available/blocked}`. Engine : miss bloque retreat (parité Showdown), occupied snapshot post-KO, terrain triggers post-retreat (lava/deep_water/ice). AI helper `pickAiHitAndRunRetreat` greedy max-distance + tie-break PRNG, enrichi dans `pickScoredAction`. Renderer : InputState `select_retreat_target` + cyan highlight `HighlightKind.Retreat`, click→fill retreatPosition, Escape→back. CI : 1566 unit + 202 intégration verts.
- **Sessions content fill — à intercaler** entre plans mécaniques. Pool restant Gen 1 : ~162 moves complexes restants / 58 abilities / 137 items. OP sets 160/160 full → finition = couverture exhaustive pour Team Builder libre (joueur compose hors OP sets). Format suggéré : 1 session = batch ciblé (ex: "tous les Poing/Punch", "tous les statuts moves manquants", "items berries", "abilities defensives") plutôt que mass dump. Lancer entre 2 plans mécaniques avancées pour pas s'épuiser. À planifier au fil de l'eau via plans 087+ "Content Batch G/H/I…".
- **Méta-plan Team Builder** (validé 2026-05-12) :
  - **081** — Team data model + validator + storage + Showdown io (core, réutilisable multi) ✅
  - **082** — Curate OP sets list (Smogon + CoupCritique, 1-3 sets/Pokemon roster) + script analyse fréquence content manquant → `docs/op-sets-gap-analysis.md` ✅
  - **083** — Content Batch F : 2 moves (giga-drain, focus-blast), 9 items (choice-specs, eviolite, black-sludge, leek, thick-club, white-herb, flame-orb, salac-berry, normal-gem), hook `onStatLowered`. 157/160 sets `full` (98%). ✅
  - **084 + 084b** — Système Météo complet (Sun/Rain/Sand/Snow, BP/accuracy/defense modifiers, Synthesis, weather-ball/solar-beam, abilities chlorophyll/swift-swim/sand-veil/cloud-nine, heat-rock, weather war, WeatherHud top-center, i18n, sandbox selector, Solar-Beam "Rayonne!", flottants WeatherDamage) ✅
  - **085** — Team Builder UI : `MyTeamsScene` (list + delete + new + generate random + export) + `TeamEditScene` (6 slots + 2 colonnes édition + Pokemon/Move/Item modals + Nature dropdown + SP sliders + 4 presets + Set OP + Showdown io). Auto-save débounce 300 ms localStorage. i18n FR/EN ~100 clés. ✅
  - **086** — `TeamSelectScene` refonte + sous-pick au placement (`PlacementPhase` étendu : `availablePokemonIds`/`placedByPlayer`/`finishPlayer`/`isComplete` flexible, `BattleSetupConfig` moveOverrides+abilityOverrides, DOM 3-col scene, `TeamSelection.slots`, last-selection localStorage, refresh AI bulk, AI default random, i18n + CSS modulaire). ✅
  - **087** — Refactor `roster-poc.ts` → `playable-pokemon.ts` : module `packages/data/src/playable/` avec `PlayablePokemonEntry { id; custom? }` (movepool + abilityId retirés). `PokemonDefinition.movepool` dérivé = union OP sets (curé, OP set 1 first) ∪ learnset legal ∩ implémenté. `PokemonDefinition.abilityId` dérivé = `reference.abilities.ability1`. `audit-learnsets` supprimé. Le concept "POC roster" est mort. ✅
- **Décisions méta-plan** :
  - IV=31 fixe (jamais exportés Showdown)
  - SP↔EV : 1 SP = 8 EV (export `sp×8` clamp 252, import `floor(ev/8)` clamp 32)
  - Niveau fixé 50 (Champions style)
  - localStorage illimité (pas de cap pour l'instant)
  - Équipes 6 Pokemon, format détermine N participants (placement = sous-pick)
  - Validateur core réutilisable (multijoueur futur)
  - Learnset = union levelUp+TM+tutor walkée via chaîne `evolvesFrom` (Showdown pattern)
  - ID normalizer : Showdown compresse sans tiret (`fireblast` vs `fire-blast`)
- **Bonus plan 064 différé — marquages arène + pokéball centrale** : approches dans `docs/plans/064-decorations-obstacles.md` (peinture Aseprite manuelle, génération procédurale). Reco : approche manuelle pour arène propre rapide, ou reporter post-Babylon via `DecalMap`.
- **Rewrite renderer Babylon (Phase 3.5) → déplacée APRÈS Phase 7** (décision 2026-04-20). Pistes à garder :
  - Shim type Inspector (`src/types/babylonjs-inspector.d.ts` → `declare module "@babylonjs/inspector" { export {}; }`) pour `skipLibCheck: false`.
  - Audit bundle `rollup-plugin-visualizer`, cible 180-220 kB gzip vs 273 kB spike.
  - Flat-shaded `ShaderMaterial` custom (cohérence pixel-art FFTA).
  - `GridMaterial.gridOffset = (0.5, 0, 0.5)` (shader local-space).

## Reporté / à refaire

- **Dette test `damageTo`** — helper dupliqué inline dans ~29 fichiers `*.test.ts` (viole `tests.md` : helpers → `packages/core/src/testing/`). Cleanup global à faire en une passe (centraliser dans `testing/`). Signalé par code-reviewer plan 116.
- **Affichage nature dans InfoPanel** — reporté à la refonte InfoPanel globale. Mécanique core livrée (plan 072), UI absente. Reprendre étapes 4 + 5 du plan 072 quand InfoPanel revu.
- **UI (menus, panels, timeline, log)** — à décider : Phaser overlay 2D ou HTML/CSS par-dessus. Trancher après spike selon stack retenue.
- **Plan 080 — Token optimization** (Phase 1+3 DONE, 2+4 partiel, 5 pending). Faits : rules `globs:` → `paths:` (conditionnel), CLAUDE.md/RTK.md/MEMORY.md compressés, 27 agents (-3), **U-A autoUpdate désactivé** (refresh manuel via `/understand-anything:understand-diff`, session-closer rappel auto). Skips : caveman hook (gain réel marginal ~35/tour), MCP audit (schemas deferred OK). Décision restante humain : skills à désinstaller (liste plan), validation `/cost` session fraîche pour mesurer gains.

## Fait récemment

- 2026-06-08 — **Plan 117 terminé — B4 « Terrains » (4 moves poseurs + moteur Champs + Champ'Duit). Total : 386 moves (+4).** Système `FieldTerrain` (zones peintes diamant Manhattan r3, statiques, multi-zones coexistantes, double porte « sur tile + au sol »). 4 moves : Champ Herbu, Champ Électrifié, Champ Brumeux, Champ Psychique. Objet Champ'Duit (durée 5→8). Champ Psychique = barrière anti-dash (divergence Showdown #431). Gate CI : **2597 unit + 269 intégration**. Plan 112 B4 : 4/10 livrés. 6 moves dépendants → plan 118.
- 2026-06-07 — **Plan 116 terminé — B2 « Soin » (11 moves). Total : 382 moves (+11). Vague 1 roadmap maître terminée.** Nouveaux systèmes : HealTarget (soin cible résolue), CureTeamStatus, HealByTargetStat, PostHealOverTime, PostWish. Volatils HoT : Racines (1/8 PV/tour, déraciné si déplacement/knockback, perd immunité Vol/Lévitation) + Anneau Hydro (1/16 inconditionnel). Vœu différé model-agnostic (pendingWish + actionCounter, fire tour suivant cible, 50% PV max lanceur, targetsAllyOrSelf). Aromathérapie + Fontaine de Vie rayon Manhattan 2 autour lanceur. Vole-Force soin = Atq effective cible (+ Atq -1). Dévorêve gate cible endormie. Boule Pollen = Blast r1 (dégâts ennemis + soin alliés via appliesIf). Fix filtre immunité-type (moves soin/statut ne sont plus bloqués par efficacité type). 11 badges Racines/Anneau Hydro/Vœu renderer, tooltips, log, i18n FR/EN. 46 tests ajoutés. Gate CI verte.
- 2026-06-06 — **Plan 115 terminé — B3 Dégâts conditionnels. Total : 371 moves (+17).** Horloge d'actions (`actionCounter` + stamps model-agnostic round/CT, réutilisable B7/B9/B10). 17 moves livrés : Avalanche, Vendetta, Assurance, Voix Envoûtante, Feu Envieux, Écho, Chant Canon, Poing de Colère, Vengeance, Trépignement, Ronflement, Dernier Recours, Chargeur, Baston, Aire d'Herbe/Feu/Eau. Divergences Showdown documentées (priorité Avalanche/Vendetta adaptée, crescendo Écho cross-équipe via action-stamp, combo Gages différé B4). Gate CI : **2500 unit + 269 intégration verts**.
- 2026-06-05 — **Plan 113 terminé — B1 « Quasi-prêt ». Total : 354 moves (+6).** 4 extensions moteur : override Déf physique (`hitsPhysicalDefense` → Choc Psy/Frappe Psy), override type-efficacité (`typeEffectivenessOverride` ×2 Eau → Lyophilisation), multi-hit puissance croissante + précision par coup (`escalatingHitPower`/`perHitAccuracy` → Triple Axel), crash-on-miss ½ PV (`crashOnMiss` → Pied Voltige/Talon-Marteau, déclenché aussi par Protection/immunité via scan events). 4 tags tooltip + i18n FR/EN. 28 tests positionnels. 3 moves mis de côté (Frustration/Retour/Puissance Cachée — inutilisables gen 8/9, watch Pokémon Champions). Gate CI : **2430 unit + 269 intégration + build + lint + typecheck verts**.
- 2026-06-05 — **Plan 111 terminé — Moves liés au poids. Total : 348 moves (+4).** Champ `weight: number` (kg) sur `PokemonInstance`, propagé depuis `PokemonDefinition.weight`. 2 nouveaux `DynamicPowerKind` : `TargetWeight` (6 paliers parité Showdown) + `WeightRatio` (7 cas, palier ×3 inclusif → 80). **Balayage** (`low-kick`, Combat, Phys) + **Nœud Herbe** (`grass-knot`, Plante, Spé) + **Tacle Lourd** (`heavy-slam`, Acier, Phys) + **Tacle Feu** (`heat-crash`, Feu, Phys). Tous Single 1-1. Tests : `dynamic-power-system.test.ts` (13 cas) + 4 positionnels. Gate CI : **2402 unit + 269 intégration + build + lint + typecheck verts**.
- 2026-06-04 — **Plan 110 terminé — Moves stat-source. Total : 344 moves (+2).** Enum `AttackStatSource` (UserDefense/TargetAttack) + champ `MoveDefinition.attackStatSource`, branché dans `calculateDamageWithCrit` via helper `resolveAttackStat`. **Bodypress** (`body-press`, Combat, utilise Déf du lanceur) + **Tricherie** (`foul-play`, Ténèbres, utilise Atq de la cible). Single 1-1 contact. Bagarre (`guts`) ne booste plus ces moves (Atk lanceur non utilisée). Tag MoveTooltip FR/EN. Fix i18n FR `Bodypress` (reference avait "Big Splash" → backlog). 14 tests (11 positionnels sur 2 fichiers dont Guts ne booste pas Bodypress + 3 unit damage-calc). Gate CI : **2376 unit + 269 intégration + build + lint + typecheck verts**.
- 2026-06-03 — **Plan 109 terminé — Power conditionnel. Total : 342 moves (+12).** Moteur `dynamicPower` + enum `DynamicPowerKind` + type `DynamicPowerSpec` + champs `MoveDefinition.dynamicPower` / `ignoresBurnAttackDrop`. 12 moves livrés : Façade, Châtiment, Choc Venin, Acrobatie, Force Ajoutée, Boule Élek, Gyroballe, Gigotage, Contre, Saumure, Pression Extrême, Giclédo. Tag renderer MoveTooltip « Puissance variable ». Familles Poids/Timing/Compteurs/Terrain différées. Gate CI : **2361 unit + 269 intégration verts**.
- 2026-06-02 — **Plan 107 terminé — Content Batch G6 (11 moves simples ratés). Total : 330 moves. Tous batches G clos.** Force (Single 1-1), Écrasement (Single 1-1, Flinch 30%), Double Baffe (Single 1-1, hits:2), Camaraderie (Single 1-3, Atq -1 + bypassAccuracy), Rafale Feu / Végé-Attaque / Hydroblast (Line 5, recharge), Giga Impact (Dash 3, recharge), Lame Solaire (Single 1-1, charge+sun-skip), Laser Météore (Line 5, charge+AtqSpé+1 self), Trempette (Self, no-op). Core : `validate.ts` exempte `TargetingKind.Self` (décision #410) ; golden figé `GOLDEN_MOVESETS` 55 actions/5 rounds (décision #411). Gate CI : **1624 unit + 269 intégration + build + lint + typecheck verts**.
- 2026-06-02 — **Consolidation + audit roadmap moves**. Roadmap : 2 blocs Phase 4 fusionnés (lot livré `[x]` plans 088–101 + liste unique « Mécaniques restantes »). 10 deferred batches G tous casés (nouveaux points : Buff multi-allié pour Grondement/Influx Magnétik, Effet conditionnel état cible pour Venimprégne). **Audit complet 185 restants** (calcul réel via learnsets roster) : pool learnable = 494, écart compteur 159→**185** corrigé. Ajout **Batch G6** (11 simples ratés : Force/Écrasement/Double Baffe/Camaraderie/recharge×4/Lame Solaire/Laser Météore/Trempette — `move.recharge` réutilisable confirmé). Points dédiés **Morphing/Transform** (Mew + Ditto/Imposter Phase 9) et **Puissance Cachée** (résolu : Champions sans IV → Ténèbres 60 BP fixe). Corps Perdu sorti de G6 (never-miss + prio -1). Recale counts Item ~8→12, Misc ~9→26. Changement doc seule.
- 2026-06-02 — **Plan 106 terminé — Content Batch G5 (23 moves pure stat + pure statut)**. Total : **319 moves**. **Dernier batch G.** Zéro mécanique nouvelle. Debuffs ennemi Single (Grimace/Charme/Croco Larme/Ondes Étranges/Strido-Son/Regard Touchant/Confidence) + Chatouille (Atq+Déf) + Cone « tous ennemis » (Gros'Yeux/Mimi-Queue/Doux Parfum-Esquive/Sécrétion). Buffs : Armure (self Déf+1), Rengorgement (DoubleBuff), Poliroche (MajorBuff), **Coaching (1er buff-stat-allié générique via `targetsAlly`)**. Statut : Poudre Toxik/Para-Spore/Danse Folle (Zone r1), Gaz Toxik (Cone), Doux Baiser (Single), Vantardise (Atq+2+Conf), Flatterie (AtqSpé+1+Conf). **4 deferred** (mécanique core absente → mini-plans) : Dépit (PP), Venimprégne (conditionnel), Influx Magnétik (Plus/Minus), Grondement (multi-allié). Patterns via `move-pattern-designer` (7 noms FR corrigés depuis reference). Count test 296→319. Gate CI : **1624 unit + 269 intégration + build + lint + typecheck verts**.
- 2026-06-02 — **Plan 105 terminé — Content Batch G4 (36 moves dégâts + stat-drop / high-crit / recoil / drain)**. Total : **296 moves**. Recoil rammers en Dash 3 (Bélier/take-down, Éclair Fou/wild-charge, Rapace/brave-bird, Aquatacle/wave-crash, par cohérence volt-tackle/flare-blitz) ; Martobois/wood-hammer reste Single. Drain : Vampi-Poing/drain-punch (0.5), Vole-Vie/absorb (0.5), Vampibaiser/draining-kiss (0.75). High-crit (critRatio reference → Damage seul) : Lame de Roc/stone-edge, Tunnelier/drill-run (Line 2), Griffe Ombre/shadow-claw, Tranch'Air/air-cutter, Coupe Psycho/psycho-cut, Tranche-Nuit/night-slash. 22 stat-drop secondaire (Vit/Atk/Déf/DéfSpé/AtqSpé/Préc, données canon vérifiées). AoE canon → Zone (bulldoze r1, muddy-water/electroweb r2) / Cone (snarl 1-3, struggle-bug 1-2) / Slash (breaking-swipe/air-cutter/psycho-cut/night-slash/razor-shell). Bugfix hors-plan : flake `focus-blast.test.ts` (secondaire 10% non seedé, ~2.6% fail) → `createPrng(1)` déterministe. Gate CI : **1624 unit + 269 intégration + build + lint + typecheck verts**.
- 2026-06-02 — **Plan 104 terminé — Content Batch G3 (24 moves dégâts + secondaire statut/flinch/confusion)**. Vibraqua, Canicule, Sable Ardent, Vibrobscur, Vent Violent, Ébullition, Éclair, Élecanon, Feu d'Enfer, Poudreuse, Détritus, Purédpois, Extrasenseur, Ouragan (spéciaux) ; Psykoud'Boul, Direct Toxik, Tête de Fer, Détricanon, Poison Croix, Draco-Charge (+knockback 1), Étincelle, Étonnement, Frotte-Frimousse, Queue-Poison (physiques). Total : 260 moves. 3 moves sortis vers plans dédiés : Lyophilisation (type-chart override vs Eau), Ronflement (gate sommeil), Talon-Marteau (crash on miss). Enforcement noms FR durci (hook UserPromptSubmit + règle CLAUDE.md + data.md + agents).
- 2026-06-01 — **Plan 103 terminé — Content Batch G2 (23 moves dmg spécial + multi-hit)**. 13 spéciaux (swift, dragon-pulse, dazzling-gleam, hyper-voice, overheat, vacuum-wave, leaf-storm, aura-sphere, magical-leaf, power-gem, disarming-voice, draco-meteor, shock-wave) + 10 multi-hit (dual-wingbeat, rock-blast, scale-shot, bullet-seed, double-hit, pin-missile, fury-attack, bone-rush, icicle-crash, tail-slap). Total : 236 moves. 5 moves sortis du batch : psyshock/psystrike (dégâts spé sur Déf physique), alluring-voice/burning-jealousy (secondaire conditionnel si cible boostée), triple-axel (escalade multi-hit) — plans dédiés.
- 2026-05-31 — **Plan 102 terminé — Content Batch G1 (40 moves dmg pur physique)**. 40 entrées `tacticalOverrides` + noms EN. Total : 213 moves. Riders complexes différés (throat-chop, lash-out, temper-flare, fury-cutter, ice-spinner, steel-roller, supercell-slam, fell-stinger, psychic-fangs, raging-bull, poltergeist).
- 2026-05-30 — **Plan 101 terminé — Encore + Disable (Encore / Entrave)**. 2 moves disruptors move-locking livrés. `PokemonInstance.lastUsedMoveId?` (champ persistant, écrit à la complétion de `executeUseMove`). `VolatileStatus.moveId?`. `StatusType.Disabled`/`Encored`. `EffectKind.Disable`/`Encore` + handlers dédiés `handle-disable.ts`/`handle-encore.ts` (check Substitute en tête, échec propre si pas de lastUsedMoveId). 6 nouveaux `BattleEventType` (MoveDisabled/MoveEncored/DisableBlocked/EncoreBlocked/DisableFailed/EncoreFailed). `getLegalActions` : Disable retire 1 move, Encore ne garde que l'encored ; mobilité conservée. `executeUseMove` gardes défensives. Refactor `taunt-tick-handler.ts` → `timed-volatile-tick-handler.ts` (généralisé Taunted+Disabled+Encored, fin anticipée Encore si PP 0). `handleKo` vide désormais `volatileStatuses`. Helper testing `buildMoveRegistry`. Data : disable/encore dans `tactical.ts`, 4 OP sets (alakazam-disable/gengar-disable/hypno-encore/mr-mime-encore → 173→177 sets). Renderer : InfoPanel badges `Entrave/Encore {turns}t`, MoveSelector grisé, MoveTooltip tags rouges, BattleLogFormatter 8 cas, floating text, GameController. i18n FR/EN 6 clés. IA : `scoreDisableApplication`/`scoreEncoreApplication` + helpers `lastMoveIsThreat`/`lastMoveIsLowValue` + garde-fou score 0 si pas de lastUsedMoveId. Bugfix hors-plan : terrain-integration.test.ts flake (PRNG non seedé → seed `createPrng(0)`). Gate CI : **1624 unit + 269 intégration + typecheck + biome ci packages clean + build verts**.
- 2026-05-28 — **Plan 100 terminé — Taunt (Provoc)**. Move disruptor anti-setup livré : `StatusType.Taunted` volatile 3 tours. Check `executeUseMove` (avant PP) + `getLegalActions` filtre moves statut si taunted. `taunt-tick-handler.ts` décrement EndTurn. Interactions canon : Sub bloque Taunt, Safeguard et Mist ne bloquent pas. `taunt` dans tactical.ts + 4 OP sets (alakazam/gengar/aerodactyl/starmie-taunt — 169→173 sets). Renderer : badge InfoPanel `Provoc {turns}t`, MoveTooltip tag rouge, floating text orange, BattleLogFormatter 3 cas. IA : `scoreTauntApplication` (×1.8 si ≥40% moves statut, ×0.3 si HP < 30%). i18n FR/EN 5 nouvelles clés. Gate CI : **1618 unit + 254 intégration (+8 Taunt) + lint + typecheck + build verts**.
- 2026-05-25 — **Plan 099 terminé — Substitute**. Move iconique Gen 1 livré : `PokemonInstance.substituteHp?` field + `EffectKind.PostSubstitute` + damage/status/stat-change interception. Coût 25% maxHp, sub absorbe dégâts/statuts/baisses-stats ennemis. Sound flag (32 moves canon dont sing/growl/screech) + bypasssub bypass auto. Drain heal + recoil basés sur dégâts totaux (canon Gen 5+). Renderer swap atlas `dummy` (sprite déjà extrait) via `getEffectiveSpriteId()` helper. IA threat-aware (low-HP → 0, déjà sub → -1, menace alentour → ×1.5). 7 OP sets (mewtwo/alakazam/gengar/jynx/vaporeon/zapdos sub variants). 10 tests intégration. Gate CI : **1618 unit + 246 intégration + typecheck + build verts**.
- 2026-05-25 — **Plan 098 terminé — Brume + Rune Protect (Team Protections bundle)**. 2 moves canon livrés. Refactor unifié `ScreenAura` → `TeamAura` (4 kinds : Reflect/LightScreen/Mist/Safeguard). Hooks `isProtectedFromStatDecrease` (handle-stat-change + Intimidate ability) et `isProtectedFromStatus` (handle-status). Events `StatChangeBlocked` / `StatusBlocked` + `ProtectionReason` const enum. **Bug critique** : `attacker.playerId === target.playerId` trop strict → corrigé en `attacker.id === target.id` (self-induced bypass uniquement — Rest, Struggle, etc.). IA `threat-detection.ts` + scoring threatBonus ×1.5. Renderer indicateurs 4 kinds + InfoPanel badges + BattleLogFormatter. i18n FR/EN 11 nouvelles clés. Gate CI : **1618 unit + 236 intégration + typecheck + lint + build verts**. Plan 095 zéro régression.
- 2026-05-24 — **Plan 097 terminé — Loaders + FOUC fixes**. `index.html` preload font + `font-display: block` (kill FOUC). `LoadingOverlay` Phaser composant (progress bar + label X/Y + tips rotatifs 3s). 8 tips i18n FR/EN. `BootScene` minimal (fonts ready + UI shared preload). `BattleLoadingScene` parallèle (tips combat). `BattleScene` lazy sprites strict (engaged IDs only, dédupliqué cross-teams). `MapSelectPreviewScene` camera fade 150ms. Tests : 9 nouveaux unit (loading-tips, extract-engaged-ids). Gate CI : 1616 unit + 220 intégration verts.
- 2026-05-21 — **Plans 088–091 terminés — TP moves + semi-invul, normalisation IDs kebab, Sandbox Studio + design system**. TP moves (`TargetingKind.Teleport`), états semi-invulnérables (Flying/Burrowing/Diving/Vanished), IDs kebab 100% dans reference, refonte Sandbox Studio (dummyControl, MovePickerModal mutualisé), migration design system (tokens CSS, helpers DOM `ui/dom/`). Gate CI : 1552 unit + 191 intégration.
- 2026-05-20 — **Bugfixes hors plan (caméra, learnsets, hyper-fang flaky, gender toggle, MapSelect)**. `recenterOnActivePokemon` force=true, learnsets Gen 1 vides corrigés (10 mons), `buildMoveTestEngine` options object, bouton ♂/♀ TeamEditScene, reset state Phaser MapSelect. Gate CI : 1524 unit + 189 intégration.
- 2026-05-19 — **Plans 086–087 terminés — Refonte TeamSelectScene + playable-pokemon**. PlacementPhase étendu (sous-pick format), scene DOM 3 colonnes, playable-pokemon module (movepool dérivé OP sets + learnset). Gate CI : 1514 unit + 189 intégration.

---

## Contexte prochaine session

**Phase 3 terminée (2026-04-26). Phase 4 en cours (plans 069–117 terminés, dernier 2026-06-08).** Abilities, genres, natures, objets tenus, SP, Roster Batch A-E, Météo complète, Team Builder UI + polish, refonte TeamSelectScene + sous-pick au placement, refactor roster-poc → playable-pokemon, TP moves + semi-invulnérables, normalisation IDs kebab build-time, charge moves + HitAndRun + Baton Pass, barrières aura mobile (Reflect/Light Screen), Team Protections bundle (Mist/Safeguard) + refactor TeamAura unifié 4 kinds, pipeline itch.io, loading screens + FOUC, Substitute, Taunt, Encore + Disable, Content Batches G1–G6 (168 moves — **tous batches G clos**), couverture tests 100% moves + garde-fou CI, Power conditionnel moteur dynamicPower + 12 moves, Moves stat-source (Bodypress/Tricherie), Moves liés au poids (Balayage/Nœud Herbe/Tacle Lourd/Tacle Feu), B1 Quasi-prêt (plan 113), B3 Dégâts conditionnels (plan 115 : horloge d'actions, 17 moves), B2 Soin (plan 116 : 11 moves), **B4 Terrains (plan 117 : moteur Champs zonés + 4 poseurs + Champ'Duit, 386 total)**. 80 Pokemon jouables + dummy — roster Gen 1 complet (hors Ditto). 177 OP sets. **386 moves.** Prochain : roadmap maître plan 112, **117 moves restants** classés par système (B4 dépendants → plan 118). Voir "À faire maintenant" ci-dessus.

**Référence archive plan 061** : branche `plan-061-occlusion-before-3d-pivot`. Consulter si besoin de comprendre ce qui a été tenté en 2D iso avant pivot.

---

## Conventions de mise à jour (pour Claude)

- **À faire maintenant** : 1 à 3 items max. Item principal en premier.
- **Reporté** : `- [agent/action] — raison`. Ex: `- visual-tester plan 060 — dev server redémarré nécessaire`.
- **Fait récemment** : `- YYYY-MM-DD — ce qui a été fait`. Cap 10, vire les plus anciens.
- Mettre à jour fin de plan, fin d'étape significative, ou quand agent reporté.
- Item "À faire" → "Fait" : déplacer.
- Item "Reporté" impertinent : supprimer avec ligne dans "Fait".
