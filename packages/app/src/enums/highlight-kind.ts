/**
 * Board highlight kinds moved to `@pokemon-tactic/render-ports` (plan 125).
 * Re-exported here so existing renderer import sites stay unchanged.
 */
export { HighlightKind } from "@pokemon-tactic/render-ports";
